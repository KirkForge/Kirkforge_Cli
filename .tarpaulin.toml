[tarpaulin]
# Skip integration tests that require a live Ollama server; unit tests cover
# the bulk of the logic.
exclude-files = ["tests/integration_test.rs"]
# Exclude the binary entry point and build script from coverage targets.
exclude = ["kirkforge", "build_script_build"]
