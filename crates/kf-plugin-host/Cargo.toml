[package]
name = "kf-plugin-host"
version = "0.2.0"
edition = "2021"
description = "Runtime host for KirkForge plugins — registry, trust policy, and dispatch."

[dependencies]
# SDK manifest types live in src/sdk.rs (folded from kf-plugin-sdk, WO 47.4).
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
tracing.workspace = true
thiserror.workspace = true
toml.workspace = true
libc = "0.2"
minisign-verify = "0.2"

[dev-dependencies]
tempfile.workspace = true
minisign = "0.9"
