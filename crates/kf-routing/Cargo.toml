[package]
name = "kf-routing"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
repository = "https://github.com/KirkForge/KirkForge-Cli"
description = "Pure Rust port of the kf-plugin orchestrator's pure modules: routing engine, classifier, correction loop, truth model, task profiles, cost rates, path safety"

[dependencies]
serde = { workspace = true }
sha2 = "0.10"
regex = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
serde_json = { workspace = true }
proptest = "1.6"
