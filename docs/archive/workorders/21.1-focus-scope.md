# Workorder 21.1 — Scope-Creep Decisions: Cut / Spin-off / Keep-and-Finish

## Status: Done

## Problem

The review's Part 3 "scope creep" verdict still stands: ~50K LOC of
satellite-crate bloat (draw + video + redundant CLI/hosts splits) that "the
agent gets no weaker" without. `kf-compress-hosts` was already cut; the rest is
still present. This workorder records the **decision** for each remaining
scope-creep crate, with the owner's stated direction folded in: the 3
rust-native plugins (draw/video/stratum) are wanted **finished, not cut** (see
21.2), so their items here carry a "keep-and-finish" lean and a documented
flip-to-cut condition.

## Source

Review Part 3 + reality-check Part 3 table (`a870571`). Folds WO 20.0.9 §S1/§S2
scope items forward with an explicit decision gate.

## Targets

### 21.1-R1 — `kf-draw` decision: keep-and-finish vs spin-off (HIGH · est M · dep: —)

**Reality check (`a870571`):** "`Cargo.toml:243` `default = [\"stratum\",\"budget\"]`
— `draw` removed from default. **ADR-0003-document-model.md still missing**;
`kf-draw-core/src/lib.rs:10` still dangles the reference. ADR-048:13,29 still
claims 'default: enabled' — now stale." Crates unchanged at 22,888 LOC; zero
users (3 `.td.json` files, all author demos in `examples/`).

**Scope:** `Cargo.toml` (default features + workspace members if spinning),
`docs/adr/0048*`, the keep/cut decision record. (ADR-048 text reconciliation
itself lives in 21.9-R3 with `dep: 21.1-R1`.)

**Success (done):**
- A decision is recorded (ADR or workorder note): **keep-and-finish as a
  rust-native plugin** (owner direction, per 21.2-R2) OR spin-off to its own
  repo. Either way the decision is explicit, dated, and signed off.
- If keep-and-finish: `kf-draw`'s default-off status is confirmed intentional
  and the compiled-in path is fixed (tracked in 21.2-R2); ADR-048 updated to
  "default: disabled (opt-in); rust-native plugin" (via 21.9-R3).
- If spin-off: `kf-draw` + `kf-draw-core` removed from this workspace;
  `Cargo.toml` workspace members trimmed; binary size delta measured and pasted.

**Failure (regression / not-done):**
- No decision recorded, or decision recorded but ADR-048 still claims
  "default: enabled" = FAIL.
- Spin-off chosen but a dangling `kf-draw-core/src/lib.rs:10` reference to the
  missing ADR-0003 remains in-tree = FAIL.
- Keep-and-finish chosen but 21.2-R2 (compiled-in path) left open = FAIL.

**Defer:** none. Decision is the deliverable.

### 21.1-R2 — `kf-video` decision: keep-and-finish vs remove (+ dispatch bug evidence) (HIGH · est M · dep: —)

**Reality check (`a870571`):** "`Cargo.toml:247` not in default;
`default_plugin_sources()` (`config/tools.rs:56-57`) only inserts it under
`#[cfg(feature=\"video\")]` → on a default build it's **neither compiled-in nor
shell-loaded**. ADR-049:33 claims a shell fallback that doesn't fire on the
default build." 11,224 LOC; zero users (6 demo `.mp4`s, all from `kf-video
demo`; zero `brand.json`).

**Scope:** `Cargo.toml`, `config/tools.rs:56-57`, the keep/cut decision record.
The dispatch bug *fix* is owned by 21.2-R3; this item owns the decision +
ADR-049 reconciliation.

**Success (done):**
- Decision recorded: **keep-and-finish** (owner direction, 21.2-R3) OR remove.
- If keep-and-finish: ADR-049:33 rewritten to match reality (the shell fallback
  does not fire on default builds until 21.2-R3 lands); 21.2-R3 tracked as the
  blocking dependency.
- If remove: `kf-video` deleted from workspace; ADR-049 marked Superseded;
  ~11.2K LOC delta measured and pasted.

**Failure (not-done / regression):**
- Decision deferred without a flip-condition = FAIL (silent deferral).
- ADR-049:33 still claims a working default-build fallback after the decision =
  FAIL.
- Keep-and-finish chosen but dispatch bug (config/tools.rs:56 gating) left
  unfixed = FAIL (that's 21.2-R3 failing, which blocks this).

**Defer:** none.

### 21.1-R3 — Remove `kf-budget-hosts` stub crates (MEDIUM · est S · dep: —)

**Reality check (`a870571`):** "`kf-budget-hosts` (634 LOC, stubs) — STILL
PRESENT. `lib.rs:6` still 'Only Claude Code has wired CLI hook handlers
today'." The Cursor/Aider/KfCode modules are documented-intended-shape stubs
that never funded.

**Scope:** `crates/kf-budget-hosts/`, root `Cargo.toml` workspace members, any
imports, `docs/adr/0002*` (the decomposition justification) update.

**Success (done):**
- Cursor/Aider/KfCode stub modules removed; only the wired Claude Code host
  retained (or the whole crate inlined if that's cleaner — match the
  `kf-compress-hosts` deletion precedent).
- Root `Cargo.toml` no longer lists the removed crate; `cargo check --workspace`
  green; the multi-host *intent* documented in an ADR if not already.

**Failure (regression):**
- Stubs retained "for a future contributor" = FAIL (paying interest on an
  unfunded loan).
- Workspace fails to build after removal = FAIL.

**Defer:** none.

### 21.1-R4 — Fold/delete redundant `kf-budget-cli` (14,038 LOC) (MEDIUM · est S · dep: 21.1-R3)

**Reality check (`a870571`):** "`kf-budget-cli` (14,038 LOC) + `kf-compress-cli`
(1,924 LOC) — STILL PRESENT. Standalone redundant CLIs unchanged." The core
binary already folds the budget library in; no evidence of external users.

**Scope:** `crates/kf-budget-cli/`, root `Cargo.toml`, any docs/README
references, ADR-0002 reconciliation.

**Success (done):**
- `kf-budget-cli` removed (or its unique functionality confirmed folded into
  the core binary's `kf budget` subcommand, whichever the audit shows);
  `cargo check --workspace` + `cargo test --workspace` green.
- ~14K LOC workspace delta measured and pasted in the closing note.

**Failure (regression):**
- CLI retained because "might have users" with no evidence of any = FAIL.
- A budget feature only the standalone CLI exposed goes missing from the core
  binary = FAIL.

**Defer:** none.

### 21.1-R5 — Fold/delete redundant `kf-compress-cli` (1,924 LOC) (LOW · est S · dep: —)

**Reality check (`a870571`):** "`kf-compress-cli` (1,924 LOC) — STILL PRESENT.
Standalone redundant CLI unchanged." Sibling of 21.1-R4.

**Scope:** `crates/kf-compress-cli/` (note: its `main.rs:299` is one of the
empty-pipeline sites — see 21.3-R1), root `Cargo.toml`, docs.

**Success (done):**
- `kf-compress-cli` removed; its sole real caller path
  (`CompressionPipeline::new()`) is confirmed covered by the core binary or by
  21.3's transform wiring.
- `cargo check --workspace` green; ~1.9K LOC delta pasted.

**Failure (regression):**
- CLI retained with no justification = FAIL.
- A compress entry point disappears with no core-binary replacement AND 21.3
  hasn't landed its pipeline wiring = FAIL (coordinate with 21.3-R1).

**Defer:** none.

## Verification

- Each of the 5 crates above has a recorded decision (cut / spin-off /
  keep-and-finish) with a dated sign-off.
- Removed crates: absent from `Cargo.toml` workspace members; `cargo check
  --workspace --all-targets` green; LOC delta pasted.
- Kept crates: their completion tracked in 21.2; their ADRs reconciled (via
  21.9-R3).
- Net `crates/` LOC delta measured and recorded in `state.md`.

## Coverage check (orphan guard)

Folds review Part 3 rows: kf-draw (R1), kf-video (R2), kf-budget-hosts (R3),
kf-budget-cli (R4), kf-compress-cli (R5). The `kf-compress-hosts` row is FIXED
(excluded). Bench-tasks-rigged row is FIXED (excluded). No review Part 3 item
left unplaced.
