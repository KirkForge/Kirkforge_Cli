# Workorder 23.0 — WO 23 Series Overview: Phase 3 Differentiators

## Status: Done

## Problem

Phase 0 (silent breakages) and Phase 1 (scope creep) are DONE. Phase 2 is ~50% done (regex grep, replace_all, extended-thinking, lessons.md closed; PTY, AppState, mouse still open). The remaining Phase 2 and Phase 3 items are competitive gaps — features that make KirkForge's differentiators actually differentiate, not just exist.

This series closes every open Phase 2 item and every Phase 3 item from the Phase 2/3 review roadmap that is not yet shipped.

## Source

Code review roadmap — Part 6 (Phase 2–3), updated through WO 22.

## WO 23 sub-workorders

| # | Workorder | Review # | Theme | One-line scope |
|---|---|---|---|---|
| 23.1 | [pty-streaming-bash](23.1-pty-streaming-bash.md) | #16 | PTY + streaming for bash | Replace pipe-based stdout/stderr capture with `portable-pty` behind `cfg(feature = "pty")`; stream partial output during execution |
| 23.2 | [appstate-decomposition](23.2-appstate-decomposition.md) | #20 | Decompose AppState (~63 fields) into sub-structs | Extract `ApprovalState`, `SearchState`, `CostState`, `CacheState` from `AppState`; add builder/default |
| 23.3 | [mouse-click-drag](23.3-mouse-click-drag.md) | #21 | Add mouse click/drag to TUI | Handle `MouseDown`/`MouseUp`/`MouseDrag` in `handle_mouse`; click-to-select, drag-to-scroll |
| 23.4 | [lsp-context-federation](23.4-lsp-context-federation.md) | #23 | Wire LSP into context index | Call `lsp_query` ops from context-index retrieval to ground name-based resolution with LSP definitions/refs |
| 23.5 | [remember-tool](23.5-remember-tool.md) | #25 | Add `remember` tool + system-prompt instruction | Expose a `remember` tool so the model can explicitly store facts; add system-prompt instruction for memory auto-population |
| 23.6 | [mcp-first-overlay](23.6-mcp-first-overlay.md) | #27 | MCP-first overlay (ADR-050 written, not shipped) | Fold hooks/verifiers into MCP middleware; deprecate bespoke tool/skill path; document transition |
| 23.7 | [task-concurrency-semaphore](23.7-task-concurrency-semaphore.md) | #28 | Real concurrency limit for `task(background=true)` | Make the existing `bg_semaphore` configurable via `Config`; expose `max_background_tasks` setting |
| 23.8 | [doom-loop-circuit-breaker](23.8-doom-loop-circuit-breaker.md) | #29 | Hard circuit-breaker for doom loops | After N doom-loop hits, auto-switch to plan mode or halt; not just hint injection |
| 23.9 | [max-tokens-hard-cap](23.9-max-tokens-hard-cap.md) | #30 | Max-tokens continuation hard cap | Add a hard cap on continuation rounds so `FinishReason::Length` cannot loop indefinitely |

## Cross-cutting rules (from AGENTS.md)

- Every item must have a pasted gate run + head SHA before closing.
- No silent deferrals — any item not fully completed must be disclosed with (a) what, (b) why, (c) remaining work, (d) where tracked.
- Changes to architecture, plugin system, feature flags, verifier bus, or context index MUST also update `docs/TECHNICAL.md`.
- ADR changes MUST update both the file header and `docs/adr/README.md` index.

## Verification (whole series)

- `cargo test --locked --workspace --no-fail-fast` green
- `cargo clippy --all-targets -- -D warnings` green
- `cargo fmt --check` green
- `cargo check --workspace --all-targets` green
- `cargo test -p kf-budget-core --test adr_xref_drift` green (if any ADR touched)
- `scripts/ci_local.sh` green