# Workorder 28.0 — WO 28 Series Overview: Open Issues + Tech Debt + Deferred Work

## Status: In Progress

## Problem

WO 27 closed the deep-review CRITICAL (C1 landlock) + 5 of 7 sub-workorders (plugin-trust, themes, mouse, + the test-health purge). The gitnexus reindex (2026-08-12, 17,665 nodes) confirmed the remaining architecture debt (2 Rust cycles, 5 TS cycles) + the change blast radius (47 files / CRITICAL). This series closes EVERY remaining open issue from the deep review, the state.md tech-debt ledger, and the prior-WO deferred items — each on its own workorder, each spelled out with root cause + file:line + approach + gates. No defer-the-can.

## Source

- `kf-code-review.md` 7th edition — open findings H3-remaining, H5-remaining, H7, H8-remaining.
- `state.md` "Deferred items (explicitly tracked)" — the full ledger.
- gitnexus reindex: `check --cycles` (7 cycles), `impact` / `context` / `trace` (structure analysis).
- The 3 planning subagent reports (arch, tests, deferred-hunt).

## The 17 workorders

### Architecture + security (28.1-28.5)
| WO | Title | Root cause | Est |
|----|-------|------------|-----|
| 28.1 | Break tools↔session cycle (H7) | 24 import sites reach UP into session; task.rs constructs Executor | L |
| 28.2 | Break budget↔stratum cycle (NEW) | maybe_escalate_stratum() @ budget.rs:349 ↔ stratum listener types | S |
| 28.3 | Split turn.rs god-object (H8-rem) | 2087 LOC; dispatch_tool_call_batch/stream_iteration/pre_run_verdict oversized | M |
| 28.4 | Fix TS plugin-SDK cycles (NEW) | 5 barrel-re-export cycles in npm/kf-plugin/packages/ | M |
| 28.5 | Landlock on background bash jobs (27.5 follow-up) | resolve_paths is private; pub(crate) re-export + plumb to bash_jobs spawn | S |

### Test health + CI (28.6-28.12)
| WO | Title | Root cause | Est |
|----|-------|------------|-----|
| 28.6 | e2e binary-spawn hang (27.2 R1) | run_line_mode startup wedges (daemon/plugin/MCP); in-process test works | M |
| 28.7 | Coverage gate (27.2 R4) | baseline TBD; check-cov-regression.sh missing; kf-testdoctor guard ignored | M |
| 28.8 | daemon/client.rs tests (27.2 R5) | 549 LOC, zero tests; TUI↔daemon transport unverified | M |
| 28.9 | Session coverage >75% (24.6/25.16) | src/session ~68.6%; 19 untested executor/budget/compaction/verifier flows | XL |
| 28.10 | Slow-test feature-flag split (25.2-R4) | e2e tests run in every --workspace; gate behind required-features | S-M |
| 28.11 | Bench manifest validation (25.7-R3) | count exists in check-artifact; gap is TECHNICAL.md rows + schema | S |
| 28.12 | Dead-ref CI gate for src/ (25.8-R4) | check-artifact greps scripts/CI/docs but NOT src/+crates/ | S-M |

### Deferred work + capabilities (28.13-28.17)
| WO | Title | Root cause | Est |
|----|-------|------------|-----|
| 28.13 | Bedrock/Vertex test mocks (22.9-R4) | no CI contract tests for SigV4 + AWS event-stream + GCP token | M |
| 28.14 | Plugin3 env backward-compat shim (26.10-R2) | PLUGIN3_* → KF_BUDGET_* rename broke backward compat | S |
| 28.15 | Memory semantic dedup (26.10-R4) | near-duplicate facts accumulate; TF-IDF retrieval untightened | M |
| 28.16 | Anthropic computer_use beta (26.7-R4) | hosted API path (computer tool type + anthropic-beta header + vision loop) | L |
| 28.17 | Bash evasion tripwire posture (H3-rem) | encoding/indirection evade deny-list; document tripwire-vs-boundary | S |

## Ordering + dependencies

```
28.5 (bg landlock, trivial)     ─── ship first, closes 27.5 deferral
28.2 (budget↔stratum, small)    ─── independent, quick win
28.1 (tools↔session, L)         ─── biggest architecture win; do after 28.2/28.5
28.3 (turn.rs split, M)         ─── independent of 28.1
28.4 (TS cycles)                ─── NOTE: may be superseded by WO 29 (TS→Rust SDK migration)
28.6 (e2e hang)                 ─── unblocks real e2e CI coverage
28.7 (coverage gate)            ─── unblocks 28.9
28.8 (daemon tests)             ─── independent
28.9 (session 75%)              ─── depends on 28.7 (the gate)
28.10/28.11/28.12 (CI gates)    ─── independent, parallelizable
28.13-28.17 (capabilities)      ─── independent; 28.16 is L-sized
```

Highest leverage: **28.1** (the cycle break unblocks crate extraction), **28.6** (real e2e coverage), **28.7+28.9** (coverage enforcement). The quickest wins: **28.5** (5 lines), **28.2** (move one type), **28.14** (env shim).

## Note on WO 29 (TS→Rust SDK migration)

WO 28.4 (TS plugin-SDK cycles) may be SUPERSEDED by WO 29 — the planned migration of the entire `npm/kf-plugin/` SDK to Rust. If WO 29 lands first, 28.4 is moot (the TS code is deleted). If 28.4 lands first, it's a tactical fix that WO 29 then erases. Decide based on sequencing: if WO 29 starts immediately after 28, skip 28.4.

## Gate (whole series)

- Each WO: its own commit, its own gate pasteed with the head SHA.
- CI green on `dev` before any WO is marked Done.
- AGENTS.md §9 doc-sync + §11 defer-disclosure.
- The coverage gate (28.7) must land before 28.9 (session 75%) can verify.
