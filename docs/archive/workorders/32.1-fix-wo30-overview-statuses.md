# Workorder 32.1 — Fix Stale WO 30 Overview Statuses

## Status: Done

## Problem

`docs/workorders/30.0.0-wo30-overview.md` marks WO 30.1, 30.2, 30.5, and 30.6 as
"OPEN" — but all four are shipped (verified by the 2026-08-14 audit subagents).
The README table (`docs/workorders/README.md:592-597`) is correct (says "Done");
the overview master index is stale. This is the exact failure mode AGENTS.md §9
warns about.

## Evidence (verified 2026-08-14)

| WO | Overview says | Actual | Evidence |
|----|--------------|--------|----------|
| 30.1 | OPEN | **Done** (`8717bd1`) | `src/session/worktree.rs:14-99` — `WorktreeSession::create` runs `git worktree add` |
| 30.2 | OPEN | **Done** (`63800ed`) | `src/tools/task.rs` — `status()`/`cancel()`/`list()` + `TaskMetadata` |
| 30.5 | OPEN | **Done** (`04c8f60`) | `crates/kf-context-index/src/lib.rs:989` — `filter_entry` skips `target`/`.git`/`node_modules` |
| 30.6 | OPEN | **Done** (`8717bd1`) | `src/session/task_spawner.rs:236-252` — forwards to parent approval channel |

## Approach

Update the WO 30 overview table rows (lines 163-168) to match the README statuses.
Also update the `#[ignore]` audit count (line 38: "36 total" → "35 total" — the
audit found 35, not 36; the extra count was a stale entry from before the e2e
fixes shipped).

## Files to touch

- `docs/workorders/30.0.0-wo30-overview.md` — lines 163-168 (status column) + line 38 (count)

## Gate

- `cargo test -p kf-budget-core --test adr_xref_drift` (docs consistency)
- Visual: overview statuses match README table

## Done condition

All WO 30 overview rows match the README table. No item marked OPEN when it's Done.