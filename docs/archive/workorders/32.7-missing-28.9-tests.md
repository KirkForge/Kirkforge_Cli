# Workorder 32.7 — WO 28.9 Missing 11 Tests (R3/R4/R5 — Silent Deferral)

## Status: Done (`b294295`)

## Problem

WO 28.9 planned 19 tests (R2.1-R2.6 = 6, R3.1-R3.4 = 4, R4.1-R4.5 = 5, R5.1-R5.4 = 4).
Only 5 shipped. R2.5 and R2.6 are explicitly disclosed as deferred. The remaining
**11 tests** (R3 budget-slicing + R4 compaction + R5 verifier-bus) are **not
mentioned in the status at all** — this is a potential AGENTS.md §11 (no silent
deferral) violation.

Verified against source: only 1 of the 12 named tests exists
(`slice_keeps_tool_call_tool_result_pairs_intact` at
`src/session/prompt/compaction.rs:772`). The other 11 do not exist anywhere.

## Root cause

The WO 28.9 worker shipped 5 tests, disclosed 2 as deferred (R2.5/R2.6), and
silently dropped the remaining 12 (R3/R4/R5). This is the exact failure mode
AGENTS.md §11 forbids: "Silently disabling, gating off, or stubbing a requested
feature to make a commit 'pass' is a REGRESSION, not progress."

## Missing tests

**R3 — Budget slicing (4 tests):**
- `slice_trips_when_context_exceeds_threshold`
- `slice_selects_correct_messages_for_compaction`
- `slice_preserves_conversation_order`
- `slice_keeps_tool_call_tool_result_pairs_intact` — EXISTS (compaction.rs:772)

**R4 — Compaction (5 tests):**
- `compaction_triggers_at_threshold`
- `compaction_calls_llm_for_summary`
- `compaction_preserves_verifier_findings_tail`
- `compaction_retriggers_on_long_session`
- `turn_resumes_correctly_after_compaction`

**R5 — Verifier bus (4 tests):**
- `verifier_bus_broadcasts_every_tool_result`
- `verifier_registration_supports_dynamic_add_remove`
- `verifier_verdicts_aggregate_across_multiple_verifiers`
- `bus_disconnect_mid_turn_does_not_panic`

## Approach

1. Write the 11 missing tests (R3.1-R3.3, R4.1-R4.5, R5.1-R5.4).
2. Each tests a real flow in `src/session/` (slicing, compaction, verifier bus).
3. If a test proves genuinely blocked (e.g. needs a real LLM), disclose it
   explicitly per AGENTS.md §11 — don't silently skip it.

## Files to touch

- `src/session/budget.rs` — R3 slicing tests
- `src/session/prompt/compaction.rs` — R4 compaction tests
- `src/session/verifier/bus.rs` — R5 verifier-bus tests

## Gate

- `cargo test --workspace` — all 11 new tests pass
- `cargo clippy --all-targets -- -D warnings`
- Session coverage moves toward 75% (WO 28.9 target)

## Done condition

All 11 missing tests exist and pass. No silent deferrals. Session coverage
measured and reported.