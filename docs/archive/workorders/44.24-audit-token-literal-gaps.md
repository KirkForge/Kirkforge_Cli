# WO 44.24 — Audit scrubber misses github_pat_/glpat-/AIza/ya29 token literals

## Status: Done

## Problem

`scrub_free_text` (WO 43.3) redacts credential-shaped `NAME=value` pairs
and a fixed set of token literals (src/shared/audit.rs:1006-1012):

```
Bearer\s+[A-Za-z0-9_\-\.=]+ | sk-[A-Za-z0-9_\-]+ | ghp_[A-Za-z0-9]+ |
AKIA[0-9A-Z]+ | xox[bp]-[A-Za-z0-9\-]+
```

Four common token shapes are absent, so bare occurrences survive into
`audit.ndjson` via the three free-text fields that reach the scrubber
(bash `command` at audit.rs:943-949, plugin `args_summary` at :111, hook
`reason` at :96):

- `github_pat_[A-Za-z0-9_]+` — GitHub fine-grained PATs (only classic
  `ghp_` is matched; the existing test at audit.rs:1291 pins `ghp_` only)
- `glpat-[A-Za-z0-9\-_]{20,}` — GitLab PATs
- `AIza[0-9A-Za-z_\-]{35}` — Google API keys
- `ya29\.[A-Za-z0-9_\-]+` — Google OAuth access tokens (the existing
  test at audit.rs:1287 passes only because that sample sits behind a
  `Bearer ` prefix; a bare `ya29.` literal is not matched)

The audit log is the persistent forensic record (survives the session,
per-entry fsync); a fine-grained GitHub PAT echoed into a hook reason or
bash command line today lands on disk in cleartext.

## Root cause

The literal list in WO 43.3 was transcribed from the most common shapes
in the original incident; the provider coverage was never completed and
the tests only exercise the shapes that are covered.

## Approach

1. Extend the `literals` alternation in `scrubber()` (audit.rs:1011) with
   the four shapes above. Keep alternation ordering safe (the regex is a
   single alternation; no lookarounds needed since these prefixes are
   distinct from `sk-`/`ghp_`).
2. Tests: each new shape, BARE (no `Bearer` prefix, no `NAME=`), must
   vanish from `scrub_free_text` output and produce `[REDACTED]`; a
   non-token `AIzaphenia` style false-positive check (`AIza` + short
   suffix should NOT be redacted — keep the 35-char length quantifier).

## Files

- src/shared/audit.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`; new unit tests above must pass.

## Done

Extended the `literals` alternation in `scrubber()` (audit.rs:1015)
with four provider token shapes so bare occurrences vanish into
`[REDACTED]`:

- `github_pat_[A-Za-z0-9_]+` — GitHub fine-grained PATs (only classic
  `ghp_` was matched before).
- `glpat-[A-Za-z0-9\-_]{20,}` — GitLab PATs (20+ char body).
- `AIza[0-9A-Za-z_\-]{35}` — Google API keys (exactly 35 chars; the
  length quantifier is the false-positive guard — `AIzaphenia` survives).
- `ya29\.[A-Za-z0-9_\-]+` — Google OAuth access tokens (the existing
  test only passed because that sample sat behind a `Bearer ` prefix;
  bare `ya29.` was not matched).

The prefixes are distinct from `sk-`/`ghp_` (none is a prefix of
another), so the single alternation needs no lookarounds or
reordering. The three comment blocks that enumerate the literal shapes
(header at :983, inline at :1008, doc-comment at :1025) were updated to
list the new providers.

Tests: added `scrub_free_text_redacts_bare_provider_tokens`
(audit.rs:1322) covering each new shape BARE (no `Bearer` prefix, no
`NAME=`) — each must produce `[REDACTED]` and the raw token must not
appear in the output. Includes the false-positive guard: `AIzaphenia`
(`AIza` + short suffix) is NOT redacted, verifying the 35-char length
quantifier. The existing `scrub_free_text_redacts_all_token_literals`
test still passes (its `ya29.xyz` sample sits behind `Bearer` and is
matched by the `Bearer` arm, unchanged).

Gate (all four, warm cache):
- `scripts/test-fast.sh` → 4676 passed, 15 skipped (GATE1_EXIT=0).
- `cargo clippy --all-targets -- -D warnings` → Finished, no warnings
  (GATE2_EXIT=0).
- `cargo fmt --check` → clean (GATE3_EXIT=0).
- `cargo check --workspace --all-targets` → Finished (GATE4_EXIT=0).

No deferrals. No new deps. Smallest correct diff (regex alternation +
comments + one test).
