# WO 44.28 — Phase 2.5 symlink-swap re-verification is unreachable on the allowed path

## Status: Done

## Problem
The WO 38.1 symlink TOCTOU defense (`symlink_swap_denied`, `src/session/executor/dispatch.rs:724-738`) is
nested inside the `GuardVerdict::Denied` arm of the read-before-edit gate
(`src/session/executor/dispatch.rs:448-493`):

```rust
let needs_read_gate = name == "edit_file" || (name == "write_file" && path.exists());
if needs_read_gate {
    if let GuardVerdict::Denied(msg) = self.sandbox.check_edit(...).await {
        ...
        if let Some(msg) = symlink_swap_denied(&path) {   // ← only runs when ALREADY denied
```

The comment at dispatch.rs:453-462 says the walk runs "right before the body closes the
final-component and parent-swap cases **for reads and writes**", and commit `cfad1342`
(WO 38.1) intended "symlink component walk before the body". But:

1. When `check_edit` returns `Allowed` (the normal case — the model read the file earlier,
   which is exactly the attack precondition), the walk never runs; the tool body executes at
   dispatch.rs:497 with no symlink re-check. The walk only changes the *message* of a denial
   that was already happening for a different reason.
2. `read_file`/`read_image` never reach the walk at all: `needs_read_gate` is false for reads
   (dispatch.rs:447), so even the denied-path walk is edit/write-only. The in-tree test
   `symlink_swap_denied_blocks_swapped_file` (dispatch.rs:766-783) is explicitly documented as
   "the read-side `.ssh` swap case from the audit" — yet production never runs the walk on any
   read.

Attack shape (unchanged since WO 38.1 claimed it closed): batch `[bash, edit_file]` where the
bash call replaces a validated parent dir or the target file with a symlink between Phase-1
canonicalization and the Phase-2.5 body. Phase 1 resolved the real path; the gate passes
(file was read); the body then opens attacker-controlled symlink target.

## Root cause
The `if let GuardVerdict::Denied` binding placed the re-verification inside the denial branch
instead of before the gate, so the defense executes only on calls that were already blocked.
Reads were never wired into the walk despite the test naming the read-side case as the
motivating audit finding.

## Approach
1. Hoist `symlink_swap_denied(&path)` out of the `Denied` arm: run it unconditionally in
   Phase 2.5 for every deferred file call (read_file, read_image, write_file, edit_file)
   right before `run_prepared_call` (dispatch.rs:497), inserting the existing
   `ToolOutcome::Failure(AccessDenied)` result on a hit (mirrors dispatch.rs:463-478).
2. Keep the existing read-gate denial branch as-is; it no longer needs its own symlink copy.
3. Add a regression test in `src/session/executor/tests/dispatch.rs`: batch where a helper
   swaps the target for a symlink after Phase 1 but before Phase 2.5, with the file
   pre-marked read (gate allows) — the body must NOT run. Plus the read-side variant
   (`read_file` on a swapped final component).
4. Update the `ponytail:` walk-to-open residual-window comment to keep pointing at the
   openat2 upgrade path.

## Files
- `src/session/executor/dispatch.rs`
- `src/session/executor/tests/dispatch.rs`

## Gate
- `cargo test -p kf-code --lib executor::tests::dispatch` (new symlink tests pass)
- `scripts/test-fast.sh`
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --check`
- `cargo check --workspace --all-targets`

## Done

Hoisted `symlink_swap_denied(&path)` out of the `GuardVerdict::Denied` arm to run
unconditionally in Phase 2.5 for every deferred file call (`read_file`,
`read_image`, `write_file`, `edit_file`), right before `run_prepared_call`
(`src/session/executor/dispatch.rs:441-468`). On a hit it inserts the existing
`ToolOutcome::Failure(AccessDenied)` result and `continue`s, mirroring the
pre-fix denial shape. The read-gate's `Denied` arm keeps its own denial for the
read-before-edit reason but no longer needs its own symlink copy. The
`ponytail:` walk-to-open residual-window comment is preserved at
`dispatch.rs:449-452` and still points at the openat2(RESOLVE_NO_SYMLINKS)
upgrade path.

### Regression tests (`src/session/executor/tests/dispatch.rs`)
- `symlink_swap_blocks_edit_file_when_read_gate_allows` — `#[cfg(unix)]`: a
  same-batch bash call swaps the edit target for a symlink after Phase 1 but
  before Phase 2.5. The file is pre-marked read so the read-before-edit gate
  ALLOWS (the attack's exact precondition). The hoisted walk must deny before
  `run_prepared_call`; the symlink target's content must be untouched.
- `symlink_swap_blocks_read_file_on_swapped_final_component` — `#[cfg(unix)]`:
  the read-side variant. A same-batch bash call swaps the read target's final
  component for a symlink pointing at a secret. The walk must deny; the secret
  must not leak into any `ToolResult`.

### Pre-existing parallel-test race fixed (root cause, not symptom)
`dispatch.rs`'s `temp_root()` helper used a shared dir name
(`kf_wo38_symlink_walk_<pid>`) across all 4 `symlink_swap_denied_*` unit tests.
Under nextest's parallel runner one test's `remove_dir_all` wiped another's
`victim.txt` mid-canonicalize → 3 of 4 tests flaked (confirmed on the base
commit `54822687`, pre-44.28). Fixed with a per-call `AtomicUsize` counter so
each test gets a unique dir. Root-cause fix, not a test weakening. This was
required to make the gate green (AGENTS.md §6); the flake would have blocked
`scripts/test-fast.sh` regardless of this WO's changes.

### Gate (final, on `wo/wo44.28` HEAD `eb728af1`)
- `scripts/test-fast.sh` — **4661 tests run: 4661 passed, 15 skipped** (clean
  run; one intermediate run had a pre-existing timing flake in
  `attached_cancel_token_kills_inflight_bash_promptly` under parallel
  compile+test load — passes in isolation and on the rerun; unrelated to
  this WO, not in the touched files).
- `cargo clippy --all-targets -- -D warnings` — clean, `Finished` (no warnings).
- `cargo fmt --check` — clean (exit 0).
- `cargo check --workspace --all-targets` — clean, `Finished`.

### Commits (on `wo/wo44.28`, not merged)
- `f93deb95` — fix(wo44.28): hoist symlink-swap walk to run on every deferred file call
- `666819f9` — test(wo44.28): regression tests for symlink-swap on allowed/read paths
- `eb728af1` — style(wo44.28): cargo fmt

### Impact analysis
- `symlink_swap_denied`: private fn, single call site (dispatch.rs:453),
  tests in same file. Not in the gitnexus index (stale, pre-WO 38.1).
  Verified by grep — no cross-file refs. Blast radius contained to
  `spawn_batch`'s Phase 2.5 loop. Risk: LOW.
- `check_edit`: not modified. Only the placement of the `symlink_swap_denied`
  call moves (out of `check_edit`'s `Denied` arm, before `run_prepared_call`).
