# WO 45.17 — Split `crates/kf-workflow/src/lib.rs` (2707 lines / 105477 bytes)

## Status: Done

## Problem

`crates/kf-workflow/src/lib.rs` is **2707 lines / 105477 bytes** (verified via
`wc -l` / `wc -c`). It is a single-file crate that bundles schema types,
reference resolution, the workflow executor, and file discovery:

1. **Schema types** — `StepKind`, `Step`, `Budget`, `Workflow` (+ impl),
   `StepOutput`, `StepRequest`, `BatchErrors` (lines 41–456, ~420 lines).
2. **Reference + condition evaluation** — `resolve_step_refs`, `extract_field`,
   `evaluate_condition` + `CONDITION_TIMEOUT_SECS` (lines 465–605, ~140 lines).
3. **`WorkflowExecutor`** — struct + impl, the largest impl block
   (lines 607–1272, **~665 lines**). This is the core execution loop with
   dependency resolution, batch dispatch, fan-out/fan-in, on-error routing,
   budget-exceeded handling.
4. **`WorkflowSummary` + discovery** — summary struct + `find_workflow_file`
   + `user_workflow_dir` (lines 1274–1323, ~50 lines).
5. **Inline tests** (lines 1326–2707, ~1380 lines).

No submodules exist — the crate is one file.

## Root cause

The workflow engine grew from a simple agent-step DAG (WO-2 era) to support
bash/tool steps, fan-out/fan-in, conditions, on-error routing, budget limits,
and critique — all added to `WorkflowExecutor::run` and its helpers in one
file. The executor impl alone (665 lines) is larger than many complete
modules.

## Approach

Opportunistic extraction (no behavior change, pure code movement into
submodules within the same crate):

1. **`executor.rs`** (new, ~680 lines) — move `WorkflowExecutor` struct +
   impl. Re-export via `pub use executor::WorkflowExecutor;` in `lib.rs`.
2. **`refs.rs`** (new, ~150 lines) — move `resolve_step_refs`, `extract_field`,
   `evaluate_condition`, `CONDITION_TIMEOUT_SECS`. These are pure functions
   over `HashMap<String, StepOutput>` with no executor state dependency.
3. **`discovery.rs`** (new, ~40 lines) — move `find_workflow_file` and
   `user_workflow_dir`.
4. **`lib.rs`** keeps: crate docs, schema types (`StepKind`, `Step`, `Budget`,
   `Workflow`, `StepOutput`, `StepRequest`, `BatchErrors`, `StepRunner` trait),
   `WorkflowSummary`, module declarations + re-exports, inline tests.

### Split map

| Section | Lines (current) | Destination |
|---|---|---|
| Schema types + `StepRunner` trait | 41–456 | stays in `lib.rs` |
| `resolve_step_refs`, `extract_field`, condition | 465–605 | `refs.rs` |
| `WorkflowExecutor` struct + impl | 607–1272 | `executor.rs` |
| `WorkflowSummary` | 1274–1294 | stays in `lib.rs` |
| `find_workflow_file`, `user_workflow_dir` | 1296–1323 | `discovery.rs` |
| tests | 1326–2707 | stays in `lib.rs` (or split per-fn) |

## Files

- `crates/kf-workflow/src/lib.rs` (edit — add `mod executor; mod refs; mod discovery;` + re-exports)
- `crates/kf-workflow/src/executor.rs` (new)
- `crates/kf-workflow/src/refs.rs` (new)
- `crates/kf-workflow/src/discovery.rs` (new)

## Gate

- `scripts/test-fast.sh`
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --check`
- `cargo check --workspace --all-targets`
- Windows cross-compile: `cargo clippy --target x86_64-pc-windows-gnu`
- No behavior change: pure code movement; existing workflow tests must pass
  unchanged.

## Done

Pure mechanical extraction — no behavior or API change. `lib.rs` went from
2707 lines to 1877 lines; the three sibling modules hold the extracted code:

| File | Lines | Contents |
|---|---|---|
| `crates/kf-workflow/src/lib.rs` | 1877 (was 2707) | crate docs, `mod` decls + `pub use` re-exports, schema types (`StepKind`, `Step`, `Budget`, `Workflow` + impl, `StepOutput`, `StepRequest`, `BatchErrors`, `StepRunner` trait), `WorkflowSummary`, inline `mod tests` (41 tests) |
| `crates/kf-workflow/src/executor.rs` | 675 (new) | `WorkflowExecutor` struct + impl: `run` loop, `check_budget`, `insert_on_exceeded`, `build_agent_prompt`, `run_fan_out`, `run_fan_in`, `handle_batch_error`, `handle_partitioned_batch_error` |
| `crates/kf-workflow/src/refs.rs` | 144 (new) | `resolve_step_refs`, `extract_field` (private), `CONDITION_TIMEOUT_SECS` (cfg-gated), `eval_condition_bounded` |
| `crates/kf-workflow/src/discovery.rs` | 29 (new) | `find_workflow_file`, `user_workflow_dir` |

Public API preserved verbatim — `lib.rs` re-exports `WorkflowExecutor`,
`eval_condition_bounded`, `resolve_step_refs`, `find_workflow_file`,
`user_workflow_dir` so all external call sites (`src/tools/workflow.rs`,
`src/tui/commands/workflow.rs`, `src/jobs/runner.rs`,
`src/main/line_mode.rs`) compile unchanged. All `ponytail:` / `ceiling:`
annotations preserved in their original functions.

Import cleanup: `anyhow` (macro) and `std::sync::Arc` moved from `lib.rs`
top-level into the `mod tests` block (test-only usage after the executor
moved out). `PathBuf` dropped from `lib.rs` (only used by `discovery.rs`).

Commit: `0d4c0ee0` on branch `wo/wo45.17`.

### Gate output (all four green)

```
$ cargo test -p kf-workflow
test result: ok. 41 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.05s
     Doc-tests kf_workflow
test result: ok. 0 passed; 0 failed; 0 ignored; 0 filtered; 0 measured; 0 finished in 0.01s

$ scripts/test-fast.sh   # cargo nextest run --profile ci-fast --workspace --lib --bins --locked
     Summary [ 439.089s] 4707 tests run: 4707 passed, 15 skipped

$ cargo clippy --all-targets -- -D warnings
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 9m 02s

$ cargo fmt --check
EXIT: 0
```

### Deferrals

None. The WO's optional "split tests per-fn" was not taken — tests stay in
`lib.rs::tests` because they cross module boundaries (they construct
`Workflow`/`Step` literals from `lib.rs` and call `WorkflowExecutor` from
`executor.rs` via the re-export; splitting would require duplicating the
`make_runner` helper and the `SleepingBatchRunner`/`ConcurrencyTrackingRunner`
fixtures across files). Keeping them in one `mod tests` with `use super::*`
is the smallest diff and preserves test cohesion.
