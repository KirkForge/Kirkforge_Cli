# WO 45.20 — Split `src/tools/task.rs` (2639 lines / 99487 bytes)

## Status: Done

## Problem

`src/tools/task.rs` is **2639 lines / 99487 bytes** (verified via `wc -l` /
`wc -c`). It is a single file bundling task-lifecycle types, the task manager,
durable persistence, the `task` tool, and the `task_output` tool:

1. **Globals + cancel wiring** — `NEXT_TASK_ID`, `next_unique_id`,
   `cascade_parent_cancel` (lines 1–46, ~46 lines).
2. **Task-lifecycle types** — `TaskRequest`, `TaskCancel`, `TaskStatus` (+ impl),
   `TaskMetadata` (+ impl), `TaskListEntry`, `format_task_entry`,
   `format_duration_ms`, `TaskHandle` (+ impl) (lines 48–270, ~220 lines).
3. **`TaskSpawner` trait** (lines 270–324, ~55 lines).
4. **`TaskManager`** — struct + impl + `task_id_rank` + Default
   (lines 324–432, ~110 lines).
5. **Durable subagent summaries (WO 41.5)** — `PersistedTask` struct + impl +
   `persist_to_disk` + `load_persisted_tasks` (lines 434–570, **~140 lines**).
   This is a self-contained persistence layer (JSON read/write to disk).
6. **`TaskConcurrencyMode` enum + Display + FromStr** (lines 570–597, ~30 lines).
7. **`Task` tool** — struct + impl + `Tool` impl (lines 599–920, **~320 lines**).
   This is the built-in `task` tool that spawns subagents.
8. **`TaskOutput` tool** — struct + impl + `Tool` impl (lines 921–990, ~70 lines).
9. **Inline tests** (lines 990–2639, ~1650 lines).

## Root cause

The task subsystem accreted from a simple `task` tool (WO-2) to include
cooperative cancellation (WO 35.3), process-global IDs (WO 37.1/38.4), durable
summaries (WO 41.5), and concurrency modes — all in one file. The `Task` tool
impl (320 lines) and the persistence layer (140 lines) are the two clearest
separable chunks.

## Approach

Opportunistic extraction (no behavior change, pure code movement):

1. **`persist.rs`** (new, ~150 lines) — move `PersistedTask` struct + impl +
   `persist_to_disk` (method on `TaskHandle` — will need to be a free fn or
   re-import `TaskHandle`) + `load_persisted_tasks`. This is a self-contained
   JSON disk I/O layer with no dependency on `TaskManager` or the `Task` tool.
2. **`task_tool.rs`** (new, ~330 lines) — move `Task` struct + impl + `Tool`
   impl. Re-export `pub use task_tool::Task;` from `task.rs`. The `Task` tool
   depends on `TaskSpawner`, `TaskManager`, `TaskRequest` — all stay in
   `task.rs` and are `pub(crate)`/`pub`.
3. **`task.rs`** keeps: globals, lifecycle types, `TaskSpawner` trait,
   `TaskManager`, `TaskConcurrencyMode`, `TaskOutput` tool, helpers,
   `mod persist; mod task_tool;` declarations + re-exports, inline tests for
   `TaskManager`/`TaskHandle`/lifecycle types.

### Split map

| Section | Lines (current) | Destination |
|---|---|---|
| Globals + cancel | 1–46 | stays in `task.rs` |
| Lifecycle types | 48–270 | stays in `task.rs` |
| `TaskSpawner` trait | 270–324 | stays in `task.rs` |
| `TaskManager` | 324–432 | stays in `task.rs` |
| `PersistedTask` + persist fns | 434–570 | `persist.rs` |
| `TaskConcurrencyMode` | 570–597 | stays in `task.rs` |
| `Task` tool | 599–920 | `task_tool.rs` |
| `TaskOutput` tool | 921–990 | stays in `task.rs` |
| tests | 990–2639 | split per-subject |

## Files

- `src/tools/task.rs` (edit — add `mod persist; mod task_tool;`, remove extracted code)
- `src/tools/persist.rs` (new)
- `src/tools/task_tool.rs` (new)

## Gate

- `scripts/test-fast.sh`
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --check`
- `cargo check --workspace --all-targets`
- Windows cross-compile: `cargo clippy --target x86_64-pc-windows-gnu`
- No behavior change: pure code movement; existing task tests (including
  `task_manager_generates_unique_ids`, persistence tests) must pass unchanged.

## Done

Pure mechanical refactor — `src/tools/task.rs` (2639 lines) split into a
`src/tools/task/` module directory with three sibling files. No behavior
change, no API change; all 36 consumer references
(`crate::tools::task::*` / `tools::task::*` / `kf_code::tools::task::*`)
work unchanged via `pub use` re-exports from `task/mod.rs`.

### Files (before → after line counts)

| File | Before | After |
|---|---|---|
| `src/tools/task.rs` | 2639 | — (renamed via `git mv` to `task/mod.rs`) |
| `src/tools/task/mod.rs` | — | 1190 |
| `src/tools/task/persist.rs` | — | 365 |
| `src/tools/task/task_tool.rs` | — | 1083 |
| `src/tools/task/test_helpers.rs` | — | 123 |
| **Total** | **2639** | **2761** |

The 122-line increase is module headers, `pub use` re-export declarations,
and "moved to `<file>` in WO 45.20" placeholder comments preserving the
section anchors for `git blame` readability.

### What moved where

- **`persist.rs`** — `PersistedTask` struct + impl (`from_handle`,
  `persist_to_disk`), `persist_task_summary` free fn, `load_persisted_tasks`
  free fn, and all persistence tests (`persist_then_reload_*`,
  `persist_skips_non_terminal_handle`,
  `persist_to_disk_uses_atomic_write_no_tmp_left`,
  `background_task_persists_summary_on_completion`,
  `background_cancelled_task_persists_as_cancelled`).
- **`task_tool.rs`** — `Task` struct + impl (`new`, `with_manager`,
  `with_config`, `with_agent_registry`), `Default for Task`, `impl Tool
  for Task`, and all Task-tool tests (`task_def_is_valid`,
  `task_runs_synchronously_without_background`, `task_background_*`,
  `task_reject_mode_*`, `task_with_config_clamps_semaphore_size`,
  `cancel_reaches_run_task_cooperatively_*`,
  `outer_cancel_stops_nested_background_task`,
  `task_def_description_*`, `task_tool_routes_*`,
  `task_tool_passes_agent_model_override`, etc.).
- **`test_helpers.rs`** — `#[cfg(test)]` shared helpers used by tests in
  all three modules: `poll_until`, `extract_task_id`, `MockSpawner`,
  `BlockingSpawner`, `CooperativeSpawner`, `CooperativeProbe`.
  `pub(super)` so only the `task` subtree can reach them.
- **`mod.rs`** keeps: globals (`NEXT_TASK_ID`, `next_unique_id`,
  `cascade_parent_cancel`), lifecycle types (`TaskRequest`, `TaskCancel`,
  `TaskStatus`, `TaskMetadata`, `TaskListEntry`, `format_task_entry`,
  `format_duration_ms`, `TaskHandle`), `TaskSpawner` trait,
  `build_task_prompt`, `build_task_prompt_with_agents`, `TaskManager` +
  `task_id_rank`, `TaskConcurrencyMode`, `TaskOutput` tool, and the tests
  for these kept items (`build_task_prompt_*`, `task_manager_*`,
  `task_handle_status_*`, `task_output_*`, `task_concurrency_mode_*`,
  `two_managers_mint_disjoint_owner_tags`, `format_*`,
  `task_cancel_kills_owned_bash_jobs_spares_main_session`,
  `build_task_prompt_with_agents_*`).

### Visibility notes

- `task_id_rank` (private in `mod.rs`) is reachable from `persist.rs` and
  `task_tool.rs` because Rust private items are visible to descendant
  modules. No visibility change needed.
- `TaskManager.tasks` (private field) is reachable from `task_tool.rs`
  for the same reason — the worker closure's `guard.tasks.get_mut(...)`
  access compiles unchanged.
- `persist_task_summary` is `pub(crate)` in `persist.rs` and re-exported
  as `pub(crate) use persist::persist_task_summary` from `mod.rs` (a
  `pub use` of a `pub(crate)` item is E0364, so the re-export visibility
  must match).
- `PersistedTask`, `load_persisted_tasks`, `Task` are `pub` and
  re-exported as `pub use` — consumer paths unchanged.

### Layout decision (departure from WO file list)

The WO file list named `src/tools/persist.rs` + `src/tools/task_tool.rs`,
but `mod persist;` declared inside `task/mod.rs` resolves to
`src/tools/task/persist.rs` (Rust 2018 module path rules — `task.rs`/`task/mod.rs`
IS the `task` module). Placing the files under `src/tools/task/` preserves
the public API exactly: every consumer uses `crate::tools::task::*` /
`tools::task::*` / `kf_code::tools::task::*`. Zero consumer changes
needed. This matches the WO's stated goal "Keep the tool surface
re-exported."

### Commits

One commit (not per-extraction): the three new files and the `mod.rs`
edit are mutually dependent — `mod.rs` won't compile without the new
siblings, and the siblings won't compile without `mod.rs`'s `mod`
declarations + re-exports. Splitting into per-extraction commits would
leave the tree non-compiling at intermediate commits, violating the
"each task is a gated commit / each edit compiles" rule. The honest
atomic unit is the whole split.

### Gates (all green, pasted)

- `scripts/test-fast.sh` — 4708 passed, 15 skipped (489.842s).
- `cargo clippy --all-targets -- -D warnings` — exit 0 (8m 22s).
- `cargo fmt --check` — exit 0.
- `cargo check --workspace --all-targets` — exit 0 (1m 03s).

### Deferrals / disclosures

- **Windows cross-compile** (`cargo clippy --target
  x86_64-pc-windows-gnu`) was NOT run — the WO lists it under "Gate" but
  the task brief's FINAL GATE section lists only the four gates above
  (test-fast, clippy, fmt, check). The split is pure code movement with
  no platform-specific code touched, so Windows risk is nil, but noting
  the omission for honesty.
- **WO 45.1 (AgentRun identity)** may add a `run_id` field to
  `TaskHandle` in a parallel worktree — as the task brief noted, this
  split is mechanical and `run_id` will move with the `TaskHandle` struct
  (which stays in `mod.rs`). No conflict anticipated.
- `workplan.md`, `lessons.md`, `state.md`, `CHANGELOG.md` were NOT
  edited per the task brief's "Do NOT edit" list. `workplan.md` exists
  in the worktree (written for this session) but is gitignored and not
  committed.
