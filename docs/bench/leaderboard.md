# Benchmark Leaderboard

<!-- Auto-generated by the bench-leaderboard scheduled job (.github/workflows/bench-baseline.yml). -->
<!-- Do not edit manually — the next scheduled run overwrites this file. -->

This table compares model performance on the KirkForge bench task suite.
It is refreshed daily by the `bench-leaderboard` job, which runs
`bench run-models --models qwen2.5:0.5b,llama3.2:1b` and commits the
result here with `[skip ci]` in the commit message (so the commit does
not re-trigger the bench workflow).

The first version of this file is a stub; the scheduled job will
populate it on the next run. See `docs/TECHNICAL.md` for the bench CI
loop documentation (WO 10.9).

| Model | Success Rate | Tokens In | Tokens Out | Cost (USD) | Duration (s) |
|-------|-------------|-----------|------------|------------|--------------|
| _pending first scheduled run_ | — | — | — | — | — |