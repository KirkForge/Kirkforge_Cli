# Workorder 15.26 — Remaining Bucketlist Items (Series 15 Closeout)

## Status: Planned

## Problem
Waves 1-4 (WO 15.1-15.15) shipped all Tier 1 fixes, all Tier 2
correctness bugs, the test-file split, and all 4 god-tier refactors.
This WO is the **closeout** — the remaining Tier 3 architecture +
Tier 4 polish items that didn't get their own dedicated WO in
Waves 5-7. It consolidates them into a single backlog so Series 15
can be closed with every bucketlist item addressed (fixed, triaged,
or honestly deferred).

## What's already done (Waves 1-4)

| Bucketlist items | WO | How |
|---|---|---|
| 1.1-1.7 (Tier 1) | 15.1-15.4 | CI gate theater, plugin validate, SSRF/DNS/Docker, false deferral |
| 2.1-2.24 (Tier 2) | 15.6-15.11 | Bedrock OOM, Vertex token, cancel leak, double-record, verifier name, TOCTOU, security scanner, computer_use lock, cache cap, Anthropic EOF, PluginTool rlimits |
| 3.2 (test split) | 15.5 | executor/tests/mod.rs → 6 sub-files |
| 3.4 (main/mod.rs) | 15.12 | 2,508 → 33 lines (thin router + 9 sub-files) |
| 3.9 (plugin3-cli) | 15.14 | 7,706 → 468 lines (thin router + 7 sub-files) |
| 3.10 (draw event + state) | 15.13+15.15 | event.rs 8,226 → 2,254 + 11 test files; state.rs 4,863 → 10 files |
| 3.35 (dedup of 2.1) | 15.6 | Bedrock buffer cap |
| 3.38 (dedup of 1.5) | 15.3 | DNS rebinding |
| 5.1 (enabled_plugins) | 15.7 | Runtime gate on Stratum/Budget tool + hook registration |
| 5.2 (TS bridge drift) | 15.6 | 2 failing TS tests fixed → 1006/1006 |

## What remains (this WO)

### Tier 3 — Architecture / Code quality (26 items)

#### 3.1 Config field drift — no compile-time guard
- **Fix:** Add a `#[test]` that uses field-count assertions to verify
  every `Config` field appears in `merge_toml_into_config`,
  `apply_env_overrides`, and `config_diff_summary`. Long-term: derive
  macro. Short-term: the test catches drift.
- **Files:** `src/session/config/mod.rs`, `src/session/config/env_overrides.rs`
- **Gate:** `cargo test -p kirkforge --lib session::config`

#### 3.3 Coverage gate doesn't cover `src/tui` or `src/daemon`
- **Fix:** Either add `src/tui` to the CI coverage threshold gate
  (`.github/workflows/ci.yml:328-358`) at a realistic threshold (e.g.
  30% — it's TUI code, hard to unit-test), or document why it's
  excluded in a comment in `ci.yml`.
- **Files:** `.github/workflows/ci.yml`
- **Gate:** YAML valid + coverage job passes

#### 3.5 Monolithic functions >200 loc
- **Fix:** Decompose `run_turn_inner` (224 loc),
  `dispatch_tool_call_batch` (285 loc — partially done in 15.7),
  `record_tool_result` (280 loc — partially done in 15.9),
  `run_tui` (430 loc), `run_event_loop` (310 loc) into named
  sub-methods. No behaviour change. Note: 15.7 and 15.9 already
  touched `dispatch_tool_call_batch` and `record_tool_result`; check
  if they're still >200 loc before re-decomposing.
- **Files:** `src/session/executor/turn.rs`, `src/tui/mod.rs`

#### 3.6 `AppState` is a 44-field God object
- **Fix:** Group fields into sub-structs (e.g. `TuiState`,
  `SessionState`, `CompletionState`, `ConnectionState`). The `new()`
  constructor shrinks. No behaviour change.
- **Files:** `src/tui/app.rs`

#### 3.7 `find_cargo_root` duplicated in 3 files
- **Fix:** Extract to `src/session/verifier/helpers.rs` (or a new
  `src/session/verifier/cargo_util.rs`). Update `build.rs`, `lint.rs`,
  `test.rs` to import from the shared location.
- **Files:** `src/session/verifier/{build,lint,test,helpers}.rs`

#### 3.8 Hook `run_decision` / `run_decision_with_context` duplication
- **Fix:** Extract the ~50 shared lines (in-process hook eval, built-in
  hook eval, plugin hook eval, decision aggregation) into a private
  helper. Both public functions call it.
- **Files:** `src/session/hooks.rs`

#### 3.11 Two coexisting verifier systems with no end-to-end cross-test
- **Fix:** Add a test that fires a `write_file` event through both the
  event-driven `Verifier` path AND the `BusVerifier` path, asserting
  both produce non-conflicting results.
- **Files:** `src/session/executor/tests/` (the split test dir)

#### 3.12 `event_kinds.rs` is dead code
- **Fix:** Delete the module, or wire `verifier_event_kinds()` into
  `VerifierHandler` (it currently hardcodes its own list). Deletion is
  simpler if the hardcoded list is the contract.
- **Files:** `src/session/verifier/event_kinds.rs`, `src/session/verifier/handler.rs`

#### 3.13 `ConnectionState::Connecting` is dead code
- **Fix:** Remove the variant, or make it reachable (emit it during
  the connection phase).
- **Files:** `src/tui/app.rs`

#### 3.14 `ReadFile::minify_above_bytes` has a stale `#[allow(dead_code)]`
- **Fix:** Remove the `#[allow(dead_code)]` — the field IS used at
  line 114.
- **Files:** `src/tools/read_file.rs`

#### 3.15 `RealChromeTab` and `BrowserSessionOwner` identical `ChromeTab` impls
- **Fix:** Extract a single `ChromeTabImpl` wrapping
  `(_browser, tab)`, or delete `RealChromeTab` if `BrowserSessionOwner`
  is the only consumer.
- **Files:** `src/main/chrome_launcher.rs`

#### 3.16 `ManifestError::UnsupportedApiVersion` variant is never constructed
- **Fix:** Remove it, or construct it in `validate_api_version` for
  unknown versions.
- **Files:** `crates/kirkforge-plugin/src/lib.rs`

#### 3.17 Stderr discarded on successful bash execution
- **Fix:** Capture stderr and include it in the tool result (or a
  separate field), so `cargo build` warnings reach the model.
- **Files:** `src/tools/bash.rs`

#### 3.18 LSP `path_to_uri()` doesn't percent-encode
- **Fix:** Percent-encode the path before building the URI. Paths
  with spaces or non-ASCII produce invalid URIs.
- **Files:** `src/tools/lsp_query.rs`

#### 3.19 `gaps.rs::DEFAULT_THRESHOLDS` hardcodes CI thresholds with no drift test
- **Fix:** Add a test that parses `.github/workflows/ci.yml` and
  asserts the `DEFAULT_THRESHOLDS` match the CI values. Catches drift
  when CI thresholds change.
- **Files:** `crates/kirkforge-testdoctor/src/gaps.rs`

#### 3.20 Model-name prefix hardcoding in the trait layer
- **Fix:** Move the routing table to config (model-name → adapter-kind
  map), or document the hardcoded prefixes as the contract. Config is
  better — a new model shouldn't require a code change.
- **Files:** `src/adapters/mod.rs`

#### 3.21 `OpenAiCompatAdapter::model_info` hardcodes vision/cache by model name
- **Fix:** Make vision/cache support config-driven, not model-name-
  hardcoded. A `gpt-4.5` model gets `supports_images: false` even
  though it almost certainly supports both.
- **Files:** `src/adapters/openai_compat/mod.rs`

#### 3.22 `AnthropicAdapter::model_info` hardcodes `max_context_tokens: 200_000`
- **Fix:** Special-case by model (3.7 = 500k, 4 = 1M), or make it
  config-driven.
- **Files:** `src/adapters/anthropic.rs`

#### 3.23 `build_anthropic_body` hardcodes `max_tokens: 8192`
- **Fix:** Make it configurable via `Config`.
- **Files:** `src/adapters/anthropic.rs`

#### 3.24 `scripts/ci-local.sh` diverges from CI
- **Fix:** Add an optional `ci-local.sh full` mode that runs tarpaulin
  with the same thresholds as `ci.yml:336-364` + explicitly runs
  `cargo test -p plugin3-core --test adr_xref_drift`.
- **Files:** `scripts/ci-local.sh`

#### 3.25 All 6 verifiers run on `ToolError` events then immediately skip
- **Fix:** Filter the subscription list per verifier, or short-circuit
  `ToolError` in the handler (skip the fan-out entirely for non-
  relevant events).
- **Files:** `src/session/verifier/handler.rs`

#### 3.26 `CachingAdapter::stream` forwarder drains after consumer drop
- **Fix:** Add a cancellation token to the stream trait, or abort the
  forwarder on consumer drop. The forwarder continues draining (and
  the network) for up to 30s after the consumer drops.
- **Files:** `src/adapters/caching.rs`

#### 3.27 `atomic_write` does not fsync the parent directory after rename
- **Fix:** `fsync` the parent directory after rename, or document the
  ceiling (power loss after rename can still lose the file).
- **Files:** `src/tools/atomic_write.rs`

#### 3.28 `bedrock_signing::sign_request` silently drops non-ASCII headers
- **Fix:** Return an error on non-ASCII header values instead of
  silently dropping.
- **Files:** `src/adapters/bedrock_signing.rs`

#### 3.29 `CacheKey` uses `DefaultHasher` — not collision-resistant
- **Fix:** Use a keyed hash (`SipHasher::new_with_keys`) or sha256
  (already a dep). Or weaken the "content-addressed" doc claim.
- **Files:** `src/adapters/cache.rs`

#### 3.30 Verifier bus env-var contract differs between the two paths
- **Fix:** Document the divergence (legacy sets `KF_EVENT_JSON`, bus
  sets `KF_CHANGED_FILES`), or unify the env-var contract.
- **Files:** `src/session/verifier/bus.rs`, `src/session/verifier/plugin.rs`

#### 3.31 `apply_command_fix` spawns verifier-supplied command with user env
- **Fix:** Document that plugins are trusted for fix-application
  (consistent with the threat model), or sanitize the command.
- **Files:** `src/session/verifier/correction.rs`

#### 3.32 `bash_jobs::BashJobRegistry` watcher tasks detached
- **Fix:** Add a panic hook or watchdog that flips `Running` → `Failed`
  on watcher death.
- **Files:** `src/session/bash_jobs.rs`

#### 3.33 Only 9 entropy prefixes checked
- **Fix:** Expand `ENTROPY_PREFIXES` to cover `xai-`, `claude-`, `hf_`,
  `key-`, or document the detector as supplementary.
- **Files:** `src/session/verifier/security.rs`

#### 3.34 Naive `split_whitespace()` command parsing
- **Fix:** Use `shlex` or document the ceiling (only formatter commands
  with no args are split).
- **Files:** `src/session/verifier/correction.rs`

#### 3.36 Vertex adapter fetches token per request (no retry on fetch failure)
- **Fix:** Move the token fetch inside `send_with_retry`, or add a
  retry wrapper.
- **Files:** `src/adapters/anthropic_vertex.rs`

#### 3.37 Test verifier runs all crate tests for `main.rs`/`lib.rs` changes
- **Fix:** Default to a targeted test filter; only run the full suite
  if the changed file is `main.rs`/`lib.rs` at the crate root.
- **Files:** `src/session/verifier/test.rs`

#### 3.39 `PluginToolWrapper.run` `Cancelled` path untested
- **Fix:** Add a test for the `Cancelled` path.
- **Files:** `src/session/plugin_tools/wrapper.rs`

#### 3.40 No test for `CorrectionLoop` reaching max iterations
- **Fix:** Add a test that drives the loop to 3 iterations and asserts
  it stops.
- **Files:** `src/session/verifier/correction.rs`

#### 3.41 No test for duplicate verifier registration on `VerifierBus`
- **Fix:** Either reject duplicates + test, or document that
  duplicates are allowed (last-wins).
- **Files:** `src/session/verifier/bus.rs`

#### 3.42 No test for `ToolError` event through `VerifierHandler`
- **Fix:** Add a test that fires a `ToolError` event and asserts the
  handler's behavior.
- **Files:** `src/session/verifier/handler.rs`

#### 3.43 Smoke tests cover only 2 CLI subcommands
- **Fix:** Add `kirkforge <subcommand> --help` smoke tests for the
  remaining 8+ subcommands.
- **Files:** `tests/` (integration test dir)

#### 3.44 All integration tests are `#[ignore]`
- **Fix:** Document the tradeoff (integration tests need live Ollama),
  or add a CI job that runs the ignored tests on a schedule.
- **Files:** `.github/workflows/ci.yml` (doc or new scheduled job)

#### 3.45 `.worktrees/locks` directory is stale
- **Fix:** Either gitignore `.worktrees/locks/`, or remove
  `.worktrees/` from `.gitignore` if the worktree mechanism is no
  longer used.
- **Files:** `.gitignore`

#### 3.46 `synth_status_killed` partial stdout loss on timeout
- **Fix:** Flush the drain buffer before the kill marker, or document
  the ceiling.
- **Files:** `src/session/bash_runner/mod.rs`

#### 3.47 `bash_jobs` watcher race
- **Fix:** Register the watcher before spawning, or handle the race
  (child handle removed by `cancel()` between spawn and watcher
  registration → empty `stdout`).
- **Files:** `src/session/bash_jobs.rs`

#### 3.48 `vertex_auth::key_file_looks_valid` accepts any valid JSON
- **Fix:** Check `"type": "service_account"` in the JSON.
- **Files:** `src/adapters/vertex_auth.rs`

#### 3.49 `WorkflowExecutor::run` marks all `batch_outputs` as completed on partial results
- **Fix:** Check for partial results and mark accordingly.
- **Files:** `crates/kirkforge-workflow/src/lib.rs`

#### 3.50 `compare_reports` defaults `difficulty` to `Easy` for missing tasks
- **Fix:** Return an error if a task is missing from both sides, or
  document the fallback.
- **Files:** `crates/kirkforge-bench/src/lib.rs`

#### 3.51 `verify_task` does not inherit the curated env
- **Fix:** Pass the curated env to the verify command.
- **Files:** `crates/kirkforge-bench/src/lib.rs`

#### 3.52 ADR index README has a duplicate/stale prose list
- **Fix:** Delete the prose list (lines 111-149), or regenerate it
  from the Index table.
- **Files:** `docs/adr/README.md`

#### 3.53 `ci.yml:334` `--skip` name does not match the fixed flake
- **Fix:** Reconcile the skip name with the actual flake, or remove
  the skip now that the root cause (`save()` re-create) is fixed.
- **Files:** `.github/workflows/ci.yml`

#### 3.54 ADR-028 status transition undocumented in the ADR body
- **Fix:** Add an amendment note to the ADR body explaining when it
  was promoted from partial to full Accepted.
- **Files:** `docs/adr/0028-verifier-bus-unification.md`

### Tier 4 — Doc drift / Polish (24 items)

#### 4.1 ADR-066 says "30 tasks" in four places
- **Fix:** Update 30 → 31 in `docs/adr/066-kirk-bench-spec.md`.

#### 4.2 KIRK-BENCH "40 tasks" arithmetic doesn't add up
- **Fix:** Reconcile: 31 implemented + 19 planned = 50, not 40. Either
  fix the headline to 50, or fix the planned count to 9, or document
  the mismatch honestly.

#### 4.3 `plugin3-core/README.md` test count stale (1649 vs 1670)
- **Fix:** Update 1649 → 1670 (or the current count — re-count after
  all Series 15 work lands).

#### 4.4 state.md/CHANGELOG "testdoctor total 88" stale (actual 96)
- **Fix:** Update 88 → 96 (or the current count).

#### 4.5 `src/` test count not documented
- **Fix:** Add a sentence to `docs/TECHNICAL.md` (~L65): "The workspace
  has ~4,600 tests (2,900 in `src/`, 1,700 in `crates/`)."

#### 4.6 `ci.yml:334` `--skip` name mismatch
- **Fix:** Same as 3.53. Deduped.

#### 4.7 No architecture diagram
- **Fix:** Add a mermaid or ASCII diagram to `docs/TECHNICAL.md`. Low
  priority; the ASCII tables are functional.

#### 4.8 Coverage-per-file is the right metric
- **Fix:** Either bump thresholds in a commit that closes real
  coverage, or add soft-warning telemetry for files <50%.

#### 4.9 `plugin3-hosts` stub modules
- **Fix:** Document as intentional stubs (`ponytail: stub-only`), or
  add behavior.

#### 4.10 `m5_tests.rs` lives in `src/adapters/` as a sibling module
- **Fix:** Fold into `mod.rs`'s own `#[cfg(test)] mod tests`, or leave
  (cosmetic).

#### 4.11 `collect_carryover` substring-matches `cmd.contains("cargo test")`
- **Fix:** Document as heuristic, or tighten the match.

#### 4.12 `worktree.rs::WorktreeSession::create` interpolates `session_id` with no validation
- **Fix:** Validate `session_id` (no `..`, no `/`), or tighten the
  signature.

#### 4.13 `PostTurnHookGuard::drop` runs the hook synchronously in `Drop`
- **Fix:** Document the ceiling, or move the hook off the Drop path.

#### 4.14 `executor/loop_.rs:168` cancel-watcher task is detached
- **Fix:** Document as cosmetic, or await the handle.

#### 4.15 `executor/scout.rs::StubTool::run` uses `unimplemented!`
- **Fix:** Return `ToolOutcome::Error("stub")` instead of
  `unimplemented!`.

#### 4.16 `bedrock_signing` `extract_payload` `{"type"` literal-match is fragile
- **Fix:** Use a JSON parser, or document the fragility.

#### 4.17 Crate explosion — audit crate boundaries
- **Fix:** Periodically ask "Would this still deserve its own crate?"
  Document the audit decision per crate.

#### 4.18 kirkstratum-*/plugin3-* legacy weight (~30k lines)
- **Fix:** Document that both versions exist by design (ADR-050 two-
  path dispatch: compiled-in when feature on, shell fallback when off).
  The user confirmed awareness.

#### 4.19 More execution metrics per benchmark
- **Fix:** Extend bench report to record: tokens → cache hits →
  retrieval count → compression ratio → verification retries → cost →
  latency → success. The `BudgetChallengeReport` already records 6;
  add retrieval count + verification retries.

#### 4.20 More semantic benchmarks (context-index validation)
- **Fix:** Add bench tasks: Find callers, Rename symbol, Find dead
  code, Explain module, Dependency graph accuracy. Some map to
  existing KIRK-BENCH spec tasks.

#### 4.21 Provider abstraction — capability detection
- **Fix:** Document the long-term plan: Core abstraction → Capability
  detection → Provider-specific enhancements. Short-term fix is 3.20
  (move routing to config).

#### 4.22 Publish benchmark dashboards
- **Fix:** Verify the `bench-leaderboard` scheduled job is green and
  populating `docs/bench/leaderboard.md`. If the job is failing,
  fix it (WO 14.0 added the Ollama-pull retry; verify it works).

#### 4.23 `--harden` rlimit test is `#[ignore]`
- **Fix:** Find a way to run the `--harden` test in CI (dedicated job
  that runs `#[ignore]` security tests on Linux), or document why it
  can't be CI'd.

#### 4.24 Windows is the least battle-tested platform
- **Fix:** Add a Windows-specific CI job that runs the previously-flaky
  tests 3× to catch residual timing races.

## Execution strategy for 15.26

This is a LARGE WO (50 items). It's the closeout — not meant to be
done in one session. The items are grouped by subsystem so future
subagent waves can tackle them in parallel:

- **Batch A (config + adapters):** 3.1, 3.20-3.23, 3.28, 3.29, 3.36,
  3.48
- **Batch B (verifier + security):** 3.11, 3.12, 3.25, 3.30-3.34,
  3.37, 3.39-3.42, 3.52, 3.54
- **Batch C (tools + executor):** 3.5, 3.6, 3.7, 3.8, 3.14-3.18,
  3.26, 3.27, 3.43, 3.45-3.47, 3.49-3.51
- **Batch D (docs + polish):** 3.3, 3.13, 3.19, 3.24, all 4.1-4.24

Each batch can be a future subagent wave off the `wo15` branch.

## Gate
Each item has its own gate (listed above). The series gate is:
```
cargo fmt --check && cargo clippy --all-targets -- -D warnings &&
cargo test --locked --workspace --no-fail-fast &&
cargo test -p plugin3-core --test adr_xref_drift
```

## Done condition
- Every Tier 3 item (3.1-3.54, minus dedups) is fixed or honestly
  deferred with a note in `state.md`.
- Every Tier 4 item (4.1-4.24) is fixed or honestly deferred.
- The `adr_xref_drift` and `readme_drift` tests pass.
- `state.md` reflects the final state of Series 15.

## Notes
- This WO is the **Series 15 closeout**. It consolidates all remaining
  bucketlist items into one backlog. Future sessions can dispatch
  subagent waves per batch (A/B/C/D) off the `wo15` branch.
- Items 3.35, 3.38, 4.6 are dedups of already-fixed items (2.1, 1.5,
  3.53). Not re-listed in the batches.
- The user is switching to a z.ai coding plan (Ollama budget exhausted
  — 9/10 billion tokens used). This WO is the save point for the
  remaining work.
- The `wo15` branch is pushed to origin and contains all Wave 1-4
  merges (15.1-15.15). It's the base for future batches.