# Workorder 21.0.14 — Deferred Item Tracker (WO 21 Series)

## Status: In Progress

## Problem

The WO 21 series deferred several items across sub-workorders. Per AGENTS.md
rule #11 (defer-disclosure), every deferral must state what was deferred, why,
exact remaining work, and where it's tracked. This workorder is the single
source of truth for all deferred/not-done items across the WO 21 series.

## Deferred items

| ID | From | What | Why | Remaining work | Where tracked | Priority |
|----|------|------|-----|----------------|---------------|----------|
| 21.5-R2 | WO 21.5 | Bash PTY/streaming output | Ponytail deferral: adds ~2MB to binary via `portable-pty` dependency | Evaluate `portable-pty` or accept the size cost; wire PTY into `bash` tool for interactive/streaming output | This file | P2 |
| 21.5-R4 | WO 21.5 | MCP sampling/createMessage + roots/list | MCP spec capability not implemented; `UNSUPPORTED_CAPABILITIES` still lists `&["sampling", "roots"]` | Implement MCP spec sampling (`sampling/createMessage`) and roots (`roots/list`), or document why deferred with ADR | This file | P1 |
| 21.5-R7 | WO 21.5 | HTML-to-markdown conversion | `web_fetch.rs` uses regex tag-stripping (ponytail ceiling, no formal defer clause) | Either integrate a real HTML-to-md library (e.g. `html2md`) or accept the ponytail ceiling and add a `ponytail:` comment with upgrade path | This file | P2 |
| 21.5-R9 | WO 21.5 | Anthropic computer-use beta tool | Not implemented; lowest priority per WO 21.5 | Implement Anthropic-specific `computer_use_2024-11-11` beta tool format; coordinate with existing `computer_use` dedup logic | This file | P2 |
| 21.6-R1 | WO 21.6 | LSP federation with context index | Call-graph resolution is name-based; never queries rust-analyzer for definition/resolve | Integrate LSP pool with `ContextIndex` for definition/resolve queries; large item touching LSP client, index, and retrieval | This file | P1 |
| 21.6-R2 | WO 21.6 | Memory auto-populate | `MemoryStore` stays empty unless hand-edited; store and read path exist but write path from agent turns doesn't | Add a `remember` tool or post-turn hook that auto-writes facts to `MemoryStore` | This file | P2 |
| 21.7-R1 | WO 21.7 | Default OS sandbox (landlock/seatbelt) | Still rlimits-only; real sandbox requires syscall-level enforcement | Implement landlock (Linux) and seatbelt (macOS) integration; large item with real syscall work, platform-specific | This file | P1 |
| 21.7-R2 | WO 21.7 | seccomp syscall filter | ADR-054 still deferred; implement libseccomp or close formally | Either implement libseccomp filter (breaks static binary story per ADR-054) or formally close ADR-054 with updated evidence | This file | P1 |
| 21.7-R3 | WO 21.7 | Diff-review-before-apply gate | TUI has approval component but it's not wired as a mandatory gate on every file-modifying tool | Wire the approval component as a mandatory diff-review gate before every file write/edit tool applies changes | This file | P1 |
| 21.9-R5 | WO 21.9 | Fuzzing expansion | No new fuzz targets added | Add fuzz targets for parser and code paths using `cargo-fuzz`; at minimum one target per parser module | This file | P2 |
| 21.11-R9 | WO 21.11 | ADR for redundant CLI crate removal | `kf-budget-cli` and `kf-compress-cli` were removed from workspace but no ADR records the decision | Write ADR documenting the fold+yeet decision for these crates | This file | P2 |

## Priority definitions

- **P0**: Blocks release — none currently.
- **P1**: Should do before next release — sandbox hardening, MCP spec compliance, diff-review gate.
- **P2**: Nice to have — PTY streaming, HTML-to-md, computer-use beta, memory auto-populate, fuzzing, ADR for crate removal.

## Gate

No code gate — this is a tracking document. Verify accuracy by checking each
referenced source file for the described state.

## Done condition

Every item in the table is either completed (with commit SHA) or formally
re-assigned to a future series workorder with a bidirectional link.
