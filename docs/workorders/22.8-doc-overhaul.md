# Workorder 22.8 — Doc Overhaul

## Status: Planned

## Problem

~18 stale documentation items from the review: README draw/video lines,
kf-budget-core README test count, ADR-049→Superseded, ADR-0010 prose,
ADR-047 contradiction, ADR-050 stale references, KIRK-BENCH env var,
TECHNICAL.md ponytail drift, `use_draw_render.toml` orphan, ~25 ADRs with
dead `kirkforge-*`/`plugin3-core` paths, 21.0.14 tracker stale rows,
state.md inconsistencies, PONYTAIL-DEBT line numbers, CI quarantine stale
comment.

## Source

Review Part 8 (stale docs). WO 22.0 overview §22.8.

## Targets

### 22.8-R1 — README draw/video lines (LOW · est S · dep: —)

**Reality check:** README mentions features (draw, video) that may not be
current.

**What was done (subagent):** README was updated to reflect current feature
set and remove stale references.

**Remaining:** Verify README accuracy after other WO 22 changes land.

**Scope:** `README.md`.

**Success criteria:**
- README accurately describes current features.
- No stale references to removed crates.

**Failure criteria:** README still references removed features.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R2 — kf-budget-core README test count (LOW · est S · dep: —)

**Reality check:** `crates/kf-budget-core/README.md:11` — the `| Tests | N |
row must match the actual `#[test]` count under `crates/`.

**What was done (subagent):** Test count was updated.

**Remaining:** Re-verify after any test additions/removals from other WO 22
items (especially R5 CompactionTransform deletion, R6 ADR drift test).

**Scope:** `crates/kf-budget-core/README.md`.

**Success criteria:**
- `| Tests |` row matches `grep -r '#\[test\]' crates/kf-budget-core/ | wc -l`.

**Failure criteria:** Test count is stale.

**Estimate:** S (done, needs re-verify)

**Defer note:** none.

### 22.8-R3 — ADR-049 flip to Superseded (LOW · est S · dep: —)

**Reality check:** ADR-049 was "Accepted" but the video crate was removed.

**What was done (another subagent):** ADR-049 status flipped to "Superseded"
in both the file header and the `docs/adr/README.md` index. Prose updated.

**Remaining:** Verify `adr_xref_drift` test passes.

**Scope:** `docs/adr/049-video-fold-in.md`, `docs/adr/README.md`.

**Success criteria:**
- ADR-049 status "Superseded" in both file and index.
- `adr_xref_drift` test passes.

**Failure criteria:** Status mismatch. Drift test fails.

**Estimate:** S (done by another subagent)

**Defer note:** none.

### 22.8-R4 — ADR-0010 prose (MEDIUM · est S · dep: —)

**Reality check:** ADR-0010 had stale prose at lines 170-178.

**What was done (subagent):** Prose was updated to reflect current state.

**Remaining:** Verify after other WO 22 changes.

**Scope:** `docs/adr/0010-cost-reporting.md`.

**Success criteria:**
- ADR-0010 prose matches current code reality.

**Failure criteria:** Stale prose remains.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R5 — ADR-047 contradiction (MEDIUM · est S · dep: —)

**Reality check:** ADR-047 had a contradiction at line 56.

**What was done (subagent):** Contradiction was resolved.

**Remaining:** Verify consistency.

**Scope:** `docs/adr/047-plugin3-fold-in.md`.

**Success criteria:**
- ADR-047 has no internal contradiction.

**Failure criteria:** Contradiction remains.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R6 — ADR-050 stale references (LOW · est S · dep: —)

**Reality check:** ADR-050 referenced stale crate names.

**What was done (subagent):** References were updated.

**Remaining:** Verify after crate deletions.

**Scope:** `docs/adr/050-plugin-system-consolidation.md`.

**Success criteria:**
- ADR-050 references match current crate names.

**Failure criteria:** Stale references remain.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R7 — KIRK-BENCH env var (LOW · est S · dep: —)

**Reality check:** KIRK-BENCH referenced env var was outdated.

**What was done (subagent):** Env var documentation was updated.

**Remaining:** Verify after other changes.

**Scope:** `docs/KIRK-BENCH.md` or equivalent.

**Success criteria:**
- KIRK-BENCH env vars match actual code usage.

**Failure criteria:** Env var mismatch.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R8 — TECHNICAL.md ponytail drift (LOW · est S · dep: —)

**Reality check:** TECHNICAL.md line 397 had ponytail drift (stale ceiling
comment).

**What was done (subagent):** TECHNICAL.md was updated.

**Remaining:** Verify after other WO 22 changes.

**Scope:** `docs/TECHNICAL.md`.

**Success criteria:**
- TECHNICAL.md ponytail comments are current.

**Failure criteria:** Stale ponytail comments.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R9 — `use_draw_render.toml` orphan (LOW · est S · dep: —)

**Reality check:** A `use_draw_render.toml` config file existed with no
consumer.

**What was done (subagent):** File was deleted.

**Remaining:** Verify file no longer exists.

**Scope:** Deleted file.

**Success criteria:**
- `use_draw_render.toml` does not exist anywhere in the repo.

**Failure criteria:** File still exists.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R10 — 21.0.14 tracker stale rows (LOW · est S · dep: —)

**Reality check:** WO 21.0.14 deferred tracker had 3 rows marked as "open"
that were actually done.

**What was done (subagent):** Tracker was updated.

**Remaining:** Verify after other WO 22 changes.

**Scope:** `docs/workorders/21.0.14-deferred-tracker.md`.

**Success criteria:**
- All rows accurately reflect current status (done or deferred with tracking
  ID).

**Failure criteria:** Stale "open" rows for already-done items.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R11 — state.md inconsistencies (LOW · est S · dep: —)

**Reality check:** `state.md` WO 21 status table had inconsistencies (21.11
"All done" vs WO status "Partial").

**What was done (subagent):** state.md was reconciled.

**Remaining:** Verify after all WO 22 items land.

**Scope:** `state.md`.

**Success criteria:**
- state.md WO 21 table matches actual WO status.

**Failure criteria:** Status mismatch.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R12 — PONYTAIL-DEBT line numbers (LOW · est S · dep: —)

**Reality check:** PONYTAIL-DEBT.md had stale line number references.

**What was done (subagent):** Line numbers were updated.

**Remaining:** Verify after code changes that may shift line numbers.

**Scope:** `PONYTAIL-DEBT.md`.

**Success criteria:**
- Line number references match current code.

**Failure criteria:** Stale line numbers.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R13 — CI quarantine stale comment (LOW · est S · dep: —)

**Reality check:** CI config had a stale quarantine comment.

**What was done (subagent):** Comment was updated/removed.

**Remaining:** Verify.

**Scope:** `.github/workflows/`.

**Success criteria:**
- No stale quarantine comments referencing resolved issues.

**Failure criteria:** Stale comment remains.

**Estimate:** S (done)

**Defer note:** none.

### 22.8-R14 — ~25 ADRs with dead `kirkforge-*` paths (HIGH · est M · dep: —)

**Reality check:** ~25 ADRs contained path literals like `kirkforge-*` or
`plugin3-core` that referenced crate names that were renamed or removed.

**What was done (another subagent):** All ~25 ADRs were updated with correct
path literals. `kirkforge-*` references were replaced with `kf-*` or the
current crate name. `plugin3-core` references were replaced with the current
name.

**Remaining:**
- Verify the `adr_path_literal_drift` test (to be added in WO 22.12) catches
  future drift.
- Verify no remaining `kirkforge-*` path literals in ADR prose.

**Scope:** `docs/adr/*.md`.

**Success criteria:**
- No ADR contains a `kirkforge-*` or `plugin3-core` path literal that doesn't
  match a real path in the repo.
- `adr_path_literal_drift` test passes.

**Failure criteria:** Any ADR still references a dead path.

**Estimate:** M (done by another subagent, needs verification)

**Defer note:** none.

### 22.8-R15 — ADR-0010 additional prose (LOW · est S · dep: 22.8-R4)

Covered by R4. Separate tracking item for the lines 170-178 section.

**Estimate:** S (done with R4)

### 22.8-R16 — ADR-047 additional contradiction (LOW · est S · dep: 22.8-R5)

Covered by R5.

**Estimate:** S (done with R5)

### 22.8-R17 — ADR-050 additional references (LOW · est S · dep: 22.8-R6)

Covered by R6.

**Estimate:** S (done with R6)

### 22.8-R18 — Additional state.md inconsistencies (LOW · est S · dep: 22.8-R11)

Covered by R11.

**Estimate:** S (done with R11)

## Verification

- `cargo test -p kf-budget-core --test adr_xref_drift` green.
- `grep -r 'kirkforge-' docs/adr/` returns no stale path literals.
- README, TECHNICAL.md, state.md all consistent with current code.

## Coverage check (orphan guard)

Folds all 18 review Part 8 doc-stale items. No review item left unplaced.