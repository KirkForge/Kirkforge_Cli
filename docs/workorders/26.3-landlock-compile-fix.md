# Workorder 26.3 — P0 Landlock Compile Fix (F17)

## Status: Completed

## Priority: P0 — feature flag is broken; cannot compile with `--features landlock`

## Problem

Building with `--features landlock` fails to compile. `src/session/bash_runner/mod.rs:466` references `apply_landlock` which has a compile error under the landlock feature gate. The function is only called in tests, but the compile error blocks anyone who enables the feature (CI, downstream consumers, security hardening).

## Source

- Handoff audit (WO 26 bug-fix series), finding F17.
- `src/session/bash_runner/mod.rs:466`.

## Requirements

### R1 — Fix the compile error under `--features landlock`

- Read `src/session/bash_runner/mod.rs` around line 466 to identify the compile error.
- The fix is likely a missing import, wrong type, or cfg-gate mismatch.
- Verify: `cargo check --features landlock` compiles cleanly.
- Verify: `cargo check` (without the feature) still compiles.
- If `apply_landlock` is dead code outside tests, consider gating it with `#[cfg(test)]` or `#[cfg(feature = "landlock")]` as appropriate.

### R2 — Run existing tests under the feature flag

- `cargo test --features landlock -p kf-code` should pass.
- If landlock tests require kernel support (Linux 5.13+), add a `#[cfg(target_os = "linux")]` gate and a skip-on-non-linux note.

## Success criteria

- `cargo check --features landlock` compiles without errors.
- `cargo check` (no feature) still compiles.
- `cargo test --features landlock -p kf-code` passes (or skips gracefully on unsupported kernels).
- `cargo clippy --all-targets -- -D warnings` clean with and without the feature.

## Failure criteria

- Feature flag still fails to compile.
- Fix breaks the default build (no feature).
- `apply_landlock` removed instead of fixed (it's needed for sandbox hardening).

## Estimate

S (~30 min). Depends on the exact compile error — likely a missing cfg gate or import.

## Defer note

None — this is a compile fix, not a feature implementation.
