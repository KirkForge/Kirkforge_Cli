# Workorder 30.0.0 — Master Index: All Unfinished Work

## Status: Living document (updated each session)

*Consolidated from `state.md`, `lessons.md`, `kf-code-review.md` (7th ed @ `5296f41`), every `docs/workorders/28.*` + `29.*` status line, a full `#[ignore]` audit, and a TODO/FIXME sweep. Each row verified against worktree HEAD `7a0de4d` (branch `sweep30`, 2026-08-12).*

**Legend:** Est = S (<½ day) / M (1-3 days) / L (~1-2 weeks) / XL (multi-week). "OPEN" = real work remains; "PARTIAL" = shipped with a disclosed residual; "CLOSED" = verified shipped, see the table at the bottom.

---

## BLOCKER (pre-existing — flagged, another subagent fixing concurrently)

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **Committed merge-conflict markers in `Cargo.toml:73-78` + `Cargo.lock`** | `cargo test` fails to parse | OPEN | S | Blocks ALL cargo commands from a fresh clone. `<<<<<<< HEAD` at `Cargo.toml:73` over the `kf-orchestrator` workspace dep. Not a deferred feature — a broken merge commit (WO 29.7 window). **Must fix before any CI-green claim.** |
| **`adr_xref_drift::status_counts_match_index_table_summary` RED** | state.md (WO 29.6 note) | OPEN | S | Could not re-verify independently — the workspace won't build because of the conflict markers above. ADR-status-counts vs `docs/adr/README.md` index drift. Another subagent fixing concurrently. |

---

## Architecture + structural debt

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **H7 — `tools ↔ session` cycle residuals (3 port traits)** | review H7; WO 28.1 status | PARTIAL | S-M | WO 28.1 R1 shipped (relocation per ADR-073: `tools→session` imports **26→3**, cycle cut). 3 genuine session-layer residuals remain for full crate-extraction: `bash_runner` shell-I/O, `bash_jobs` registry, `remember::memory`. None is cyclic; each needs its own port trait. Tracked in WO 28.1 disclosure. |
| **H8 — `executor/turn.rs` further split** | review H8; WO 28.3 | PARTIAL | S-M | WO 28.3 shipped: `turn.rs` **2087→1191 LOC** (-43%); `executor/{dispatch,pre_run,stream,scout,sandbox}.rs` extracted. Further phase extraction of `dispatch_tool_call_batch` / `stream_iteration` possible but no longer a god-object. |
| **WO 28.4 — TS plugin-SDK barrel-re-export cycles** | WO 28.4 (Planned); WO 29.0 note | OPEN — **recommend SKIP** | M | 5 cycles in `npm/kf-plugin/packages/`. **NOT moot yet** — the TS tree (`npm/kf-plugin/`, `bridge-emitter.ts`, `security-emitter.ts`, `plugins/kf-plugin/tools/*.sh`) is all still present (verified at HEAD). It becomes moot the moment WO 29.9 deletes the TS tree. Recommendation: skip 28.4, ship WO 29.9 instead — deletes the code rather than fixing cycles in code about to be deleted. |

## Test health

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **WO 28.6 — 7 binary-spawn e2e hang** | review H6/WO 27.2-R1; WO 28.6 (Planned) | OPEN | M | `run_line_mode` startup wedges (daemon/plugin/MCP init). In-process equivalent (`wiremock_integration.rs`) PASSES. 7 scenarios in `tests/e2e/scenarios/*.rs` carry `#[ignore = "WO 27.2: ..."]`. Either debug via checkpoints or convert to in-process (recommended). |
| **WO 28.7 — coverage gate** | review H6; WO 25.16/27.2-R4; WO 28.7 (Planned) | OPEN | M | `scripts/check-cov-regression.sh` doesn't exist; `docs/coverage-baseline.md` is "TBD"; `kf-testdoctor` self-guard is itself `#[ignore]`'d (see below). Unblocks WO 28.9. |
| **WO 28.9 — session coverage >75%** | WO 24.6/25.16; WO 28.9 (Planned) | OPEN | XL | `src/session` ~68.6%; 19 untested executor/budget/compaction/verifier flows. Depends on WO 28.7 (the gate). |
| **`kf-testdoctor/gaps.rs:416` `default_thresholds_match_ci_yml`** | TODO/FIXME sweep; `ponytail:` pin | OPEN (piggybacks 28.7) | XS | `#[ignore]` until coverage-gate `targets` dict is restored in `ci.yml`. Upgrade path: remove `#[ignore]` when WO 28.7 lands. |
| **10 bare `#[ignore]` lacking reason strings (style debt, NOT broken)** | `#[ignore]` audit | OPEN | XS | All are slow/network/timing tests that should carry `= "..."` reasons: `tui/commands/mod.rs:354` (job-cancel race), `web_fetch.rs` ×5 (real DNS NXDOMAIN ~10s each), `executor/tests/approval/timeout.rs:19` (5s timeout wait), `plugin_tools/tests.rs:653` (needs Node SDK — moot after WO 29.9), `bash_jobs.rs:700` (spawns MAX_JOBS). None is "known-broken"; this is annotation hygiene. |

### Full `#[ignore]` audit (36 total at HEAD)

| Class | Count | Status |
|-------|-------|--------|
| Legit env-gate: Ollama | 7 | `tests/integration_test.rs` — keep |
| Legit env-gate: tmux + display | 2 | `tests/e2e/scenarios/tui_*.rs` — keep |
| Legit env-gate: headless Chrome | 2 | `tools/computer_use.rs` — keep |
| Legit env-gate: setrlimit + CPU burn | 2 | `tools/bash.rs`, `plugin_tools/tests.rs` — keep |
| Legit env-gate: Docker | 1 | `tools/bash.rs:827` — keep |
| Legit env-gate: display server | 1 | `tui/clipboard.rs:48` — keep |
| Legit env-gate: timing-sensitive watcher | 1 | `plugin_tools/tests.rs:1330` — keep |
| Legit env-gate: spawns cargo/clippy | 3 | `session/verifier/{build,lint,test}.rs` — keep |
| **Broken: binary-spawn e2e** | **7** | **WO 28.6 — OPEN** |
| **Bare `#[ignore]` (no reason string)** | **10** | **Style debt — see above** |
| **Total** | **36** | |

## CI / ops

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **Version bump 0.3.6 → next** | state.md "0.3.7 NOT done" | OPEN | XS | `Cargo.toml` still `version = "0.3.6"`. The 0.3.7 target in state.md is itself stale (WO 27/28/29 have shipped since) — bump to whatever the next release is. |
| **`main` fast-forward** | state.md; git log | OPEN | XS | `origin/main` at `d848b37` (WO 27.2 CI fix); HEAD at `7a0de4d` (WO 29.7). `main` is 3+ WO merges behind `dev`. Review 7th ed's "main ff'd" referred to `d848b37`, now stale. |
| **WO 28.11 R3 — bench name-set manifest** | WO 28.11 disclosure | OPEN (disclosed) | XS | R1+R2 shipped; R3 (generate name-set from source in CI) deferred as YAGNI — the TECHNICAL.md row check (R1) catches renames in practice. |
| **4 of 9 fuzz targets orphaned** | review MEDIUM | OPEN | S | Not wired as `[[bin]]`. Low urgency. |
| **Local install at `/home/henrik/own-code/kf-code`** | state.md | OPEN | XS | Carryover; not code debt. |

## Security

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **C1 follow-up — landlock allow-list dogfooding** | review 7th ed #1 P0 | OPEN (maturity, not capability) | ongoing | Capability fully shipped (`security.landlock_extra_paths` + `--i-accept-unsandboxed` release escape). Sole remaining cap is real-workload exercise — expect `ENOENT`/`EACCES` + operator reach-for-escape in first weeks. No new code unless a path class is missing. |
| **H3 — bash deny-list bypassable (narrowed)** | review H3; WO 28.17 (Planned) | OPEN | S | `$()`/backticks/`<()` detected (`5296f41`); var indirection + base64/hex + eval still evade. WO 28.17 = document tripwire-vs-boundary posture. Defense-in-depth behind landlock. |
| **WO 28.16 — Anthropic `computer_use` hosted beta** | WO 28.16 (Planned); state.md deferred #3 | OPEN | L | Hosted API path: `computer` tool type + `anthropic-beta` header + coordinate-vision model. Verified at HEAD: adapter has **no `anthropic-beta` header** → still open. (The local CDP `computer_use.rs` is a different capability and does NOT satisfy this.) |
| **H5 background bash FS landlock — R3 test** | WO 28.5 status | PARTIAL | XS | Capability SHIPPED (WO 28.5: `resolve_paths` is `pub(crate)`, wired at `bash_jobs.rs:204`). Only the R3 *test* is deferred (needs landlock-capable kernel). |
| MEDIUM: no per-verifier timeout (`handler.rs:76`) | review MEDIUM | OPEN | S | A wedged `cargo build` hangs the turn. |
| MEDIUM: `computer_use` navigate DNS-rebinding guard | review MEDIUM | OPEN | S | Now behind a real sandbox; lower urgency. |
| MEDIUM: `block_dotfiles: false` default; `~/.aws`/`~/.netrc` not in deny list | review MEDIUM | OPEN | S | landlock now blocks `~/.aws` by exclusion; deny-list item is belt-and-suspenders. |
| MEDIUM: audit-log doesn't record `read_file` | review MEDIUM | OPEN | S | — |
| MEDIUM: MCP stdio subprocesses inherit full parent env incl. API keys | review MEDIUM | OPEN | S | — |

## Capabilities (deferred features)

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **WO 29.9 — delete `npm/kf-plugin/` + TS CI jobs + eslint/tsc deps** | WO 29.9 (Planned) | OPEN — **LAST** | S | The capstone of WO 29. Deletes the entire TS tree → makes WO 28.4 moot, makes `plugin_tools/tests.rs:653` moot. Ships after 29.8. |
| **WO 29.8 — port health-server** | WO 29.8 (Planned) | OPEN | M | HTTP + auth + rate-limit + Prometheus. Depends on WO 29.4 (DONE) + 29.5 (DONE). |
| **WO 29.7 residual — `ModelClient` production impl** | WO 29.7 disclosure | OPEN | M | `kf-orchestrator` trait seam has no production impl; 3 `PanickingClient` test stubs stand in. Remaining: `impl ModelClient for ExecutorAdapter` in `src/session/`; replace the stubs in production paths. |
| **WO 29.7 residual — `ValidatorConfig` execution** | WO 29.7 disclosure | OPEN | M | Shell/structured validator parses but doesn't run; loop falls through to `decide_correction` with `task_pass=None`. Remaining: wire to `tokio::process::Command`; port `runTaskValidator`/`runStructuredTaskValidator`. |
| **WO 29.7 residual — R6 SLO monitor + R7 security-emitter integration** | WO 29.7 disclosure (workorder-level deferral) | OPEN | S | R6 low CLI value (workorder-deferred); R7 tracked in WO 29.9. |
| **WO 29.1 R3 — delete `plugins/kf-plugin/tools/*.sh`** | WO 29.1 disclosure | OPEN | XS | Deferred until Node fallback removed (so `--no-default-features` rebuilds still work). Ships with/after WO 29.9. |
| **WO 28.13 — Bedrock/Vertex test mocks** | WO 28.13 (Planned); state.md #15 | OPEN | M | No CI contract tests for SigV4 + AWS event-stream + GCP token. Persona Bedrock/Vertex plumbing itself DONE (WO 26.6-R2); this is the test-mock gap. |
| **WO 28.15 — memory semantic dedup** | WO 28.15 (Planned); review 3.4 | OPEN | M | Near-duplicate facts accumulate; TF-IDF retrieval untightened. |

## Documentation drift

| Item | Source | Status | Est | Notes |
|------|--------|--------|-----|-------|
| **`state.md` "Current state (2026-08-10)" section is stale** | state.md lines 84-98 | OPEN | S | Claims "main ff NOT done", "WO 27 STARTED", "version 0.3.7 NOT done". Reality: WO 27 shipped, WO 28.x + 29.x merged, version still 0.3.6 (not 0.3.7). Needs a refresh pass to reflect HEAD `7a0de4d`. |
| **`kf-code-review.md` 7th ed open-findings are stale** | review @ `5296f41` | OPEN (point-in-time) | — | Predates WO 28.x + 29.x. Its "still open" list has drifted: H5 background-FS-landlock is now CLOSED (WO 28.5); H8 turn.rs is 1191 LOC not 2087 (WO 28.3); tools↔session imports are 3 not 26 (WO 28.1). Not a regression (it's a dated snapshot) but the "open findings" no longer match HEAD. Refresh at next review edition. |
| **`docs/workorders/30.0.0-wo30-overview.md`** | this file | NEW (living) | — | This index. Update each session; fold closed items into the CLOSED table. |

## TODO / FIXME comments in Rust source

A full `grep -rn "TODO\|FIXME\|HACK\|XXX" src/ crates/` (excluding test files) surfaces **23 textual hits, of which 21 are false positives** (`src/tools/todo.rs` is the TODO-list *tool*, not TODO comments; `microcompaction.rs` references "TODOs" in prompt prose; `jobs/store.rs:328` is test data `job-20260716-XXX`). The real ones:

| Location | Status | Notes |
|----------|--------|-------|
| `src/main/error.rs:49` | OPEN (minor) | `// TODO: as more library calls return typed errors, replace these string probes with further downcast_ref checks.` Real, low-priority — incremental as the error taxonomy grows. |
| `src/session/plugin_ops.rs:427` | NOT debt | "TODO: implement this skill…" is the placeholder string inside the **skill-scaffold template** (`prompt = """…"""`). It is shown to users who scaffold a new skill; not a debt marker. |

---

## CLOSED (verified shipped at HEAD `7a0de4d` — no action needed)

| Item | Closing evidence |
|------|------------------|
| **C1 — landlock never applied in prod** | WO 27.1 all 6 phases: `91a2365` (Phases 1-3: un-gated, default-on Linux, fail-closed, wired into `pre_exec`) + `5296f41` (Phases 4-6: `security.landlock_extra_paths` + `--i-accept-unsandboxed` release escape + ADR-054/TECHNICAL.md sync). |
| **C2 — e2e stream/routing hang** | `e92f8d3` + `260e7d8` (90s `STREAM_IDLE_TIMEOUT` + `[adapter_routing] "e2e-" = "Ollama"`). |
| **H1 — budget mutex-poison cascade** | `6129891` (`.unwrap_or_else(\|e\| e.into_inner())`). |
| **H2 — file-tool panic session loss** | `b5e7190` (`catch_unwind` on `run_prepared_call`). |
| **H4 — web_fetch SSRF-via-redirect** | `b5e7190` (`.redirect(Policy::none())`). |
| **H5 — background bash jobs skip limits (capability)** | WO 28.5: `resolve_paths` re-exported `pub(crate)`; `bash_jobs.rs:204` wires landlock FS confinement + rlimits + `CLONE_NEWNET`. (Only R3 *test* deferred — needs landlock kernel.) **Supersedes the review 7th-ed "bg-FS-landlock deferred" item.** |
| **H8 — anthropic god-object (half)** | WO 27.3-R2 `0e09fcb`: `anthropic.rs` 2317 LOC → `anthropic/{mod,sse,content_blocks,usage}.rs`. |
| **H8 — turn.rs god-object (the split itself)** | WO 28.3 `b588e0b`: 2087→1191 LOC, `executor/{dispatch,pre_run,stream,scout,sandbox,cost_tracking}.rs` extracted. (Further phase extraction optional — see architecture table.) |
| **H9 — workspace plugin trust bypass** | WO 27.4 `2e7c174` + `e4dccbb`: `plugin_trust_workspace` opt-in (default `false`, fail-closed). |
| **21 "known-broken" `#[ignore]` tests + `config_field_count_drift_guard` canary** | WO 27.2-R2 `838e611`/`73c0453`/`336a3db` (root cause: `SandboxConfig::default()` derive zeroed rlimits → SIGXCPU/SIGXFSZ). `config_field_count_drift_guard` is a **live `#[test]`** (NOT ignored); `CONFIG_FIELD_COUNT = 99`. |
| **H7 — tools↔session cycle (the cycle itself)** | WO 28.1 `50fb79b`: relocation per ADR-073, `tools→session` imports **26→3**, cycle cut. (3 non-cyclic port-trait residuals remain — see architecture table.) |
| **CI green (the 5th-ed debt)** | `d848b37`/`ff9cf6e` (`cfg(unix)` gates for Windows). |
| **Plugin3 env backward-compat shim** | WO 28.14: `PLUGIN3_*_DIR` → `KF_BUDGET_*_DIR` one-release shim in `crates/kf-budget-core/src/paths.rs`. |
| **Bash streaming / MCP sampling / TUI memory widget / sessions-list refresh / AppState decomposition / persona Bedrock-Vertex plumbing / testdoctor parallel + caching** | WO 26.6/26.7/26.8/26.9 (all marked DONE in state.md deferred-items ledger). |
| **WO 28.2 — budget↔stratum cycle** | WO 28.2 (R1 shipped; R2 optional, deferred). |
| **WO 28.8 — `daemon/client.rs` tests** | WO 28.8 (R1-R5 + happy path; R6 Windows deferred). **Supersedes review MEDIUM "549 LOC zero tests".** |
| **WO 28.10 / 28.12 — slow-test feature gate / dead-ref CI gate** | `824ce82` merge. |
| **WO 29.1-29.7 ports (the foundation)** | kf-routing (29.3), kf-rbac (29.5), kf-memory-store (29.6), kf-orchestrator (29.7 R1-R5), EventBus+AuditLogger (29.4), Rust security emitter (29.2) — all Done/In-Progress with disclosed residuals (see capabilities table). |
| **WO 26.10 — provider hardening** | state.md marked "NOT STARTED" but **superseded**: its components shipped elsewhere — Plugin3 shim → WO 28.14 (DONE), landlock default → WO 27.1 (DONE), memory dedup → WO 28.15 (still open), provider mocks → WO 28.13 (still open). Not a separate WO anymore. |

---

## Recommended next-session ordering (highest leverage first)

1. **Fix the `Cargo.toml`/`Cargo.lock` conflict markers** (BLOCKER) — unblocks all gates. (Concurrent subagent.)
2. **WO 29.9 — delete `npm/kf-plugin/`** — deletes code rather than fixing it (makes WO 28.4 + `plugin_tools/tests.rs:653` moot; retires ADR-028 NDJSON wire format for real).
3. **WO 28.6 — convert 7 binary-spawn e2e to in-process** — real e2e coverage, kills the orphan-process class.
4. **WO 28.7 → 28.9 — coverage gate + session 75%** — the discipline cap.
5. **Refresh `state.md`** — the 2026-08-10 current-state section is stale (this index can be the source of truth for the refresh).

*Items below the line are independent and parallelizable: WO 28.13, 28.15, 28.16, 28.17, 29.8, the bare-`#[ignore]` annotation pass, the `error.rs:49` typed-error TODO.*
