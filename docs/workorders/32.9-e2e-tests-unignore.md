# Workorder 32.9 — WO 28.6 e2e Tests Still `#[ignore]`'d

## Status: Pending

## Problem

WO 28.6 planned debugging the 7 binary-spawn e2e scenarios that hang in
`run_line_mode` startup. The hang is FIXED (context-index skip + daemon stdio
detach + `target/` filter). The 7 scenarios are still `#[ignore]`'d but the
ignore reasons now say "WO 27.2 startup hang is fixed — run with `cargo test
--test e2e --features e2e-tests -- --ignored`". They're runnable on demand but
not in default CI.

## Approach

1. Run the 7 e2e scenarios with `--ignored` to verify they pass.
2. If they pass, remove the `#[ignore]` attributes (or change to `#[ignore =
   "requires e2e-tests feature"]` if they need the feature gate).
3. If any fail, debug + fix forward.

## Files to touch

- `tests/e2e/scenarios/*.rs` — remove/update `#[ignore]` attributes

## Gate

- `cargo test --test e2e --features e2e-tests` — all scenarios pass without `--ignored`

## Done condition

The 7 e2e scenarios run in the default `--features e2e-tests` CI job without
needing `--ignored`.