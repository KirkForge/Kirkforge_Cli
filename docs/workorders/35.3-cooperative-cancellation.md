# WO 35.3 — Cooperative cancellation + temp-dir cleanup

## Status: Partially done

Done (WO 35.3 commit `fix(wo35.3)`):
- `TaskManager::cancel` is cooperative: flag + `CancellationToken` + notify;
  the `task` tool worker awaits `run_task` to completion instead of dropping
  it (the `ceiling:` comment at the old task.rs:505-510 is resolved and
  removed). Cancelled tasks keep status `Cancelled` and retain partial output
  (incl. worktree patch) in `TaskHandle.cancelled_result` via `task_output`.
- `TaskRequest.cancel: Option<TaskCancel>` threads the handle's cancel pair
  into `run_task`; the turn loop checks the flag before each turn; the token
  is attached to the subagent `Executor` (`set_cancel_token`) and per-call
  tool tokens are live children — an in-flight bash process group dies in
  milliseconds, not at `tool_timeout_secs` (regression test:
  `attached_cancel_token_kills_inflight_bash_promptly`).
- `ParallelOrchestrator::cancel_all()` cancels every in-flight role handle
  (flags/tokens threaded by `spawn_role`).
- Temp-dir cleanup is Drop-based (`TempDirGuard` in task_spawner.rs) —
  removed on error returns, cancellation, and normal completion (tests:
  `run_task_error_return_still_cleans_temp_dir`,
  `run_task_precancelled_exits_early_and_cleans_temp_dir`).

Remaining (disclosed):
1. Background bash jobs (`bash background=true`) spawned by a cancelled
   subagent are NOT cancelled — the global `BashJobRegistry`
   (`src/session/bash_jobs.rs`) has no owner tracking and the `Bash` tool has
   no task-id in `ToolContext`, so jobs cannot be attributed to a subagent.
   They remain individually cancellable via `bash_cancel` / `/jobs`.
   Remaining work: owner tag on `BashJobRegistry::spawn` + a task-id field
   threaded `ToolContextBuilder` → `ToolContext` → the bash tool; then a
   `cancel_jobs_of(owner)` call in the cancel path. Tracked in
   `state.md` pending.
2. An in-flight *model stream* is not aborted mid-request: cancellation ends
   it at the next stream event or the adapter's `request_timeout_secs`
   (bounded, but not instant). Remaining work: select the adapter stream
   against the cancel token inside `stream_iteration`.
3. The parent session's own prompt-cancel (TUI Ctrl-C) still uses the
   WO 15.7 snapshot-at-dispatch token semantics for in-flight tools; only
   subagent executors attach a live root token. Remaining work: thread a
   CancellationToken from the TUI cancel watcher into the parent executor
   (same `set_cancel_token` seam).

## Problem

`TaskManager::cancel()` (`src/tools/task.rs:248-258`) drops the
subagent future mid-flight. Verified consequences, all worse than the
review claimed:

1. **Temp dir leaks** — `run_task`'s cleanup
   (`remove_dir_all`, `task_spawner.rs:347`) is end-of-function, not
   Drop-based, so the `conversation.ndjson` + per-turn checkpoints +
   `.tmp` files leak on cancellation AND on every error path
   (log-open failure 240-243, executor-construction failure 248-256,
   any turn error `?` at 312-315).
2. **Foreground bash survives cancel** — tool calls run as detached
   `tokio::spawn` tasks (`dispatch.rs:293`); a dropped JoinHandle
   detaches (333-335), so the bash child (kill_on_drop) dies only at
   its own `tool_timeout_secs` — up to 120s after the user cancelled.
3. **Background bash jobs survive cancel** — they live in the global
   `BashJobRegistry` (`bash_jobs.rs:57`), orphaned but running.
4. **ParallelOrchestrator has no cancellation path at all** —
   `spawn_role` awaits `run_task` directly (165-168); its TaskManager
   entries are bookkeeping no one listens to.

## Root cause

The cooperative machinery already exists but is not wired:
`tokio-util` CancellationToken is used in `ToolContext`
(`tools/mod.rs:92`), bash (`tools/bash.rs:94`), workflow, jobs; the
executor has `cancelled: Arc<AtomicBool>` checks
(`turn.rs:363,973,1019`, `dispatch.rs:302,371-385`) and aborts
detached tasks on that flag; `run_task` even creates its own
`cancelled` flag (`task_spawner.rs:303`) — **nothing ever sets it**.
`TaskManager::cancel` has no path to any of it. The fix is wiring, not
building. (The existing `ceiling:` comment at `task.rs:505-510` names
exactly this upgrade path.)

## Approach

1. Replace `run_task`'s ad-hoc `cancelled` flag with a real
   `CancellationToken` threaded from `TaskHandle` → `run_task` →
   `run_turn_collecting` → executor (or minimally: have `cancel()`
   set the existing flag + fix the snapshot-at-dispatch issue,
   `helpers/mod.rs:74-82`).
2. On cancel: abort detached tool tasks, kill bash children
   (foreground via the token, background via the registry scoped to
   the subagent), then run cleanup — cooperatively, in `run_task`,
   instead of dropping the future.
3. Make temp-dir cleanup Drop-based (a guard struct whose Drop calls
   `remove_dir_all`) so error paths and panics clean up too.
4. Give the ParallelOrchestrator path a cancel path: select its
   `run_task` awaits against the TaskManager cancel signal (same
   pattern the `task` tool worker already uses), plus a
   `ParallelOrchestrator::cancel_all()`.

## Files to touch

- `src/tools/task.rs` — cancel wiring, worker select arm
- `src/session/task_spawner.rs` — token/flag plumbing, Drop-guard for
  temp dir
- `src/session/executor/` — `turn.rs`/`dispatch.rs` only where the
  flag is read (should already work once set)
- `src/session/parallel_orchestrator.rs` — cancel_all + select arm
- `src/session/bash_jobs.rs` — subagent-scoped job cancellation (if
  tractable; otherwise disclose as follow-up)

## Gate

- `cargo clippy -p kf-code --lib --tests -- -D warnings`
- `cargo fmt --check`
- `cargo nextest run -p kf-code --lib tools::task session::`
- New tests: (a) cancelled subagent's temp dir is removed (no
  `kf-code-task-*` left in tempdir); (b) error return from a turn also
  removes the temp dir; (c) cancel while a bash `sleep` is running
  kills the child within a bounded window (not tool_timeout_secs);
  (d) ParallelOrchestrator cancel_all stops in-flight roles.

## Done condition

Cancellation is cooperative end-to-end (model request aborted, tools
aborted, bash killed, cleanup run); temp dirs never outlive their
subagent on any path; the `ceiling:` comment at `task.rs:505-510` is
resolved and removed.
