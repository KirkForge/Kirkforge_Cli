# WO 38.13 — Docs truth pass + WO status reconciliation

## Status: Pending

## Problem (doc-claims + stubs audits)

1. **TECHNICAL.md overclaims**: personas-Anthropic-only paragraph
   (stale pre-26.6, :375); `kf-budget` binary that doesn't exist
   (:674-675); shell-plugin fallback "sources" that are an empty
   HashMap (:1336); bash allowlist "deferred" but shipped in 32.18
   (:230-233 + bash_safety.rs:364 comment); budget/stratum present
   tense while latent (38.8 fixes the latency; the doc must match
   reality either way); test-count undershoot (:56-58); hooks event
   list inconsistent + duplicated bullet; stale bench-workflow claim
   (:1116).
2. **ADR staleness**: ADR-019/026/040 (Accepted) describe the
   archived VSCode extension as live — amendment notes needed.
3. **WO status: two sources of truth disagree for ~70 workorders**
   (file `## Status` vs README index): files-Pending/index-Done for
   most of 32/33; files-Done/index-Pending for all of 34-37. The
   adr_xref_drift test pattern exists for ADRs; nothing polices WOs.
4. **Silent deferrals to surface**: `tools` plugin command advertises
   seven dead TS lint engines (native.rs:213-226 — contradicts the
   honest `verify` sibling); `doctor` claims C/C++/Go/Rust/SQL
   scanning that only handles JS/TS/Py (native.rs:87-92, 159-163);
   WO 32.6 cross-tool benchmark "shipped" but never run (results doc
   is an unfilled template — tracked in WO 39.1 now, fix the status
   here); stale kf-plugin-tools Cargo comment; jobs/mod.rs stub doc;
   testdoctor ignore pointing at deleted CI; verifier bus register
   doc describing removed stubs.
5. **config.toml.example drift**: defaults 100/5/30 → actual
   200/20/120; minify_write_side now true; enabled_plugins example
   silently disables stratum+budget; missing [session]
   worktree_enabled knob.

## Approach

1. TECHNICAL.md pass: fix every listed item; one honest-sentence
   budget caveat (or WO 38.8 lands first and it becomes true).
2. ADR amendments (not status flips — history notes).
3. WO reconcile: sweep all headers vs index (script-assisted),
   single-source or drift-test: extend the kf-budget-core drift test
   pattern to WO status (header ↔ README row must agree).
4. native.rs facade strings corrected to the Rust runtime truth;
   the other stub-doc one-liners; 32.6 status → "Partially done
   (framework only; live run tracked in WO 39.1)".
5. config.toml.example regenerated values + [session] section.

## Gate

`cargo test -p kf-budget-core --test adr_xref_drift` + the NEW WO
drift test green; `bash scripts/check-artifact-consistency.sh` green;
grep-audit: no "tool-lint-" outside history, no "deferred pending"
on the shipped allowlist.

## Done condition

Every doc claim is either true or explicitly marked as
deferred-with-tracking; WO status has one enforced source of truth.
