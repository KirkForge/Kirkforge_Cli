# WO 39.1 — Bench suite repair + first cross-tool run

## Status: Phase 1-2 Done (Phase 3-4 Deferred)

## Problem (bench audit)

1. **Suite cannot load**: `benches/tasks/use_workflow_run.toml:23`
   uses verify variant `output_contains` — not in the enum
   (kf-bench lib.rs:31-39); `load_tasks` fails ALL 30. CI only checks
   3 top-level keys, so it's invisible.
2. **Free-win tasks**: ≥5/30 verify pass on the UNEDITED setup
   (add_doc_comment, fix_failing_test, fix_clippy_warning, add_adr,
   add_enum_variant); pr_review's verify is `cargo build` (passes on
   no-op); 4-5 `use_*` tasks reference kf-only tools → must be
   excluded from cross-tool subsets.
3. **Cross-tool script broken 4 ways** (`scripts/run-cross-tool-bench.
   sh`): nonexistent `--prompt` flag, exit-code-as-success, no
   workspace isolation, hardcoded 0 tokens; references a nonexistent
   example + tasks dir.
4. **No external harness**: zero code invokes `claude -p` /
   `codex exec` / `opencode run`.
5. **Metrics gaps**: ratios never computed (computable post-hoc);
   no repeat/variance support; seed not pinned in bench path;
   ExternalToolReport lacks cost + prompt/completion split.

## Approach (phased — each phase shippable)

**Phase 1 (hours)**: fix the TOML; extend the CI lint to validate
verify variants; repair/replace the 5 free-win verifies; tag the
kf-only tasks; `bench run` exit code reflects success.
**Phase 2 (hours)**: `bench export-tasks <dir>` helper (materialize
setup files); replace the script's kf-code half with
`kf-code bench run --output`; compute verified_success/total_tokens +
per-dollar + per-wall-clock ratios post-hoc (jq/python over JSONs).
**Phase 3 (1-2 days)**: generic external runner (bash+jq): per tool ×
task — export workspace, run `claude -p --output-format json
--dangerously-skip-permissions` / `codex exec --json` / `opencode
run` with per-tool model pinning, parse per-CLI usage formats, run
the SAME verify in the task dir, emit ExternalToolReport rows with
real tokens/cost/wall-clock. Add cost field to the report struct.
**Phase 4 (half day + credentials)**: same-model plumbing — one
LiteLLM gateway exposing both API shapes, same key for all four;
record model id + gateway + per-tool token-counting semantics in the
results doc; run it, publish under docs/benchmarks/out/ with
single-shot disclosure; update WO 32.6 status honestly.

## Gate

Phase 1: suite loads, `bench list` shows 30, CI lint validates
variants. Phase 2: kf-code baseline JSON with computed ratios.
Phase 3-4: cross-tool table with ≥3 agents, honest constraints
section. Every phase: standard clippy/fmt/nextest.

## Done condition

The token-economics thesis has a published, methodologically honest
cross-tool number — the thing no competitor architecture page can
ignore.
