# WO 42.1 — Delete dead testdoctor test + fix coverage gate path

## Status: Done

## Problem (verified: tests+CI agent)

`crates/kf-testdoctor/src/gaps.rs:416` — `default_thresholds_match_ci_yml` reads
`.github/workflows/ci.yml` which no longer exists (WO 40.1 split it into 3 files).
The test is `#[ignore]`d with reason "coverage-gate targets dict not yet in ci.yml"
but ci.yml is gone, not "not yet." The thresholds now live in `scripts/ci-local.sh:128`.

## Approach

Delete the test. It's dead code pointing at a phantom file. The coverage gate
itself works (inline in ci-local.sh). If a drift guard is wanted, it should read
ci-local.sh or ci-nightly.yml, not a deleted workflow.

## Gate

Standard. `cargo nextest run -p kf-testdoctor` still green.