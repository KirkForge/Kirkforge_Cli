# WO 43.0 — Series 43 overview: honest-assessment backlog serialization

## Status: Planned

## Problem

Findings from the honest codebase assessment (stub paths, silent-error sites,
orphan crate, placeholder ADRs, missing hardening, test-infra upgrades)
existed only as a backlog dump with stale line numbers. Six read-only
analysis agents verified every claim against the tree and serialized the
surviving work as WO 43.1-43.17.

## Claim verification summary (drift found and corrected)

Claims that were WRONG or already shipped (closed in the corresponding WO):

- **scout.rs:138 `unimplemented!()` "live panic path"** — inside
  `#[cfg(test)] mod tests` (scout.rs:103), compiled out of release. Drift.
- **ADR 0011/0012 "placeholder, no code"** — already `Rejected (2026-07-17)`,
  headers + index agree. Triage is done; 43.7 fixes stale prose only.
- **ADR 0018:79 "executor intentionally stubbed"** — stub is gone; Bash +
  Workflow both execute (runner.rs:40-47). ADR overclaims.
- **"Landlock deferred / test-only"** — landlock is default-on in the bash
  path (ADR-054 amendment); seccomp shipped opt-in (WO 30.4). Real gap:
  plugin wrapper passes `None` landlock paths (wrapper.rs:254). → 43.11.
- **"Session-scoped state only"** — sessions, undo, carryover, tasks, jobs,
  usage all persist. Real gap: no single classification table; background-bash
  registry dies silently. → 43.10.
- **Windows rename retry on atomic write** — already shipped
  (`rename_with_retry`, atomic_write.rs:72-94). → 43.2 narrowed to
  open/fsync EINTR.
- **EAGAIN on non-blocking pipes** — misapplied; atomic writes target regular
  files. Real pipe EAGAIN handled at line_mode.rs:452,477. Dropped.
- **MCP content-hash consent "missing"** — shipped as WO 42.5; plugins have
  NO consent prompts at all. → 43.17 mirrors the MCP ledger.
- **WO 7.6 Windows parity "WIP"** — Done; full blocking nextest on
  windows-latest (ci-merge.yml:192-220). → 43.12 scopes the residual.
- **`.ok()` = 18 sites / `_ => {}` = 48** — actual: 137 raw `.ok()` (5 risky),
  43 catch-alls (~2 real event drops). → 43.8.
- **TECHNICAL.md 18 unimplemented spec tasks** — 19 rows at :1435-1455, plus
  3 unmapped. → 43.13.

Claims CONFIRMED real: adapter config no-ops (8 silent setters),
PlaceholderTab error opacity, orphaned kf-rbac crate, string-probe error
classification (src/main/error.rs:52-73), audit redaction residual
(bash command / PluginTool args / Hook reason), path-safety untested
branches, no-throw dispatch residual (3 panic sites), correction prompt
genericity.

## Series table

| WO | Title | Priority |
|----|-------|----------|
| 43.1 | Typed adapter errors (retire string probe) | High |
| 43.2 | Atomic-write EINTR retry (open/fsync) | Medium |
| 43.3 | Audit-log redaction residual gap | High |
| 43.4 | Path-safety property tests (proptest already a dev-dep) | High |
| 43.5 | PlaceholderTab + adapter no-op disclosure | Medium |
| 43.6 | Wire kf-rbac into the daemon | Medium |
| 43.7 | Placeholder-ADR prose triage (0011/0012/0018) | Low |
| 43.8 | Silent-error triage (5 risky `.ok()` sites) | Medium |
| 43.9 | Per-failure correction prompts (kf-routing) | Medium |
| 43.10 | Cross-session state preservation policy | Medium |
| 43.11 | Landlock graduation to plugin path | High |
| 43.12 | Windows test parity residual | Low |
| 43.13 | Spec-task triage (TECHNICAL.md) | Medium |
| 43.14 | cargo-mutants nightly gate | Low |
| 43.15 | Machine-greppable ADR predicates | Low |
| 43.16 | No-throw dispatch finish + grep gate | Medium |
| 43.17 | Plugin consent ledger (mirror MCP 42.5) | Medium |

Triage note: ~11 functions ≥200 lines exist (top: 746/434/390/378/351
lines). No WO — refactor-on-touch policy, listed as context in 43.16.

## Gate

`cargo test -p kf-budget-core --test adr_xref_drift` — new WO rows must
agree with file `## Status` headers.

## Done

All 17 findings serialized with verified file:line evidence; drift corrected
in-line; no code changed this session.
