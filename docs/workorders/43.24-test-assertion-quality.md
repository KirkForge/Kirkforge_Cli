# WO 43.24 — Test assertion-quality triage (assert-free, tautological, stale ignores)

## Status: Planned

## Problem

Sampled audit of 4,741 test attrs (src/ + crates/). Crude assert-free scan
returned 96 candidates; manual verification → ~25 true positives. Rest were
scanner FPs: proptest strategy braces (`{0,64}`) defeating brace-matching,
helper-fn asserts (tui/keys `check`), `#[test]` inside string fixtures
(minify/lang.rs).

Worst verified offenders (name promises, body never checks):
- src/adapters/mod.rs:1620 `build_reqwest_client_returns_a_client` — body is
  `let _client = build_reqwest_client();` Asserts nothing.
- src/session/hooks.rs:1546 `test_run_with_context_fires_in_process_hook` —
  registers no hook, asserts nothing.
- src/shared/access/mod.rs:888,898 `test_warn_if_unsandboxed_*_is_quiet` —
  "quiet" never observed (no output capture). Security-adjacent.
- src/session/mcp_client/mod.rs:1745 `..._null_id_is_routed_correctly` — no
  assert; sibling `missing_id_is_noop` (:1749) asserts `pending.is_empty()`.
- src/session/mcp_client/mod.rs:1435,1731 noop tests — assert nothing.
- src/shared/permission.rs:1533 `glob_match_total_unicode` — `let _ =` body,
  no-panic-only property.
- ~13-test `*_does_not_panic`/`*_is_noop` smoke class (grep.rs:973,
  go_vet.rs:126, go_test.rs:131, landlock.rs:314, process_group.rs:153,
  tui/mod.rs:1628, daemon/client.rs:856, executor/types.rs:170,
  executor/tests/turn.rs:22, executor/approval.rs:166, carryover.rs:816,
  session_index.rs:707, kf-orchestrator sink.rs:114).

Security-module deny ratios (by test-fn name):
- permission.rs: 49 fns + proptest suite — deny 8 / allow 5, fails-closed
  covered. Healthy.
- path_safety.rs: 44 fns — deny 18 / allow 12. Healthy (43.4 adds properties).
- bash_runner/mod.rs: 51 fns — deny/block 18. Healthy.
- audit.rs: 4 fns — 1 redaction (bash cmd), 0 for plugin-args/hook-reason
  residuals → owned by 43.3, cross-ref only, no duplication here.

`#[ignore]` inventory: 17 sites, reasons accurate except:
- crates/kf-rbac/tests/jwt.rs:402 cites "tracked in WO 32.10" — 32.10 is Done
  (`c20c833`); live tracking is state.md pending. Stale pointer.
- src/session/plugin_tools/tests.rs:1005 "timing-sensitive watcher" — flake
  by design (500ms debounce); rewrite with explicit signal or keep ignored.

Sleeps: WO 40.4 held — all >50ms sleeps are in `#[ignore]`d tests or bounded
poll+deadline loops; residual ~12 naked 5-25ms slop sleeps (bash.rs:694,720;
dispatch.rs:907,934; task_spawner.rs:826,907; task.rs:1057,1111;
wrapper.rs:655; tui/commands/mod.rs:303,320; correction.rs:561). Order
dependence: none found — metrics statics use documented thread-local
override + TEST_LOCK (metrics.rs:305-319).

## Root cause

No gate checks assertion quality. Smoke tests ("doesn't panic") accumulated
and names drifted from bodies. cargo-mutants (43.14) finds these mechanically
later; this WO fixes the known ones first.

## Approach

1. Fix named weak tests to assert observable state: reqwest client config,
   capture output for "quiet" tests, `pending` untouched in mcp noops,
   register a hook + marker in hooks.rs:1546.
2. Delete tests that cannot fail (mcp_client:1731 empty-map noop).
3. Upgrade `glob_match_total_unicode` to at least assert determinism.
4. Repoint stale kf-rbac ignore reason at state.md pending.
5. Optional, only if cheap: assert-free-body heuristic in kf-testdoctor
   (suggest.rs already parses test sources).

## Files

- src/adapters/mod.rs, src/session/hooks.rs, src/shared/access/mod.rs,
  src/session/mcp_client/mod.rs, src/shared/permission.rs,
  crates/kf-rbac/tests/jwt.rs (test-only edits)

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`. No prod behavior changes expected. Index row in
docs/workorders/README.md added on pickup (audit session was file-add-only).

## Done

(not started)
