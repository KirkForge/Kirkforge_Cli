# WO 43.7 — Placeholder ADR triage (0011 persistent knowledge, 0012 speculative priming, 0018 scheduled jobs)

## Status: Planned

## Problem

Backlog claims three "placeholder" ADRs need triage. Two claims have already
drifted — the triage happened; the third drifted the other way (code moved
past the ADR).

1. **ADR-0011** `docs/adr/0011-persistent-knowledge.md:3` — `Status: Rejected
   (2026-07-17)`, with a rejection note at :6-12 ("deferred since 2026-06-24
   with zero implementation… Per the B10 review, a deferred-with-no-code ADR
   is not live architecture"). Body still self-describes as "This ADR is a
   placeholder" (:136-139). Index agrees: `docs/adr/README.md:46` shows
   `Rejected (2026-07-17)` — two-source-of-truth intact, `adr_xref_drift`
   green. Grep confirms zero implementation (no `Finding`/`FindingKind`,
   no `knowledge` feature in any Cargo.toml).
2. **ADR-0012** `docs/adr/0012-speculative-priming.md:3` — same shape:
   `Rejected (2026-07-17)`, note at :6-10, index row `docs/adr/README.md:48`
   agrees. No `PreWarmCache` anywhere in `src/` or `crates/`.
3. **ADR-0018** `docs/adr/0018-scheduled-jobs.md:79` — "their executor is
   intentionally stubbed… records `Failure: skill execution not yet
   implemented`". Stale: that string exists ONLY in the ADR. The shipped
   `JobKind` (`src/jobs/schedule.rs:76-92`) is `Bash` + `Workflow` — no
   `Skill` variant at all. Both arms are fully implemented
   (`src/jobs/runner.rs:40-47`; workflow dispatch is WO 17.7, runner.rs:8-9).
   The ADR's data model (:38-41 `JobKind::Skill`) and TUI section
   (`/jobs schedule … skill`, :85-86) describe a surface that never shipped.

## Root cause

0011/0012 were already triaged by the B10 review; the backlog item predates
it. 0018's body was never amended when WO 17.7 replaced skill jobs with
workflow jobs — the ADR now overclaims a stub that no longer exists and
underdocuments the Workflow variant that does.

## Approach

1. 0011, 0012: no status change (already Rejected, index agrees). Close the
   backlog item. Optionally soften the internal "placeholder"/"promoted from
   Deferred to Accepted" phrasing (:136-153, :148-150) to match Rejected —
   prose-only, no index change.
2. 0018: add an amendment note (dated, referencing WO 17.7): `JobKind::Skill`
   was superseded by `JobKind::Workflow { template, vars }`, fully executed
   via `kf_workflow::WorkflowExecutor`; the "intentionally stubbed" paragraph
   at :79 is historical. Update the data-model block and TUI command list to
   match shipped reality.
3. Both-source rule: 0018 keeps `Accepted` in header and index — if the
   amendment is expressed as a compound status, it must be identical in
   `docs/adr/0018-scheduled-jobs.md:3` and the `docs/adr/README.md` row
   (same commit; `adr_xref_drift` gate).

## Files

- `docs/adr/0018-scheduled-jobs.md`
- `docs/adr/0011-persistent-knowledge.md` (optional prose-only)
- `docs/adr/0012-speculative-priming.md` (optional prose-only)
- `docs/adr/README.md` (only if 0018's status string changes)

## Gate

`cargo test -p kf-budget-core --test adr_xref_drift`; `scripts/ci-local.sh quick`.

## Done

No ADR claims an unimplemented stub as current behaviour; 0011/0012 backlog
item closed as already-triaged; 0018 describes the shipped Workflow executor.
