# WO 44 — Series 44 overview: regression audit + five-area fresh sweep

## Status: Done

## Problem

WO 43 series closed (43.20, 43.22, 43.23, 43.24 implemented; full series
Done). Two questions remained: (1) did the claimed-Done WO 43 work survive
merges, and (2) where does the next phase of work live?

## Method

- 1 auditor verified all 36 Done WO 43.x claims against current code
  (file:line + git log revert check): 34 VERIFIED, 2 findings → 44.1, 44.2.
- 5 area auditors (adapters/shared, session, tui/main/daemon, tools/crates,
  tests/CI/scripts) ran fresh-eyes sweeps with a strict evidence bar
  (file:line, deduped against existing WOs + state.md deferrals + in-flight
  43.20/22/23/24 scopes). 23 findings survived.
- External review (ChatGPT) re-prioritized the findings by risk, refined
  44.21 (normalize_for_safety is a preprocessor not a shell lexer —
  ceiling documented), and confirmed 44.28 as the top-priority security
  fix. 43.34 (context-retrieval smear) was cited as Tier 1 but is already
  Done (WO 43.34, verified at lib.rs:1067 + regression test :2357).

## Priority tiers (risk-ordered, not by WO number)

### Tier 1 — fix first

| WO | Title | Risk |
|----|-------|------|
| 44.28 | Symlink TOCTOU re-check unreachable on the allowed path | P0 security: successful-path bypass |
| 44.22 | Anthropic key sent to ollama_host | P0: credential destination |
| 44.36 | Executor death-spin (closed channel → 100% CPU, silent input drop) | P1: wedges UI |
| 44.52 | PR CI silently skips all Rust checks on classifier failure | P0: release-process gate no-ops |
| ~~43.34~~ | ~~Context retrieval smear~~ | Already Done (43.34) |

### Tier 2

| WO | Title | Risk |
|----|-------|------|
| 44.31 | Budget guard compares bytes vs tokens (slices ~4× too early) | Token-economics correctness |
| 44.46 | Stratum mode config is dead wiring (never reaches runtime) | Core feature silently ignored |
| 44.20 | Bash `&` background-separator bypass | Allowlist bypass |
| 44.21 | Permission bash rules match raw text (quote/whitespace/case evasion) | Security boundary hole |
| 44.23 | Bedrock error-frame JSON escaping | Transport correctness |
| 44.24 | Audit scrubber token-literal gaps | Security observability |

### Tier 3

| WO | Title |
|----|-------|
| 44.1 | Line-mode Ctrl-C doesn't cancel in-flight turn |
| 44.2 | ADR predicate-block backfill (43.15 step 4) |
| 44.29 | `/plugins` reload drops Node/Go/Generic verifiers |
| 44.30 | `max_tool_result_chars` never applies to file tools |
| 44.37 | Scheduled-job poll does blocking disk I/O on every event-loop iteration |
| 44.38 | PTY streaming appends to wrong chat entry |
| 44.39 | Terminal left in raw mode on TUI setup failure |
| 44.44 | Workflow/jobd bash spawn hygiene (incl. Windows timeout deadlock) |
| 44.45 | Workflow engine error/budget semantics |
| 44.47 | bench verify_task unbounded blocking sh -c |
| 44.48 | Docker exit-code inversion on Windows |
| 44.53-56 | CI/test infrastructure (nightly ollama, mutants baseline, e2e gate, impact-fallback OOM) |

## Implementation wave 1 (this session, 6 parallel worktrees)

44.28, 44.22, 44.36, 44.52 (Tier 1) + 44.31, 44.46 (Tier 2 top).

## Done

All 25 findings verified at file:line in this checkout; auditors' rejected
candidates documented in session records. README index rows added.
Priority tiers assigned per external review. 44.21 and 44.22 refined
with ceiling/upgrade-path notes. 43.34 confirmed already Done (not
re-opened).