# WO 44 — Series 44 overview: regression audit + five-area fresh sweep

## Status: Done

## Problem

WO 43 series closed (43.20, 43.22, 43.23, 43.24 implemented this session;
full series Done). Two questions remained open: (1) did the claimed-Done WO 43
work actually survive merges, and (2) where does the next phase of work live?

## Method

- 1 auditor verified all 36 Done WO 43.x claims against current code
  (file:line + git log revert check): 34 VERIFIED, 2 findings → 44.1, 44.2.
- 5 area auditors (adapters/shared, session, tui/main/daemon, tools/crates,
  tests/CI/scripts) ran fresh-eyes sweeps with a strict evidence bar
  (file:line, deduped against existing WOs + state.md deferrals + in-flight
  43.20/22/23/24 scopes). 23 findings survived.

## Series contents

- 44.1-44.2: regressions/incomplete landings from the WO 43 series
  (line-mode Ctrl-C doesn't cancel in-flight turn; ADR predicate backfill
  never finished).
- 44.20-44.24: adapters + shared security (bash `&` separator bypass,
  permission rule normalization, Anthropic key→ollama_host miswiring,
  Bedrock error-frame JSON escaping, audit token-literal gaps).
- 44.28-44.31: session subsystem (symlink-swap check dead on allowed path,
  `/plugins` reload drops Node/Go verifiers, file-tool truncation bypass,
  budget byte/token unit mismatch).
- 44.36-44.39: TUI/CLI (executor death-spin, job-poll disk throttle,
  PTY stream wrong entry, raw-mode restore window).
- 44.44-44.48: tools + crates (workflow/jobd bash spawn hygiene, workflow
  error/budget semantics, stratum mode dead wiring, bench verify unbounded,
  docker exit-code inversion on Windows).
- 44.52-44.56: tests/CI (PR CI silent skip on classifier failure, nightly
  ollama red-by-construction, mutants baseline overwritten, e2e gate
  invisible, impact-fallback OOM).

Priority signal: 44.20 (allowlist bypass), 44.22 (key sent to wrong host),
44.36 (dead-but-spining executor), 44.52 (PR gate can silently no-op) are
the highest-risk items. Numbering is non-contiguous (ranges were pre-assigned
to parallel auditors); gaps are intentional.

## Done

All 25 findings verified at file:line in this checkout; auditors' rejected
candidates documented in session records. README index rows added in the
same commit as this file.
