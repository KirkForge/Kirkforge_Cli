# WO 45.16 — Split `src/session/config/mod.rs` (2952 lines / 120477 bytes)

## Status: Done

## Problem

`src/session/config/mod.rs` is the largest source file in the workspace at
**2952 lines / 120477 bytes** (verified via `wc -l` / `wc -c`). It mixes three
distinct concerns behind one module door:

1. **Config bootstrap** — `load_config`, `load_or_create_config` (+ strict
   variant), `save_config`, `freeze_launch_sandbox`, legacy path handling
   (lines 1–384, ~380 lines).
2. **TOML→Config merge** — `merge_toml_into_config`, a 436-line field-by-field
   merge function (lines 389–825). This is the single largest function in the
   file and the reason the file crossed 2000 lines.
3. **Diff summary + plugin-sources env parse** — `config_diff_summary`
   (lines 830–971, ~145 lines) and `parse_plugin_sources_env` (lines 973–997).
4. **Inline tests** (lines 1000–2952, ~1950 lines — the bulk of the file is
   tests, which is fine, but the non-test code is still ~1000 lines across
   three concerns).

The `env_overrides` submodule (line 60) is already extracted. The remaining
merge + diff logic has not been split.

## Root cause

`merge_toml_into_config` grew field-by-field as `Config` acquired fields
across WOs 22–44. No boundary was ever drawn between "bootstrap/load" and
"merge/diff", so the file accreted to 2952 lines. The merge function alone
(436 lines) is larger than many complete modules in this repo.

## Approach

Opportunistic extraction (no behavior change, pure code movement):

1. **`merge.rs`** (new, ~450 lines) — move `merge_toml_into_config` and its
   `expand_tilde_str` helper (used only by merge + load). Re-export from
   `mod.rs` via `use merge::merge_toml_into_config;` so `load_config` is
   unchanged.
2. **`diff.rs`** (new, ~170 lines) — move `config_diff_summary` and
   `parse_plugin_sources_env`. Both are pure functions over `Config` with no
   dependency on the bootstrap path.
3. **`mod.rs`** keeps: module docs, `load_config`, `load_or_create_config`*,
   `save_config`, `freeze_launch_sandbox`, `legacy_config_path`,
   `env_overrides` wiring, `parse_bool_env`, and the inline `tests` module.
   Target: ~1000 lines (non-test) + tests.

Move the corresponding test fns into the new submodules' `#[cfg(test)]`
modules so each test sits next to the code it exercises.

### Split map

| Section | Lines (current) | Destination |
|---|---|---|
| `merge_toml_into_config` | 389–825 | `merge.rs` |
| `config_diff_summary` | 830–971 | `diff.rs` |
| `parse_plugin_sources_env` | 973–997 | `diff.rs` |
| everything else | 1–384, 999–2952 | stays in `mod.rs` |

## Files

- `src/session/config/mod.rs` (edit — remove extracted fns, add `mod merge; mod diff;`)
- `src/session/config/merge.rs` (new)
- `src/session/config/diff.rs` (new)

## Gate

- `scripts/test-fast.sh`
- `cargo clippy --all-targets -- -D warnings`
- `cargo fmt --check`
- `cargo check --workspace --all-targets`
- Windows cross-compile: `cargo clippy --target x86_64-pc-windows-gnu`
  (via `scripts/ci-local.sh`)
- No behavior change: the merge/diff functions are pure code movement;
  existing tests must pass unchanged.

## Done

Pure mechanical code-movement split of `src/session/config/mod.rs`
(2952 → 1696 lines). Two sibling modules extracted; `mod.rs` is now the
re-export hub. No behavior change, no API change, no new deps.

### Commits (branch `wo/wo45.16`, not merged, not pushed)

1. `d076da71` — `refactor(wo45.16): extract merge_toml_into_config into merge.rs`
2. `8a41dd30` — `refactor(wo45.16): extract config_diff_summary + parse_plugin_sources_env into diff.rs`

### Files changed (before → after line counts)

| File | Before | After | Delta |
|---|---|---|---|
| `src/session/config/mod.rs` | 2952 | 1696 | −1256 |
| `src/session/config/merge.rs` | — | 870 | new |
| `src/session/config/diff.rs` | — | 423 | new |
| `src/session/config/env_overrides.rs` | 628 | 628 | unchanged |

### What moved

- `merge.rs` — `merge_toml_into_config` (441-line fn, `pub(super)`) + 19 tests
  (`test_merge_toml_*`, `compaction_use_llm_alias_backward_compat`,
  `price_overrides_table_parses`). Re-exported via
  `use merge::merge_toml_into_config;` in mod.rs so `load_config` and the
  drift-guard test are unchanged.
- `diff.rs` — `config_diff_summary` (`pub`, re-exported via
  `pub use diff::config_diff_summary;`) + `parse_plugin_sources_env`
  (`pub(super)`, re-imported via `use diff::parse_plugin_sources_env;` so
  `env_overrides.rs`'s `super::parse_plugin_sources_env` still resolves
  unchanged) + 24 tests (`test_config_diff_summary_*`,
  `test_parse_plugin_sources_env_*`).
- `mod.rs` keeps: module docs, `expand_tilde_str`, `parse_bool_env`,
  `load_config`, `load_or_create_config*`, `save_config`,
  `freeze_launch_sandbox`, `legacy_config_path`, `first_run_banner`, the
  `env_overrides` submodule wiring, and 53 tests (env-override tests,
  load/save/migration tests, `freeze_launch_sandbox` tests,
  `parse_bool_env`/`expand_tilde_str` tests, `config_field_count_drift_guard`,
  `schema_drift_preserves_user_values`, `strict_config_load_*`,
  `first_run_banner_*`, `save_config_atomic_*`).

### Drift guards (unchanged, all green)

- `CONFIG_FIELD_COUNT = 108` ✓
- `MERGE_TOML_EXPECTED = 100` ✓
- `ENV_OVERRIDE_EXPECTED = 96` ✓ (env_overrides.rs literal count unchanged)
- `config_field_count_drift_guard` test PASS [0.334s]
- `schema_drift_preserves_user_values` test PASS [0.250s]

### Gate outputs (all four required + Windows cross-compile)

1. `scripts/test-fast.sh` — **PASS**
   `Summary [ 156.669s] 4707 tests run: 4707 passed, 15 skipped`
   (config tests: 96/96 PASS — 19 in merge.rs, 24 in diff.rs, 53 in mod.rs)
2. `cargo clippy --all-targets -- -D warnings` — **PASS**
   `Finished `dev` profile [unoptimized + debuginfo] target(s) in 9m 45s` (exit 0, no warnings)
3. `cargo fmt --check` — **PASS** (exit 0, no diff)
4. `cargo check --workspace --all-targets` — **PASS**
   `Finished `dev` profile [unoptimized + debuginfo] target(s) in 4m 29s` (exit 0)
5. `cargo clippy --target x86_64-pc-windows-gnu --workspace --all-targets -- -D warnings` — **PASS**
   `Finished `dev` profile [unoptimized + debuginfo] target(s) in 6m 34s` (exit 0, no warnings)

### Notes

- Two transient flakes appeared in early `test-fast.sh` runs
  (`edit_file_replacement_equals_replacen` proptest 30s timeout;
  `attached_cancel_token_kills_inflight_bash_promptly` subprocess-cancel
  timing) — both in `src/tools/edit_file.rs` and `src/session/executor/`,
  neither touching `session/config`. Verified pre-existing by running both
  in isolation on the base commit `ca9a4c3b` (PASS, 5.764s + 3.997s) and on
  this branch (PASS, 5.712s + 4.033s). Cause: CPU contention with 4707
  parallel tests under the ci-fast 30s timeout on a slow build host. A clean
  re-run with warm cache passed 4707/4707.
- `parse_plugin_sources_env` kept its `#[cfg_attr(not(test), allow(dead_code))]`
  attribute (preserved verbatim in diff.rs).
- All `ponytail:` / `ceiling:` / contract doc comments preserved verbatim.
- `env_overrides.rs` line 9 import (`use super::{expand_tilde_str, parse_bool_env, parse_plugin_sources_env};`) unchanged — mod.rs re-imports `parse_plugin_sources_env` from diff.rs so `super::` still resolves.

### Deferrals

None. The full WO scope (merge.rs + diff.rs extraction) is complete.

### Unverifiable

Nothing. All gates green, all 96 config tests pass, drift guards unchanged.
