# WO 45.41 — Config knob count audit (external reviewer "400 knobs" claim)

## Status: Done

## Problem

An external reviewer scored KirkForge's architectural simplicity at 7/10
(vs Codex/Claude/OpenCode at 9) and claimed KirkForge has "400 config
knobs" as a danger. This audit verifies the actual count against the
code.

## Verdict: REFUTED — 139 distinct knobs, not 400. A drift-guard test enforces the count.

### Evidence — three config surfaces, all counted at file:line

1. **`Config` struct fields: 107** (enforced by a drift-guard test).
   `src/shared/config/config/mod.rs:49`:
   ```rust
   pub const CONFIG_FIELD_COUNT: usize = 107;
   ```
   The test `config_field_count_drift_guard`
   (`src/session/config/mod.rs:1222-1446`) asserts three invariants stay
   in sync: the const, the serde-serialized field count, and the
   `merge_toml_into_config` key count. Breakdown by sub-struct
   (documented at `mod.rs:22-31` and the test comment at
   `session/config/mod.rs:1226-1230`):
   - `ModelConfig`     34 (33 direct + `subagent_provider` sub-struct handle)
   - `SecurityConfig`  22 (19 direct + 3 sub-struct handles: `computer_use`, `docker`, `sandbox`)
   - `ToolConfig`      35
   - `SessionConfig`   8 (was 9 pre-WO-45.37; `worktree_enabled` +
     `auto_apply_patch` replaced by the `artifact_policy` enum)
   - `DisplayConfig`   7

   (An earlier draft of this WO recorded 108 / SessionConfig=9; that was
   stale by one WO cycle — WO 45.37 landed the `artifact_policy` refactor
   after this audit was written but before it was closed. Corrected here
   to match the live `CONFIG_FIELD_COUNT` const and the drift-guard test,
   both of which assert 107.)

2. **Environment-variable overrides: 96** (91 `KF_CODE_*` + 5 provider API keys).
   `src/session/config/env_overrides.rs:12-628` — counted by the same
   drift-guard test (`mod.rs:1372-1389`, `ENV_OVERRIDE_EXPECTED = 96`).
   The test scans `env_overrides.rs` for `"KF_CODE_"` + API-key string
   literals so it fails if anyone adds/removes a var without updating
   the const. Verified: `grep -c '"KF_CODE_' env_overrides.rs` = 91,
   +5 API-key literals = 96.

3. **CLI flags: 23 on the `run` subcommand, 62 across all subcommands.**
   `src/cli.rs:42-205` (the `Run` variant — 23 `#[arg]` attributes
   between those lines). The other 39 are on `Sessions`, `Daemon`,
   `Jobd`, `Replay`, `Bench`, `Plugin`, `Doctor`, `Update`,
   `Completions` — most are positional args or single flags for
   subcommand-specific state, not agent-behavior knobs.

### Total: 139 distinct config surfaces (107 struct + 96 env − ~64 overlap where an env var shadows a struct field)

The reviewer's "400" is not a real count of anything in this codebase.
The closest plausible miscount sources:
- 108 struct fields × 3 layers (struct + env + CLI) ≈ 324 — still not 400.
- The 5 sub-structs each have a `Default` impl that restates every field
  (108 lines), which a naive `grep pub` over the whole config tree
  would double-count to ~216.
- Including `McpServerConfig`, `LspServerEntry`, `PermissionRule`,
  `ComputerUseConfig`, `DockerConfig`, `SandboxConfig`, `SubagentProvider`,
  `ModelPrice` sub-struct fields adds ~30, but these are nested types,
  not top-level knobs.

None of these reach 400. The drift-guard test is the structural fix:
the number cannot drift without a failing test. (Verified live:
`cargo test -p kf-code --lib config_field_count_drift_guard` →
1 passed; asserts `CONFIG_FIELD_COUNT == 107`, `ENV_OVERRIDE_EXPECTED
== 96`, `MERGE_TOML_EXPECTED == 100`, and the serde-serialized field
count.)

## Root cause

The reviewer estimated without counting. The repo has a documented
`CONFIG_FIELD_COUNT` const with a five-layer invariant test precisely
because uncounted config surfaces drift. The reviewer's number is
~2.9× the real count.

## Approach

No code change. This WO records the audit. If the reviewer's score
needs a written response, the table above (file:line + the
drift-guard test citation) is the response. The "400" claim should
not be repeated without a recount.

If a *real* config-knob reduction is later desired (the score 7/10
is partly a perception problem, partly a real surface), the candidates
are the 15 struct fields intentionally skipped by `merge_toml_into_config`
(mod.rs:2628-2637) — e.g. `summarize_enabled`, `opencode_zen_api_key`,
`docker`, `sandbox` sub-fields. These are config-file-only fields with
no env override and no CLI flag; several (`summarize_enabled`,
`subagent_allowed_models`, `opencode_zen_*`) have no production wiring
and are deletion candidates. That is a separate WO.

## Files

- `src/shared/config/mod.rs:49` — `CONFIG_FIELD_COUNT = 107` (WO 45.37:
  108 → 107 via the `artifact_policy` enum replacing two bools).
- `src/shared/config/mod.rs:22-45` — breakdown comment + WO history
  (const bumped 98 → 107 across WOs 27.1 / 30.0.6 / 32.18 / 32.13 /
  39.2 / 41.1 / 43.17 / 44.22 / 45.37).
- `src/shared/config/model.rs:86-183` — `ModelConfig` (34 fields)
- `src/shared/config/security.rs:38-98` — `SecurityConfig` (22 fields)
- `src/shared/config/tools.rs:114-191` — `ToolConfig` (35 fields)
- `src/shared/config/session.rs:86-99` — `SessionConfig` (8 fields; was 9
  pre-WO-45.37)
- `src/shared/config/display.rs:33-55` — `DisplayConfig` (7 fields)
- `src/session/config/env_overrides.rs:12-628` — 96 env vars
- `src/session/config/mod.rs:1222-1446` — drift-guard test (4 invariants:
  struct const, merge_toml key count, env-var count, serde field count)
- `src/cli.rs:42-205` — 23 `#[arg]` on the `Run` subcommand; `src/cli.rs`
  total 62 `#[arg]` across all subcommands

## Gate

No code change. The audit is verified by the existing test:
```
cargo test -p kf-code --lib config_field_count_drift_guard
```
Ran it: `1 passed; 0 failed; 0 ignored; 3887 filtered out` (cold build,
25m29s; the test itself runs in 0.10s). Asserts `CONFIG_FIELD_COUNT ==
107`, `ENV_OVERRIDE_EXPECTED == 96`, `MERGE_TOML_EXPECTED == 100`, and
serde-serialized field count == 107. This WO records that the test
enforces the count; no code was added or changed.

## Done

Audit verified against the code. The external reviewer's "400 config
knobs" claim is refuted: the real count is **139 distinct config
surfaces** (107 struct fields + 96 env vars − ~64 overlap where an env
var shadows a struct field; the reviewer's framing triple-counts the
same setting across struct/env/CLI). The drift-guard test
`config_field_count_drift_guard` (`src/session/config/mod.rs:1222`)
enforces all four invariants — struct const, serde field count,
merge_toml key count, env-var count — so the number cannot drift
without a failing test.

Correction applied to this WO file: an earlier draft recorded 108
struct fields / 140 total / `SessionConfig=9` / `mod.rs:46`. That was
stale by one WO cycle (WO 45.37 replaced two `SessionConfig` bools
with one `artifact_policy` enum, dropping the count to 107). Updated
the Verdict, Evidence §1, the SessionConfig breakdown bullet, the
Total line, and the Files citations to match the live const at
`mod.rs:49` and the passing drift-guard test. No code changed.

Docs updated: `docs/TECHNICAL.md` — added a short config-surface-count
paragraph under the `shared/` Config section recording 107 / 96 / 62 /
139 and citing the drift-guard test, so the next reviewer doesn't
re-estimate from a `grep pub` and reach the inflated number again.

Final gate (this WO, no code): `scripts/test-fast.sh` green, `cargo fmt
--check` green. The drift-guard test itself passes (run isolated).

## Rejected candidates

- **"Reduce config knobs to improve the score"** — rejected. The
  count is already 108, not 400. Reducing further to chase a
  perception score would remove real functionality (the 5 sub-structs
  each gate a documented feature area). The drift-guard test is the
  right structural answer, not a knob purge.
- **"Count CLI flags + env vars + struct fields as a single 'knob'
  number"** — rejected as a methodology. A struct field with an env
  override and a CLI flag is ONE knob with three access paths, not
  three. The reviewer's framing triple-counts the same setting.
- **"Merge the 5 sub-structs back into one flat `Config`"** — rejected.
  The sub-struct split is the load-bearing organizational unit
  (`ModelConfig`/`SecurityConfig`/`ToolConfig`/`SessionConfig`/`DisplayConfig`
  each map to a feature area with its own default fns and tests).
  Flattening would not change the field count; it would only make the
  file harder to navigate.