# WO 46.10 — context-index mtime_rebuild misses new files not in cache

## Status: Planned

## Problem

**Location:** `crates/kf-context-index/src/cache.rs:131-203`

mtime_rebuild only checks files in cached.file_mtimes; NEW files added to the repo are never detected. Latent (production uses incremental_rebuild). Fix: also scan the directory for files not in the cache.

## Root cause

See Problem section — identified by the post-WO-45 area audit + external review.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

crates/kf-context-index/src/cache.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

—
