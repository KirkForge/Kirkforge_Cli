# WO 46.12 — check-artifact-consistency.sh grep -c || echo 0 produces double output — false failure under set -e

## Status: Planned

## Problem

**Location:** `scripts/check-artifact-consistency.sh:90`

grep -c ... || echo 0 produces 0\n0 on no matches, causing [ -eq ] to throw under set -e (false failure). Fix: use grep -c ... || true or a proper count check.

## Root cause

See Problem section — identified by the post-WO-45 area audit + external review.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

scripts/check-artifact-consistency.sh

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

—
