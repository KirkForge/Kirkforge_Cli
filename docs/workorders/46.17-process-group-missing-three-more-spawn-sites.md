# WO 46.17 — Process group missing in 3 more subprocess spawn sites (CRITICAL)

## Status: Planned

## Problem

**Location:** `src/session/verifier/correction.rs:310, src/session/verifier/security.rs:224, src/tools/bash.rs:178`

Same class as WO 46.2, missed sites: (1) apply_command_fix (sh -c $formatter) — hung formatter keeps file/pipe open past kill_on_drop; (2) trufflehog_scan — Go detectors may spawn helpers that survive; (3) docker mode — container processes outlive the timeout. Fix: add setup_process_group to all three.

## Root cause

Identified by external model audits (DeepSeek + MiniMax). See
/home/henrik/madlab/github/desktop/ds-bugs.md and mm-bugs.md for full evidence.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/session/verifier/correction.rs, src/session/verifier/security.rs, src/tools/bash.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

—
