# WO 46.26 — handle_bench_run_models silently exits 0 on 0/N pass rate — CI blind

## Status: Planned

## Problem

**Location:** `src/main/handle_bench.rs:93-144`

WO 38.10 added the 0/N guard to handle_bench_run (line 84-89) but handle_bench_run_models was missed. Every-fails-everything run exits 0; CI cannot detect it.

## Root cause

Identified by external model audits (DeepSeek + MiniMax). See
/home/henrik/madlab/github/desktop/ds-bugs.md and mm-bugs.md for full evidence.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/main/handle_bench.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

—
