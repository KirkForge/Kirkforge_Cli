# WO 46.27 — mtime_rebuild/incremental_rebuild silently drop cached embeddings

## Status: Planned

## Problem

**Location:** `crates/kf-context-index/src/cache.rs:174-175,96`

Both call from_symbols_and_edges_and_calls which constructs a fresh ContextIndex { embeddings: Vec::new() }, defeating WO 38.9 item 5's caching invariant. Fix: use Self::from_cached so cached embeddings survive.

## Root cause

Identified by external model audits (DeepSeek + MiniMax). See
/home/henrik/madlab/github/desktop/ds-bugs.md and mm-bugs.md for full evidence.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

crates/kf-context-index/src/cache.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

—
