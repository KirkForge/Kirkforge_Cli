# WO 46.30 — bench run_task env-var leak on error paths

## Status: Done

## Problem

**Location:** `src/session/bench.rs:228-324`

KF_CODE_BUDGET_CEILING set at line 229; cleanup at line 323 only on success. Any ? between leaks it to next task. run_token_budget_challenge runs 5 in a row — first failure poisons the rest. Fix: BudgetEnvGuard Drop struct.

## Root cause

Identified by external model audits (DeepSeek + MiniMax). See
/home/henrik/madlab/github/desktop/ds-bugs.md and mm-bugs.md for full evidence.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/session/bench.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

- Added `BudgetEnvGuard` (RAII, private to bench.rs): set on
  construction, Drop restores the prior value or unsets. Bound as
  `_budget_guard` right where `run_task` exported the ceiling; the old
  success-path-only `remove_var` block deleted. Covers every `?` exit
  between set and cleanup (`create_dir_all`, `ConversationLog::open`,
  `Executor::with_log_and_undo`), timeout, and unwind.
- Restore-prior (not blanket remove) — the old code clobbered a
  user-set global `KF_CODE_BUDGET_CEILING`; the guard puts it back.
- The shared `test_util::EnvGuard` and the kf-bench test-file guard are
  both `#[cfg(test)]`-only — production code needed its own; reusing
  them would have required un-gating a test util for prod, a bigger
  diff than the 25-line local guard.
- Test: `budget_env_guard_unsets_on_drop_and_restores_prior_value`
  (throwaway var name — config tests mutate the real var under a
  module-local ENV_LOCK this test cannot share).
- Gate: test-fast + clippy -D warnings + fmt --check + check
  --workspace --all-targets, all green (see session log).
