# WO 46.39 — Doc drift batch: CLI about, lib.rs path, test-doctor refs, test count, stale Cargo.lock, install.sh dead paths

## Status: Planned

## Problem

**Location:** `src/cli.rs:27, src/lib.rs:3, crates/kf-testdoctor/src/lib.rs:4, docs/TECHNICAL.md:56, crates/kf-testdoctor/Cargo.lock, scripts/install.sh:114`

Batch of doc/stale-reference fixes: (1) CLI --help says 'Native Ollama CLI' instead of provider-agnostic; (2) lib.rs:3 says binary at src/main.rs (actually src/main/mod.rs); (3) 4 refs to docs/ideas/test-doctor.md (now docs/archive/ideas/); (4) TECHNICAL.md test count off by ~620; (5) stale Cargo.lock in kf-testdoctor; (6) install.sh installs dead plugins/npm paths.

## Root cause

Identified by external model audits (DeepSeek + MiniMax). See
/home/henrik/madlab/github/desktop/ds-bugs.md and mm-bugs.md for full evidence.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/cli.rs, src/lib.rs, crates/kf-testdoctor/src/lib.rs, crates/kf-testdoctor/main.rs, docs/TECHNICAL.md, crates/kf-testdoctor/Cargo.lock, scripts/install.sh, docs/adr/0029-test-partitioning.md

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

All six fixes applied (verified against current code first — none were
already fixed):

1. `src/cli.rs:27` — about → "Provider-agnostic, verification-first coding
   agent — static binary, TUI, cloud-routed models" (matches Cargo.toml
   description; man page regenerates from the same Cli via build.rs).
2. `src/lib.rs:3` — binary path corrected to `src/main/mod.rs`.
3. All 4 `docs/ideas/test-doctor.md` refs repointed at
   `docs/archive/ideas/test-doctor.md` (kf-testdoctor lib.rs:4 + main.rs:6,
   ADR-0029 lines 13 + 47).
4. `docs/TECHNICAL.md:56-58` — test counts updated to grep-verified values:
   ~5,000 total (~3,950 src/ = 2968 `#[test]` + 983 `#[tokio::test]`;
   ~1,010 crates/ = 933 + 81). Sentence now says the `#[test]`-only count
   is what `readme_drift` pins (931 in README, drift 2 ≤ fudge).
5. `crates/kf-testdoctor/Cargo.lock` — `git rm`'d (workspace member shares
   the root lock); `crates/*/Cargo.lock` added to `.gitignore`.
6. `scripts/install.sh` — deleted the dead `plugins/` + `npm/` blocks AND
   the `DATA_DIR`/`PLUGIN_DIR` scaffolding (lines 111-146) that existed only
   to serve them; release.yml ships `bins=(kf-code)` only (RELEASE.md
   confirms no plugins/npm in artifacts; runtime resolves `plugins_dir()`
   from `data_dir()` itself). `sh -n` clean.

DEFERRED: `## Status` header left at Planned. Flipping it to Done requires
the matching `docs/workorders/README.md` index-row edit (forbidden to this
worker); flipping the header alone would redden
`wo_status_headers_match_readme_index`. Merge step: flip header + index row
together.
