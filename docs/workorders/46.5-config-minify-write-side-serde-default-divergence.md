# WO 46.5 — minify_write_side serde default true vs Default false — config construction paths disagree

## Status: Done

## Problem

**Location:** `src/shared/config/tools.rs:27-34 vs 220`

default_minify_write_side() returns true (serde default), but manual Default impl sets minify_write_side: false. Config-from-TOML gets true, Config::default() gets false. edit_file envelope-expansion mismatches depending on construction path. Fix: set line 220 to minify_write_side: default_minify_write_side() so both paths agree.

## Root cause

See Problem section — identified by the post-WO-45 area audit + external review.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/shared/config/tools.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

Root cause: `da7524f6` (2026-08-13) flipped the serde default
`default_minify_write_side()` false→true to preserve token savings and
fix the edit_file envelope round-trip, but missed the manual `Default`
impl (line 220) AND two stale test preconditions that encoded the OLD
default (`test_env_minify_write_side`, `test_merge_toml_minify_write_side`
both asserted `!cfg.tools.minify_write_side`). So Config-from-TOML got
`true`, `Config::default()` got `false` → edit_file envelope-expansion
mismatched depending on construction path.

Direction: aligned to `true` (the intended default per `da7524f6`,
`config.toml.example:205` "Default: true", and WO 38.13 "minify_write_side
now true"). Reverting to `false` would regress the deliberate
token-savings/round-trip fix.

Files touched:
- `src/shared/config/tools.rs:220` — `false` → `default_minify_write_side()`
- `src/session/config/mod.rs` — `test_env_minify_write_side` restructured
  (assert default true; env override to false; assert false)
- `src/session/config/merge.rs` — `test_merge_toml_minify_write_side`
  restructured (assert default true; merge false; assert false)
- `src/tools/read_file.rs:117,339` — stale "(default)" comments dropped
  (referred to the old false default)

Deferred (disclosed, not silent): ADR-005:173 and ADR-053:53 still claim
"`(default false)`". This is pre-existing staleness from `da7524f6` (not
introduced by this WO) and ADR amendment is WO 38.13 doc-truth territory.
Tracked in `state.md` pending.

Not changed (test fixtures, not default-claims): `src/tools/mod.rs:348,403,440`,
`src/session/bench.rs:172`, `src/tools/read_file.rs:359` explicitly set
`minify_write_side: false` to exercise the disabled path — they are
fixtures, not default-claims.
