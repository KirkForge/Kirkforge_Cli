# WO 46.9 — bench compare --fail-on-regression bypasses telemetry shutdown via std::process::exit(1)

## Status: Done

## Problem

**Location:** `src/main/handle_bench.rs:182`

std::process::exit(1) on regression skips shutdown_telemetry() at cli_dispatch.rs:240, losing OpenTelemetry span data. The sibling handle_bench_run path correctly uses anyhow::bail!. Fix: replace process::exit(1) with bail! so the normal shutdown path runs.

## Root cause

See Problem section — identified by the post-WO-45 area audit + external review.

## Approach

Read the Problem section's fix recommendation. Implement the minimal
correct fix. Add tests where practical.

## Files

src/main/handle_bench.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check`.

## Done

Replaced `std::process::exit(1)` at `handle_bench.rs:182` with
`anyhow::bail!("bench regression detected")`. The `eprintln!` above the
exit already prints the delta/threshold detail; `bail!` routes through
`handle_bench_command` → `cli_dispatch.rs:233` match → `.map_err(...)` at
238 → `shutdown_telemetry()` at 240 → `exit(e.exit_code())` at 247, so
OpenTelemetry span data flushes before the process exits with code 1
(`KirkForgeError::General`). Mirrors the sibling `handle_bench_run`
path (line 85) which already uses `bail!`.

One-line diff; no new deps, no new tests (error path already covered by
the classifier contract).
