# WO 47 — workflow_run template argument loads arbitrary workflow JSON via path traversal

## Status: Planned

## Problem

HIGH — sandbox escape. WorkflowTool::run (src/tools/workflow.rs:90) takes template from model tool-call JSON with only a non-empty check. find_workflow_file (crates/kf-workflow/src/discovery.rs:8) does PathBuf::join(format!("{name}.json")) — (1) an absolute template ('/tmp/evil') discards the base entirely (join semantics); (2) '../../..' segments walk out. The loaded file executes as a fully-trusted Workflow (spawns subagents, runs bash steps). Fix: validate template as a bare filename — reject '/', '\\', '..', and leading '/' before any PathBuf::join; same for the shared data-dir lookup.

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

crates/kf-workflow/src/discovery.rs, src/tools/workflow.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
