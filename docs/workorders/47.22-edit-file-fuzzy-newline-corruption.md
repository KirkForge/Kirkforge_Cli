# WO 47 — edit_file fuzzy fallback corrupts the trailing newline on multi-line matches

## Status: Planned

## Problem

HIGH — data corruption with a repro. src/tools/edit_file.rs:308-316: when old_string matches only after whitespace normalization AND spans ≥2 lines, byte_end = line_byte_end(last_line) includes the line's own \n (newline_positions[line_idx] + 1), so the replacement splices the following line onto the replacement's last line and drops the newline. Verified empirically (CRLF and LF) by DS. Existing CRLF tests only cover single-line fuzzy matches. Fix: line_byte_end(last) = newline_positions[last_line] (no +1); add a multi-line fuzzy CRLF regression test.

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

src/tools/edit_file.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
