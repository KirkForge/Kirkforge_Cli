# WO 47 — panic=abort makes every catch_unwind containment guard dead code in release — contract drift

## Status: Planned

## Problem

HIGH — honest-doc decision needed. [profile.release] panic = "abort" (Cargo.toml:364) means panics ABORT; the catch_unwind guards at verifier/bus.rs:169, handler.rs:130, executor/dispatch.rs:750, kf-budget-core/paths.rs:337 never fire in production. The documented contract 'a panicking tool returns a clean Failure' (dispatch.rs:741) is FALSE in release. Tests build with unwind so no test catches it. state.md pins keep-abort for binary size. Fix (pick one, keep docs honest): (a) remove panic=abort so catch_unwind works; or (b) keep abort, delete the dead wrappers, replace the contract comment with a ponytail:/ceiling: note. Either way fix the poison-abort mutexes (47.36) that turn recoverable panics into aborts.

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

Cargo.toml, src/session/verifier/bus.rs, src/session/executor/dispatch.rs, docs/TECHNICAL.md

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
