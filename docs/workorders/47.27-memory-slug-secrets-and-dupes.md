# WO 47 — Memory extractor: secrets land in filenames + same fact added up to 14× + LIKE wildcard escape

## Status: Planned

## Problem

HIGH. (1) src/session/memory/extract.rs:112-116 — make_slug takes 120 chars of user text; 'I prefer ANTHROPIC_API_KEY=sk-abc123' becomes a FILENAME under memory/ and the body carries the literal secret. Fix: strip URL-shaped, path-shaped, KEY=VALUE spans from slug input; [REDACTED] secret-looking spans in body/description. (2) extract_corrections/extract_user_preferences add the same fact up to 14× when multiple patterns match (mm-H21) — dedup before insert. (3) kf-memory-store sqlite.rs:386 LIKE query interpolates % and _ from the tag (matches prod-build for prod%) — add ESCAPE clause.

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

src/session/memory/extract.rs, crates/kf-memory-store/src/adapters/sqlite.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
