# WO 47 — HTTP MCP bearer token silently ignored + EventBus buffer identity collision

## Status: Planned

## Problem

MEDIUM. (1) src/session/mcp_client/http.rs:55-70 — McpServerConfig.bearer_token is parsed but never attached as an Authorization header; the client calls the remote MCP server unauthenticated. Fix: add `Authorization: Bearer <token>` to the SSE GET and POST when non-empty. (2) event_bus.rs emit removes the in-flight event by (sequence, kind) only, but identity/dedup uses (kind, stream_id, sequence, timestamp, value) — two streams sharing sequence+kind can remove each other's entry (GPT #6, mm P3). Fix: remove by the same identity hash used for idempotency.

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

src/session/mcp_client/http.rs, src/shared/event_bus.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
