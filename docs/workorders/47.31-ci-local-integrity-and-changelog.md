# WO 47 — ci-local failures[] integrity (tarpaulin bypass + compound run_step) + CHANGELOG 23 missing WO 46 entries

## Status: Done

## Problem

MEDIUM. (1) scripts/ci-local.sh:147-173 — the tarpaulin coverage gate uses `if ! python3; then exit 1` bypassing run_step/failures[] (a coverage regression hard-exits; the summary never records it). (2) run_step with a compound command (`run_step "x" a && b`) still aborts under set -e before failures[] updates (residual from the 46.25 fix — run_step must set +e around "$@" or document single-command-only). (3) CHANGELOG drift: only 8 of 39 WO 46.x entries made it into ## [Unreleased] (mm-C6; merge-resolution kept dropping agent CHANGELOG lines). Fix all three; backfill the 23 missing entries (one line each, WO-file has the list).

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

scripts/ci-local.sh, CHANGELOG.md

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

—
