# WO 47 — Mutex-poison + expect hygiene batch + lib unwrap gate + test EnvGuard fixes

## Status: Done

## Problem

MEDIUM batch. (1) Poison-abort sites: daemon/server.rs:516,553 + daemon/mod.rs:433 (instances Mutex .lock().unwrap()), bash_jobs.rs:155,286 (run_id), cli_dispatch.rs:38,45 (log writer — logging must never abort), kf-memory-store sqlite.rs (many). Fix: unwrap_or_else(|e| e.into_inner()) across these. (2) PRICING_TABLE.last().expect (shared/mod.rs:1074) — const table, use unwrap_or last-resort. (3) tui spawn_executor .expect (tui/mod.rs:864) — propagate instead. (4) src/lib.rs lacks #![cfg_attr(not(test), deny(clippy::unwrap_used))] that main/mod.rs has (kimi). (5) Test-guard fixes ride along with 47.21 (EnvGuard→DataDirGuard).

Source: multi-model audit vetting 2026-08-27 (mm-bugs.md, ds-bugs.md, kimi-bugs.md,
sonnet-bugs.md, gpt-bugs.md). Top-severity claims verified against the code at
dbfd8bb0 before filing.

## Root cause

See Problem — per-site. Cross-cutting theme: invariants enforced at one layer
(pre_run, session daemon) instead of the component that owns the operation;
and WO fixes applied at one site of a duplicated pattern.

## Approach

Read the Problem section. Implement the minimal correct fix. Where a fix
closes a class (scrub, gating, anchoring), grep for sibling sites of the same
pattern before considering it done.

## Files

src/daemon/server.rs, src/daemon/mod.rs, src/session/bash_jobs.rs, src/main/cli_dispatch.rs, src/shared/mod.rs, src/tui/mod.rs, src/lib.rs, crates/kf-memory-store/src/adapters/sqlite.rs

## Gate

`scripts/test-fast.sh` + `cargo clippy --all-targets -- -D warnings` +
`cargo fmt --check` + `cargo check --workspace --all-targets`.

## Done

- Poison-abort sites → `unwrap_or_else(|e| e.into_inner())`: daemon/server.rs
  (instances, 2 sites), daemon/mod.rs broadcast, bash_jobs.rs run_id (2),
  cli_dispatch.rs LogWriter write+flush (logging never aborts), kf-memory-store
  sqlite.rs (14). Sibling adapters file.rs (6) + in_memory.rs (6) left as-is —
  outside WO file list.
- PRICING_TABLE.last().expect → `unwrap_or(UNMAPPED_PRICING)` const $0
  fallback (shared/mod.rs).
- tui spawn_executor: returns `anyhow::Result<JoinHandle<()>>`, run_tui
  propagates with `?` — no startup panic on executor construction failure.
- src/lib.rs: `#![cfg_attr(not(test), deny(clippy::unwrap_used))]` added;
  36 surfaced lib-target unwraps converted to justified `expect`s
  (guarded invariants: peeked iterators, regex literals, checked-map hits,
  arm guards).
- Gate (branch wo/wo47.36): workspace check clean, clippy
  -p kf-code -p kf-memory-store --all-targets -D warnings clean, fmt clean,
  daemon:: 24/24, bash_jobs 27/27, kf-memory-store 45/45.
