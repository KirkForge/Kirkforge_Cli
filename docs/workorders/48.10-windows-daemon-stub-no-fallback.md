# WO 48 — Windows daemon stubs return Ok(None) with no disk fallback — --attach errors, --auto-resume no-ops despite help promising fallback

## Status: Done

## Problem

P2. src/daemon/client.rs:611 + cli.rs:113 — the Windows stub paths return Ok(None) where Unix falls back to the on-disk index; the 47.12-reworded help promises index fallback that Windows never does. Fix: implement the same session-index fallback behind cfg(windows), or correct the help text.

Source: post-WO-47 baseline audit (kf-code-review.md, 2026-08-28, HEAD e4725bd6).

## Root cause

See Problem. Cross-cutting theme: fixes applied at one layer/site of a duplicated
pattern (adapters, hooks, gates) leaving sibling sites divergent.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites of the same
pattern before considering it done.

## Files

src/daemon/client.rs, src/cli.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

Windows `try_list_recent`/`try_resolve_recent`/`try_resolve_id` in
`src/daemon/client.rs` (`windows_imp`) now serve the on-disk session index
(the same platform-neutral `session_index::list_sessions` /
`resolve_session_id` code the Unix WO 47.12 disk fallback uses, same
`RECENT_SESSIONS_LIMIT` cap) instead of returning `Ok(None)`. No daemon
exists on Windows, so the index is the only source — `--attach`,
`--auto-resume`, the startup picker, and the `/fork` picker now behave as
the 47.12 help text promises on all platforms. cli.rs needed no change:
its wording ("via the session daemon when running, else the on-disk
session index") is accurate post-fix. `windows_stub_tests` rewritten to
pin the new contract (list/resolve from an isolated `KF_CODE_DATA_DIR`).

Gates (fast, per owner directive): `cargo test -p kf-code daemon::client`
8/8 green (Unix side unchanged); `cargo clippy --target
x86_64-pc-windows-gnu -p kf-code --all-targets -- -D warnings` green
(compiles the changed `cfg(windows)` code + tests); `cargo check
--workspace --all-targets` green; `cargo fmt --check` green.
detect_changes: 1 file, 0 indexed symbols / processes affected (Linux
symbol graph does not include `cfg(windows)` bodies; Unix compiled
output identical).
