# WO 48 — Startup picker gate misses can_run_tui conditions — headless runs crash (os error 6)

## Status: Done

## Problem

P1. src/main/run_session.rs:216 — the picker gate checks only !non_interactive && !no_tui, missing prompt.is_none() && !term_dumb && stdout().is_terminal() that use_tui (:837-842) applies. Reproduced: headless kf-code run with prior sessions → 'No such device or address (os error 6)', exit 1. Also -p one-shots pop the full-screen picker, piped-stdout renders an invisible modal. Newly-reachable since 47.12's disk fallback. Fix: hoist one can_run_tui predicate used by both sites.

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/main/run_session.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

Hoisted one pure predicate `can_run_tui(no_tui, non_interactive, has_prompt,
term_dumb, stdout_is_tty)` mirroring `use_tui`'s exact conjunction; computed
once as `tui_capable` before log-path resolution and used by both the picker
gate and the TUI/line-mode branch. Headless / -p / piped-stdout / TERM=dumb
runs now fall through to the existing hint arm instead of the picker. Unit
test `run_session::tests::can_run_tui_requires_every_condition` pins the
conjunction. Gates: check/clippy/fmt green, 34/34 bin tests pass.
