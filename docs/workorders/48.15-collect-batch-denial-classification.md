# WO 48 — collect_batch pattern-matches body-produced AccessDenied as gate denial — skips record_tool_result

## Status: Done

## Problem

P2. src/session/executor/dispatch.rs:638-668 — the AccessDenied early-record branch (WO 15.7) was meant for pre-run gate denials but also catches body-produced denials (write_file's TOCTOU re-check, body guards), skipping record_tool_result: doom-loop breaker blinded, metrics/post-tool hooks/budget slice skipped. Same class 48.5 fixed for MCP. Fix: distinguish gate-denial (pre-dispatch) from body-denial (post-run) — only the former takes the early-record path.

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/session/executor/dispatch.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

`ToolResult` (dispatch-internal alias) gained a trailing `gate_denied: bool`.
The two Phase-2.5 gate sites (symlink walk, read-before-edit check) insert
`true`; every body-result site inserts `false`. `collect_batch`'s early-record
branch now matches `(gate_denied=true, AccessDenied)` — gate denials keep the
WO 15.7 2.8 fast path (no double denial, no guard re-check), body-produced
denials fall through to `record_tool_result` so metrics, post-tool hooks, the
budget slice, corrections, and the doom-loop breaker see them. Ordering
unchanged (Phase 3 still records in input order). 2 tests added
(`body_denied_write_file_still_runs_post_tool_hook`,
`gate_denied_edit_file_skips_post_tool_hook`). Sibling-site grep: `ToolResult`
alias confined to dispatch.rs (5 constructors, 1 destructure — all updated);
non-file body results already route through `record_tool_result` in
spawn_batch's collect loop. Fast gates green.
