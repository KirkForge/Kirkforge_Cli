# WO 48 — reload_config never re-pushes set_response_format — hot-reload with json_mode=false now DELETES a live response format

## Status: Done

## Problem

P2. src/session/executor/mod.rs:597 — reload_config pushes set_json_mode but not set_response_format; combined with 48.6's toggle-off clearing, a config hot-reload with json_mode=false wipes an explicitly configured json_schema format. Fix: push response_format on reload too (read the config's current value).

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/session/executor/mod.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

reload_config now re-pushes `set_response_format(fresh.model.effective_response_format())`
after `set_json_mode` — same order as construction (src/session/executor/mod.rs),
so an explicit json_schema survives a hot-reload with json_mode=false.
Regression test `reload_with_json_mode_false_keeps_explicit_json_schema`
(executor tests/turn.rs, local FormatRecordingAdapter mirroring the 48.6
toggle-off semantics). Fast gates green: workspace check --all-targets,
clippy -p kf-code --all-targets -D warnings, fmt, targeted turn tests 10/10.
Note (not fixed, out of file scope): the adapter-swap path never re-pushes
json_mode/response_format onto the freshly built adapter — pre-existing,
predates 48.6.
