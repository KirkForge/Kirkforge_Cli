# WO 48 — Every non-nav key leaks through the open picker modal — 48.4 fixed k/j only

## Status: Done

## Problem

P2. src/tui/keys/mod.rs:330-385 — keys other than the nav set (letters, F-keys, palette) pass through the open picker into the hidden input box / tab switches. Same class 48.4 fixed for k/j; the help-overlay handler (:1034) shows the intended consume-all standard. Fix: picker consumes all keys while open except its advertised bindings + Esc/Enter.

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/tui/keys/mod.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

2026-08-28 (worktree `wo48.20`, branch `wo48.20`). `handle_session_picker_keys`
(src/tui/keys/mod.rs) now consumes every key while the modal is open — the
trailing `None` (fall-through to search/palette/F-key/input handlers) became
`Some(Ok(()))`, matching the help-overlay consume-all standard. `consumed`
(from `picker.handle_key`) is demoted to the mark_dirty decision; the
advertised bindings (↑/↓/k/j, Enter, Esc/q) and the Ctrl+C exit check are
unchanged. Regression test `session_picker_consumes_non_nav_keys` drives the
full `handle_input_key` dispatcher: letter 'x' does not reach the input box,
F(6) does not switch tabs, modal stays open. Fast gates green: workspace
check --all-targets, clippy -p kf-code -D warnings, tui::keys 53/53
(+2 session_picker targeted), fmt, adr_xref_drift.
