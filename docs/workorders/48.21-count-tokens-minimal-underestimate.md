# WO 48 — count_tokens minimal-build off-arm (bytes/4) under-estimates 25-50% on code/CJK — feeds context-fit truncation ladder

## Status: Done

## Problem

P2. src/session/prompt/mod.rs:31 — the no-budget off-arm divides bytes by 4; code/CJK real ratio is 2.5-3. Feeds truncate_to_budget/compaction ladders (mod.rs:618/895, loop_.rs:441) — minimal builds can hit unrecoverable provider context-overflow because truncation under-fires. Fix: conservative divisor (3) or char-count heuristic for CJK-heavy content; document the ceiling.

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/session/prompt/mod.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

Branch `wo48.21` (2026-08-28), commit `fix(wo48.21): count_tokens fallback — CJK-aware estimate`.

- `count_tokens` no-budget arm now calls `fallback_estimate_tokens`:
  non-CJK bytes / 4, plus 1 token per CJK char (ranges `0x2E80..=0x9FFF`,
  `0xF900..=0xFAFF`, `0xFF00..=0xFFEF`). Pure ASCII unchanged (bytes/4);
  pure CJK goes 0.75 → 1.0 tokens/char. `ponytail:` ceiling comment on
  the fn (±20% mixed-script, ext-B+ counts via bytes; upgrade path: BPE
  table in minimal builds). BPE (`budget`) arm untouched — default builds
  bit-identical.
- Tests (`#[cfg(not(feature = "budget"))]` in prompt/mod.rs `mod tests`):
  `count_tokens_fallback_cjk_counts_at_least_one_per_char`,
  `count_tokens_fallback_ascii_unchanged`,
  `count_tokens_fallback_mixed_bumps_cjk_only` — 3/3 green under
  `cargo test -p kf-code --no-default-features --lib`.
- Gates: `cargo test -p kf-code --lib -- count_tokens estimate_message_tokens`
  3/3; `cargo check --workspace --all-targets` green;
  `cargo check --workspace --no-default-features` green;
  `cargo clippy -p kf-code --all-targets -- -D warnings` green;
  `cargo fmt --check` green. gitnexus detect_changes: 1 file, low risk,
  0 affected symbols.
- DISCOVERED (pre-existing, NOT fixed here — out of scope, needs own WO;
  kin to the WO 48.9 deferred no-default-features CI job): under
  `--no-default-features`, (a) `tests/context_economics_test.rs` does not
  compile (un-gated `kf_budget_core`/`session::budget`/`session::stratum`/
  `kf_compress_core` imports) — so `cargo test --no-default-features` fails
  at build; unit tests must be run with `--lib`. (b) `cargo clippy
  --no-default-features --lib -- -D warnings` is red on 4 pre-existing
  errors in untouched files (`skills.rs:174` unused_mut,
  `executor/helpers/mod.rs:31` dead `apply_budget_slice`,
  `executor/turn.rs:742`+`831` redundant binding redefs). None are in
  prompt code; the new `fallback_estimate_tokens` lints clean.

