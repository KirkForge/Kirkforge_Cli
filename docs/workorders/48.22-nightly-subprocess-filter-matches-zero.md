# WO 48 — P0: nightly subprocess-lifecycle test(=bare_name) filter matches zero tests — the two timeout tests have NEVER run

## Status: Planned

## Problem

P0. .github/workflows/ci-nightly.yml — the subprocess-lifecycle job's nextest filter uses test(=bare_name) which requires MODULE-QUALIFIED names (nextest = is exact match); locally reproduced matching zero tests. Broken since WO 45.59 created the job; the job has been silently green on nothing for ≥4 nightlies. Fix: use the module-qualified names (session::bash_jobs::tests::test_timeout_reaps_child_and_preserves_partial_output etc.) or substring match; verify locally with the same nextest binary + filter before push.

Source: post-WO-48 sweep audit (kf-code-review.md, 2026-08-28, HEAD 9f498f2b).

## Root cause

See Problem. Theme: per-language minifier patches instead of one lexer pass;
gate conditions checked at one site but not siblings; nightly CI never validated.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

.github/workflows/ci-nightly.yml

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

—
