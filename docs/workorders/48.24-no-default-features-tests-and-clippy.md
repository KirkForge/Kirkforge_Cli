# WO 48.24 — no-default-features: test compilation + clippy

## Status: Done

Done (branch `wo/wo48.24`, fast gates green) — merged to main via
`d9e6c60f` (fix) + `db6904c9` (merge wave; sweep-3 findings +
state/CHANGELOG backfill). See `## Done

Merged to main: fix commit `d9e6c60f`, merge-wave commit `db6904c9`
(same commits also filed the sweep-3 WOs 48.25-48.30 and backfilled
state.md/CHANGELOG). Reported outcome (from the merge commit):

- `skills.rs`: `register_folded_skills` early-returns from the
  `kf-plugin-tools` block — no cfg-dependent `unused_mut`.
- `helpers/mod.rs`: `apply_budget_slice` wrapper deleted (pure 1:1
  forwarder; both callers inside `#[cfg(feature = "budget")]`, so its
  off-arm was unreachable dead code under the minimal build).
- `turn.rs`: both call sites call
  `crate::session::budget::apply_budget_slice` directly; the two
  `#[cfg(not(budget))] let outcome = outcome;` no-op rebinds dropped.
- Tests: `#![cfg(all(feature = "budget", feature = "stratum"))]` gate at
  `tests/context_economics_test.rs:10` and
  `tests/budget_wiring_test.rs:13` (verified present at HEAD) —
  `cargo test --workspace --no-default-features` now compiles without
  `--lib`, restoring the ADR-0017 minimal-build contract.
