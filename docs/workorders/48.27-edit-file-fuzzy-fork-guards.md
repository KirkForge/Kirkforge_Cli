# WO 48 — edit_file fuzzy fork bypasses block_edits AND diff_review — whitespace drift defeats --block-edits/--harden

## Status: Done

## Problem

P1. src/tools/edit_file.rs:275-353 — the whitespace-normalizing fuzzy fallback writes to disk with no block_edits check (the only check sits at :407 on the exact-match path) and skips ctx.diff_review (defaults true): empty-diff/wrong-ratio edits the exact path rejects are applied unreviewed. Confirmed FileEdit + on-disk write under block_edits=true. Fix: hoist BOTH checks above the exact/fuzzy fork so every write path passes them. Tests: fuzzy-match edit under block_edits=true denied; wrong-ratio fuzzy edit rejected by diff review.

Source: Sweep 3 (kf-code-review.md, 2026-08-29, HEAD ae7dbd57).

## Root cause

See Problem. Theme: scanner quote-state residuals from the 48.11-13 wave; guards placed on one fork of a path; one event type over on modal gating.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/tools/edit_file.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

Extracted both write guards (diff_review + block_edits) into `fn write_denied(...)` and routed BOTH write paths — exact-match tail and whitespace-normalizing fuzzy fallback — through it before `atomic_write`. Exact-path behavior preserved byte-for-byte (same order, conditions, messages).

Tests added (src/tools/edit_file.rs):
- `fuzzy_edit_blocked_under_block_edits` — fuzzy fork under block_edits=true → BLOCK_EDITS failure, file unchanged.
- `fuzzy_edit_empty_diff_rejected_by_diff_review` — fuzzy fork no-op edit → DIFF_REVIEW failure, file unchanged.
- Legitimate-fuzzy regression covered by existing `test_fuzzy_fallback_preserves_original_formatting` / `_whitespace_tolerant` / CRLF variants (ToolContext::new ⇒ diff_review=true, so they now exercise the hoisted guard passing).

Disclosure: the WO asked for a "wrong-ratio" fuzzy test; the ratio>10 branch of review_diff is unreachable via the fuzzy fork (the replaced span is derived from old_string, so deletions ≈ old_string lines, ratio ≈ 1) — the empty-diff branch is the live diff-review rejection on that path, which the second test exercises. Both checks run on every write path regardless.

Gates (worktree wo48.27): `cargo test -p kf-code --lib tools::edit_file` 48 passed / 0 failed; `cargo fmt --check` exit 0; `cargo clippy -p kf-code --all-targets -- -D warnings` exit 0; `cargo check --workspace --all-targets` exit 0. Commit only — no merge, no push.
