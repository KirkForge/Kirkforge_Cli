# WO 48 — Event::Paste bypasses every modal gate — inserts into hidden input under picker/help/approval

## Status: Planned

## Problem

P2. src/tui/mod.rs:1239/:1253 — paste events skip the consume-all modal gating 48.20 shipped for KeyEvents; apply_paste (app.rs:843) inserts into the hidden input buffer under the session picker, help overlay, and approval dialogs. Bracketed paste is enabled. Fix: gate both Paste arms on the same modal-active conditions (~2 lines + test).

Source: Sweep 3 (kf-code-review.md, 2026-08-29, HEAD ae7dbd57).

## Root cause

See Problem. Theme: scanner quote-state residuals from the 48.11-13 wave; guards placed on one fork of a path; one event type over on modal gating.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/tui/mod.rs, src/tui/app.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

—
