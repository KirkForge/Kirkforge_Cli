# WO 48 — CJK estimate misses Hangul; ruby \' escape gap re-enables the 48.13 corruption class

## Status: Done

## Problem

P2. (1) src/session/prompt/mod.rs fallback_estimate_tokens — Hangul (0xAC00..=0xD7AF) missing from the CJK ranges; Korean text under-estimated 25%+. Add the range + test. (2) src/shared/minify/lang.rs ruby scanner — a backslash-escaped quote inside a single-quoted string (\') is not honored, closing the string early and re-enabling the comment-stripping corruption 48.13 fixed. Fix + round-trip test.

Source: Sweep 3 (kf-code-review.md, 2026-08-29, HEAD ae7dbd57).

## Root cause

See Problem. Theme: scanner quote-state residuals from the 48.11-13 wave; guards placed on one fork of a path; one event type over on modal gating.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

src/session/prompt/mod.rs, src/shared/minify/lang.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

Both items shipped on branch `wo/wo48.29` (not merged):

1. `fallback_estimate_tokens` (`src/session/prompt/mod.rs`) now counts Hangul
   syllables 0xAC00..=0xD7AF and jamo 0x1100..=0x11FF one-per-char alongside
   the CJK ranges. Test `count_tokens_fallback_hangul_counts_one_per_char`
   (cfg `not(feature = "budget")`, run under `--no-default-features`).
2. `ruby_scan_code` (`src/shared/minify/lang.rs`) skips backslash-escape
   pairs in single-quoted strings too (previously double-quote-only), so
   `\'` no longer closes the literal early and swallows a real heredoc
   marker. `shell_heredoc_opens` keeps the double-quote-only rule — correct
   for bash, where single quotes have no escapes. Test
   `test_minify_ruby_escaped_quote_keeps_heredoc_open` (heredoc-body `#`
   survival + minify/expand round trip).

Gates: workspace check --all-targets, workspace check
--no-default-features, clippy -p kf-code -D warnings, targeted tests,
adr_xref_drift, fmt — all green (see workplan/commits).
