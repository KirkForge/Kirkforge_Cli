# WO 48 — Drift test can't detect duplicate WO rows; h2 RUSTSEC-2026-0258 untracked

## Status: Done

## Problem

P2. (1) crates/kf-budget-core/tests/adr_xref_drift.rs — the README-index parse folds duplicate rows silently (BTreeMap); add a uniqueness assertion so the dup-row merge residue that happened 3 times gets caught at gate time. (2) h2 advisory RUSTSEC-2026-0258 — check cargo audit / the lockfile; bump or add the advisory exception per repo policy. Both mechanical.

Source: Sweep 3 (kf-code-review.md, 2026-08-29, HEAD ae7dbd57).

## Root cause

See Problem. Theme: scanner quote-state residuals from the 48.11-13 wave; guards placed on one fork of a path; one event type over on modal gating.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites.

## Files

crates/kf-budget-core/tests/adr_xref_drift.rs, Cargo.lock

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

2026-08-29, branch `wo/wo48.30` (not merged).

**(1) Dup-row guard**: `wo_status_headers_match_readme_index` folded
`parse_wo_index_table` rows into a `BTreeMap` (last status per WO number
wins), so duplicate README index rows were invisible. Added
`duplicate_wo_nums(&rows) -> Vec<String>` + an assert (fails naming the
dup numbers) + synthetic test `wo_index_duplicate_rows_are_detected`
injecting the historic dup shape — verified by construction, no scratch
README needed. Crate README `Tests` row 925 → 946 (row was stale; actual
`#[test]` count under `crates/` post-change).

**(2) h2 verdict**: lockfile carried h2 0.4.15 — inside the
RUSTSEC-2026-0258 affected range. Bumped to 0.4.19 (latest compatible,
patched). Lockfile-only: the h2 entry's version+checksum pair, applied
surgically because a full `cargo update -p h2` also re-pinned 16
unrelated `windows-sys` edges (resolver normalization; h2's own dep list
is unchanged between 0.4.15 and 0.4.19, so the 2-line lock is
internally consistent — proven by the `--locked` gate, which verifies
the checksum on download).

Gates: `cargo test -p kf-budget-core --test adr_xref_drift` 7/7 green;
`cargo check --workspace --all-targets --locked` green (17m31s, fresh
worktree); `cargo clippy -p kf-budget-core --all-targets -- -D warnings`
green; `cargo fmt --check` green. Commits a104e7cf (guard), d1150a67
(h2), this file.
