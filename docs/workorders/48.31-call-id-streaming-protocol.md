# WO 48 — call_id on the streaming event protocol — concurrent same-type tools interleave with no identity

## Status: Planned

## Problem

HIGH. src/session/executor/types.rs:80/84/159 — ToolStart{name,args}, ToolResult{name,...}, BashPartialOutput(String) carry no call identity; dispatch runs non-file tools via tokio::spawn concurrently, PTY chunks flow during the body, and the TUI appends to back()/name-matched placeholders (46.35's name matching is the disclosed ceiling). Concurrent bash A/bash B interleave chunks into one card. FIX (architectural, per external review): add call_id: String to ToolStart/ToolResult/BashPartialOutput; every emitter stamps it (dispatch spawn, turn record, pty.rs reader — the PTY channel needs the call id threaded to the reader); TUI keeps HashMap<call_id, message_index> for chunk routing; replay/turn_events carry it (serde default so old traces still parse). This is the fix for the WO-10-era streaming problem class.

Source: external review (ChatGPT source-level, 2026-08-29) — claims verified at file:line before filing. Already-tracked overlaps excluded: verifier worker blocking (state.md pending / 47.14 remaining), parent-dir TOCTOU (documented openat2 ceiling).

## Root cause

See Problem. Cross-cutting: concurrent things lack identity; cancellable things lack an owner; expensive things lack bounds — the review's invariant table.

## Approach

Read the Problem section. Minimal correct fix; architectural where named (48.31 is the event-protocol fix, do it properly, not another heuristic).

## Files

src/session/executor/types.rs, src/session/executor/dispatch.rs, src/session/executor/turn.rs, src/session/bash_runner/pty.rs, src/tui/events.rs, src/main/turn_events.rs, src/session/replay.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

—
