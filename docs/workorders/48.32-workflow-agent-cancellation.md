# WO 48 — Workflow agent steps spawn with cancel: None — Esc/job timeout fires but the subagent keeps running and spending tokens

## Status: Planned

## Problem

HIGH. src/tools/workflow.rs:452 and :602 — run_task(TaskRequest{ cancel: None, ... }) while bash/tool steps receive the runner's child token. Esc or a scheduled-job timeout cancels bash/tool steps but the agent step's LLM loop continues; jobs/runner.rs then reports 'workflow timed out' while the subagent still runs. FIX: bridge the runner's CancellationToken into the TaskRequest cancel pair at the workflow→TaskSpawner boundary (reuse the existing TaskCancel derivation — one bridge, no new abstraction).

Source: external review (ChatGPT source-level, 2026-08-29) — claims verified at file:line before filing. Already-tracked overlaps excluded: verifier worker blocking (state.md pending / 47.14 remaining), parent-dir TOCTOU (documented openat2 ceiling).

## Root cause

See Problem. Cross-cutting: concurrent things lack identity; cancellable things lack an owner; expensive things lack bounds — the review's invariant table.

## Approach

Read the Problem section. Minimal correct fix; architectural where named (48.31 is the event-protocol fix, do it properly, not another heuristic).

## Files

src/tools/workflow.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

—
