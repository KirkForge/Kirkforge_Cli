# WO 48 — max_turns unbounded + glob walker not cancel-aware — model-controlled resource without ceilings

## Status: Done

## Problem

MEDIUM ×2. (1) src/tools/task/task_tool.rs:167 — max_turns from model args has NO clamp: .max(1) only floors it; a model can request u64::MAX turns. Add a config ceiling (default generous, e.g. 32; config tools.max_subagent_turns) — clamp at the tool layer. (2) src/tools/glob.rs — cancel races spawn_blocking(walk); cancellation returns but the walker keeps consuming blocking-pool until it finishes. Make the walker loop check an Arc<AtomicBool> set on cancel (check per directory entry — cheap); pass it through the walkdir closure. Same for the grep fallback walker if present.

Source: external review (ChatGPT source-level, 2026-08-29) — claims verified at file:line before filing. Already-tracked overlaps excluded: verifier worker blocking (state.md pending / 47.14 remaining), parent-dir TOCTOU (documented openat2 ceiling).

## Root cause

See Problem. Cross-cutting: concurrent things lack identity; cancellable things lack an owner; expensive things lack bounds — the review's invariant table.

## Approach

Read the Problem section. Minimal correct fix; architectural where named (48.31 is the event-protocol fix, do it properly, not another heuristic).

## Files

src/tools/task/task_tool.rs, src/tools/glob.rs, src/shared/config/tools.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

(2026-08-29, branch `wo/wo48.34`, commits `edc312f2` + `15b528ba` + docs)

1. **max_turns ceiling**: new `ToolConfig.max_subagent_turns`
   (default 32 via pub const `DEFAULT_MAX_SUBAGENT_TURNS`, merge-clamped
   1–1024 alongside the other numeric knobs). Threaded through
   `ToolContextBuilder` into `Task::with_config` (new 4th param);
   `Task::run` clamps the model arg with `.min(ceiling)` —
   `u64::MAX` reaches the spawner as 32. `CONFIG_FIELD_COUNT` 108 → 109;
   all 7 builder literal sites updated (run_session, persona ×2,
   task_spawner, bench, tools tests ×3). Tool def description now
   discloses the clamp to the model. Tests:
   `task_max_turns_clamped_to_configured_ceiling` (999999/u64::MAX → 32,
   5 → 5, 1 → 1), `task_default_ctors_clamp_max_turns`, merge clamp
   tests, defaults test.
2. **cancel-aware walkers**: glob's walk extracted to
   `walk_glob_matches(base, glob_set, max, guard, cancel: &AtomicBool)`
   with a per-entry flag check; `run()`'s cancel select arm flips the
   `Arc<AtomicBool>` so the blocking-pool thread stops at the next entry
   instead of walking the tree to completion. Same flag + per-entry
   check in both `fallback_search_blocking` walker loops (grep's
   non-rg path); its cancel arm flips the flag too. Tests:
   `walk_glob_matches_cancel_flag_stops_walk` (pre-set flag → 0 matches
   vs 5-match control), `glob_cancelled_token_returns_cancelled`,
   `fallback_search_cancel_flag_stops_walk` (flag → no matches vs
   2-match control).

Gates (fast, per WO): `cargo check --workspace --all-targets`,
`cargo clippy -p kf-code --all-targets -- -D warnings`, targeted tests
(task_tool 24/24, glob 15/15, grep 34/34, shared::config 9/9,
session::config 105/105), `cargo fmt --check` — green. Full suite +
Windows cross gate owed to CI before merge.
