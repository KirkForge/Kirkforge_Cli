# WO 48 — untrusted_content delimiter is breakable (closing tag in content, truncation cuts the tag) — document as mitigation, harden the wrap

## Status: Done

## Problem

MEDIUM (docs + hardening). The 47.35 wrap is defense-in-depth, NOT a trust boundary: fetched content containing '</untrusted_content>' syntactically terminates the region; central truncation can cut the closing tag. FIX: (1) docs/comments state plainly: delimiter is a prompt-injection mitigation; permissions/sandbox/approval remain the boundary (TECHNICAL.md + the system.hbs sentence). (2) Cheap hardening: neutralize any literal '</untrusted_content>' occurrences inside the payload before wrapping (replace with '<\/untrusted_content>'); if truncation would cut the closing tag, drop the tag and mark [truncated] instead.

Source: external review (ChatGPT source-level, 2026-08-29) — claims verified at file:line before filing. Already-tracked overlaps excluded: verifier worker blocking (state.md pending / 47.14 remaining), parent-dir TOCTOU (documented openat2 ceiling).

## Root cause

See Problem. Cross-cutting: concurrent things lack identity; cancellable things lack an owner; expensive things lack bounds — the review's invariant table.

## Approach

Read the Problem section. Minimal correct fix; architectural where named (48.31 is the event-protocol fix, do it properly, not another heuristic).

## Files

src/tools/web_fetch.rs, src/tools/read_file.rs, src/session/prompt/template.rs, docs/TECHNICAL.md, prompts/system.hbs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> + targeted tests + fmt. CI verifies the full suite.

## Done

Two commits on `wo/wo48.35` (not merged/pushed):

1. `34b0a538` — hardening at the two named vectors. (a) `wrap_untrusted`
   (web_fetch.rs, the shared helper) neutralizes payload-borne literal
   closing tags (`</untrusted_content>` → `<\/untrusted_content>`), so
   only the wrapper's own close can end the region; new inverse
   `unwrap_untrusted` (pub(crate)) for the truncation seam. (b) The seam
   is the central truncation site `truncate_tool_output`
   (executor/helpers/mod.rs — NOT the wrap helper: tools have no config
   access and WO 47.33 deliberately moved caps to the executor), Success
   + FileContent arms: wrapped content over the cap is cut at payload
   level, closing tag DROPPED, region ends `...\n[truncated]` (house
   marker, audit.rs:985). Generic truncation path byte-identical;
   FileContent sets `truncated: true`. Scope note: helpers/mod.rs is the
   truncation site this WO's Problem names. read_file.rs needed no edit
   (change is helper-internal; its wrap-shape tests stay green). Tests:
   wrap neutralize + unwrap round-trip (web_fetch), 3 truncation tests
   (helpers): Success cut, FileContent flag, under-cap verbatim.
2. `db15ed6a` — honesty docs. system.hbs bullet states plainly: the tags
   are a mitigation, not a trust boundary (permissions, sandbox,
   approval gates are), oversized regions end `[truncated]` without a
   closing tag. Both template.rs goldens re-captured with the same
   tag-free literal text (template-edit contract; golden tests enforce
   byte-equality). TECHNICAL.md tools section gains the mitigation
   paragraph + both hardening behaviors.

Gate (fast, per WO): `cargo check --workspace --all-targets` clean;
`cargo clippy -p kf-code --all-targets -- -D warnings` clean; targeted
tests green — web_fetch 72/72, read_file 18/18, template 9/9, helpers
truncate_tool_output 12/12; `cargo fmt --check` clean. Full suite +
Windows cross gate owed to CI before merge (branch not pushed).

gitnexus impact (dev index): `wrap_untrusted` LOW (3 prod call sites,
not indexed); `truncate_tool_output` HIGH position risk (every tool
result flows through it) — mitigated: wrapped-only branch, generic path
pinned by existing tests unchanged.
