# WO 48 — minify shell/ruby have no cross-line quote state — multi-line string contents deleted on write-back

## Status: Done

## Problem

P1. src/shared/minify/lang.rs — minify_shell and minify_ruby are line scanners with no cross-line string state: a multi-line "..."/'...' literal spanning lines loses #-leading body lines (treated as comments) and blank lines collapse — deleted on the default-on read→envelope→write-back chain. WO 48.11's own spec said 'outside strings' — the string half never shipped for these two languages (python/js got it in 48.1/48.12). Fix: carry quote state across lines in both scanners (the 48.1 string_char idiom, adapted to the FIFO heredoc structure). Tests: multi-line string with # lines round-trips (sh + rb); heredoc-inside-string and string-inside-heredoc both correct.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/lang.rs

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

Quote state hoisted out of the per-line scanners into `minify_shell` /
`minify_ruby` (`shell_heredoc_opens` / `ruby_scan_code` now take
`&mut Option<char>`). New line branch in both minifiers, mirroring the
existing heredoc/pct verbatim branches: a quote open at line start makes
the line string content — emitted verbatim (no comment strip, no blank
collapse) while the scan advances quote state so the post-close remainder
can still open heredocs / %-literals. String and heredoc state stay
mutually exclusive: the heredoc-body branch runs first and never scans
for quotes (string opener inside a heredoc body is heredoc content), and
the scan skips `<`/`%` arms while a quote is open (heredoc opener inside
a string is string content). Unterminated literal = rest of file
verbatim (safe direction, `ponytail:` note alongside the `<<$VAR` one).

Tests (6 new, all in lang.rs):
- `test_minify_shell_multiline_string_hash_lines_round_trip` (also covers
  single-quote twin + minify→envelope→expand round trip)
- `test_minify_shell_heredoc_inside_string_is_content`
- `test_minify_shell_string_inside_heredoc_body_is_content`
- `test_minify_ruby_multiline_string_hash_lines_round_trip` (+ envelope)
- `test_minify_ruby_heredoc_inside_string_is_content`
- `test_minify_ruby_string_inside_heredoc_body_is_content`

Gates (fast, per WO): `cargo check --workspace --all-targets` green;
`cargo clippy -p kf-code --all-targets -- -D warnings` green;
`cargo test -p kf-code --lib shared::minify` 98/98 (one transient
non-scanner failure in a single run, 7/8 runs fully green — see
lessons.md); `cargo fmt --check` green. gitnexus impact: LOW (both
minifiers; chain = minify_content_by_ext → minify_source_impl →
minify_source/_safe).

Note: the WO file + README row were missing on this branch (they exist
on main at 0f0a90c0) — added here like WO 48.34 did; the integration
merge will see file/README conflicts (keep the Done version).
