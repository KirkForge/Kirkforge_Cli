# WO 48 — fallback_c_like missing backtick arm — Go raw strings / JS template literals corrupted on write-back

## Status: Planned

## Problem

P2. src/shared/minify/expand.rs:270 — fallback_c_like has no backtick arm: re-expansion inserts ;/{/, newlines INSIDE backtick literals (Go raw strings, JS template literals) because the re-indenter treats them as code. The minifier handles backticks correctly; the expander doesn't. Fix: track backtick state in fallback_c_like (and check fallback arms for other languages with raw-string syntax). Tests: Go raw string with braces/semicolons round-trips; JS template literal round-trips.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/expand.rs

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

—
