# WO 48 — fallback_python except-swallow + pass/else misindent + multi-line-string interior trims

## Status: Planned

## Problem

P2. src/shared/minify/expand.rs fallback_python — the re-indent heuristic: (1) code after an except block's last statement gets swallowed into the except block (silent None return path); (2) a lone pass re-indents to else-depth (IndentationError); (3) multi-line string interiors get trimmed. Fix: indent by the actual block openers (def/if/for/while/try/except/with/class push depth; dedent tokens pop), not statement-name heuristics; leave string interiors verbatim (needs the same string-state tracking as 48.37). Tests: try/except/else + pass + multi-line string round-trip.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/expand.rs

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

—
