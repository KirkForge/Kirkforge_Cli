# WO 48 — strip_test_blocks string-blind — column-0 #[cfg(test)] inside r#-strings deletes content

## Status: Done

## Problem

P2. src/shared/minify (strip_test_blocks) — raw/string content containing a column-0 #[cfg(test)] line gets those lines deleted when preserve_tests=false. Fix: track string state (incl. r#"..."# raw strings) while scanning for the test-module markers. Tests: r#-string containing the marker line survives; real test blocks still stripped.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

Code: af8f7749 (fix(minify): strip_test_blocks tracks string state).

`strip_test_blocks` (src/shared/minify/lang.rs) now carries string-literal state across lines while scanning: normal `"` strings (backslash escapes), raw `r#*"..."#*` strings (closed by `"` + N `#`, classified by lookback at the opening quote), `b"..."`/`br#*"`, char literals `'\x'`/`'x'` consumed inline (bare `'a` = lifetime, not a string), and `//` line comments stop the scan. Markers are only detected when the line start is outside strings; braces are only counted outside strings/comments (braces inside strings used to corrupt depth the same way).

Tests added: test_strip_test_blocks_marker_in_raw_string_survives, test_strip_test_blocks_marker_in_triple_quote_survives, test_minify_rust_raw_string_marker_round_trip.

Gates (branch wo/wo48.40, HEAD af8f7749): cargo check --workspace --all-targets clean (12m30s); cargo clippy -p kf-code --all-targets -- -D warnings exit 0 (fresh 3m18s); cargo test -p kf-code --lib shared::minify 95 passed / 0 failed (post-fmt rerun); cargo fmt --check clean.

Known ceiling: the strip pass trusts the quote structure of its input; the fallback first pass (minify_rust_inner) is not raw-string-aware — separate issue, not this WO.

