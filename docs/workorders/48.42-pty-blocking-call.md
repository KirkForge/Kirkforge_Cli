# WO 48 — --features pty: run_with_pty is a sync call in the async tool body — no timeout, no Esc abort, wedged turn

## Status: Planned

## Problem

P2. src/tools/bash.rs:442 — interactive bash calls run_with_pty directly (sync) in the async tool body; the tool timeout can't fire, Esc can't abort, the child is never killed, and dispatch.rs:411's plain handle.await wedges the turn on sleep infinity. Fix: tokio::task::spawn_blocking + select against the tool cancel token + timeout, with kill on all exit paths (the PTY feature is default-off but shipped — this is its correctness hole). Tests: PTY call with a cancel token returns Cancelled promptly.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/tools/bash.rs, src/session/executor/dispatch.rs

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

—
