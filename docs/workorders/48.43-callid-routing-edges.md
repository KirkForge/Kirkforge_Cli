# WO 48 — call_id routing edges: empty-id ToolResult steals a sibling's card; late PTY chunk after TurnComplete resurrects a streaming card

## Status: Done

## Problem

P2 ×2, REPRODUCED. src/tui/events.rs:244-245,664-677 — (1) an empty-call_id ToolResult (old-trace path) falls into the name/id matching and can remove a DIFFERENT streaming call's card; no map rebase → orphaned placeholder survives the full lifecycle. (2) A late BashPartialOutput arriving after TurnComplete resurrects a streaming card that nothing clears (events.rs:542-552,624). Fix: (1) empty call_id → legacy name-matching must skip calls with known ids (only match streaming cards whose call_id is empty); (2) on TurnComplete, clear ALL streaming state including late-arriving map entries (drop late chunks or finalize them). Reproduction probes exist in the sweep report.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

- src/tui/events.rs
- src/tui/app.rs (scope creep: `GenerationState.turn_finished` — the late-chunk
  drop needs a turn-boundary bit; mid-turn "map empty + nothing streaming +
  chunk must CREATE" is pinned by the WO 44.38 test
  `bash_partial_output_after_completed_card_does_not_corrupt_it`, while
  post-TurnComplete the same observable state must DROP, so the signal cannot
  be derived from existing state. Derives Default → no ctor edit.)

## Tests

- `legacy_tool_result_never_steals_call_id_card` (probe 1): legacy + id'd
  same-name placeholders live together; the empty-call_id ToolResult removes
  ONLY the legacy card, the survivor's map entry re-bases to the shifted
  index, chunks + the id'd ToolResult still route to the survivor, no orphan
  after the full lifecycle. Pre-fix: name-matcher's backwards scan takes the
  id'd card (newest match) — verified red by reverting the guard.
- `late_bash_partial_output_after_turn_complete_is_dropped` (probe 2): full
  lifecycle → TurnComplete → late same-id chunk is DROPPED (no ghost card,
  lands nowhere, map stays empty); a NEW turn's unknown-id chunk still
  creates its card (48.31 race-ahead contract intact). Pre-fix: ghost card
  (messages 2≠1) — verified red by reverting the guard.

## Gate

FAST GATES ONLY per owner directive (parallel worktree; full suite + Windows
cross run by the integrator): `cargo check --workspace --all-targets`
(Finished dev profile, clean), `cargo clippy -p kf-code --all-targets --
-D warnings` (Finished, clean), `cargo test -p kf-code --lib tui::events`
(55 passed / 0 failed — 53 prior + 2 new), `cargo fmt --check` (clean).
gitnexus impact `dispatch_turn_event`: HIGH by breadth (23 upstream, 19 are
the function's own unit tests; production path is drain_turn_events →
run_event_loop → run_tui). detect_changes N/A in worktree (index tracks the
main checkout — same as 48.42).

## Done

Both fixes in. (1) `remove_tool_placeholder` takes the registration map,
skips id-registered indexes (legacy pairs with legacy only) and returns the
removed index; both call sites (legacy arm + stale-index defensive fallback,
now extracted into `finalize_streaming_placeholder`) re-base
`streaming_tool_index` on removal. (2) New `turn_finished` bit on
GenerationState: TurnComplete sets it, Token/Thinking/ToolStart clear it;
`apply_bash_partial_output` (extracted from dispatch_turn_event per the
refactor-on-touch policy) drops unknown-call_id chunks while it is set — the
map-hit and empty-call_id legacy paths are untouched. Ceiling (ponytail at
site): a straggler arriving after the NEXT turn's first event re-creates a
card; per-id tombstones are the upgrade path.
