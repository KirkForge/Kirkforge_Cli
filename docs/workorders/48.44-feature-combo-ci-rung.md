# WO 48 — pty/devtools/computer_use/no-default-features combos compile but zero CI jobs build them

## Status: Planned

## Problem

P2. The feature matrix: pty ✓ compiles but 0 CI jobs build it (48.31's gates NEEDED the flag — proven fragile); devtools, devtools+computer_use, no-default-features same (48.9 disclosed). Fix: one nightly job running cargo check --workspace --all-targets for each combo (4 short check steps, one job — no test runs, just compile gates). This is the class that bit us: code that only compiles under a flag nobody builds rots silently.

Source: Sweep 4 (kf-code-review.md, 2026-08-29, HEAD 357ac36e). 1 P1 (cross-line quote state — the 48.11 spec's unshipped half), 6 P2 + 1 CI-rung gap. Two TUI findings are REPRODUCED with probes in the sweep report.

## Root cause

Line-scanner patches without cross-line state; teardown paths not covered by Drop; feature combos without CI rungs.

## Approach

Read the Problem section. Minimal correct fix.

## Files

.github/workflows/ci-nightly.yml

## Gate

Fast gates: cargo check --workspace --all-targets (+ the combo flags where named) + clippy -p <touched> + targeted tests + fmt.

## Done

—
