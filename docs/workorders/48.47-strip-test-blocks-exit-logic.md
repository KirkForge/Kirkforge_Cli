# WO 48 — strip_test_blocks exit needs a brace pair after entry — one-liner test mod empties the rest of the file; brace-less mod deletes the next fn

## Status: Done

## Problem

P1 ×2. lang.rs:442-538 — (1) a one-liner #[cfg(test)] mod tests {...} (no braces after the entry line) makes the scanner run to EOF deleting everything after; (2) brace-less #[cfg(test)]\nmod tests; deletes the following function. Fix: on entry, if the entry line itself contains the closing brace, consume just that line; for brace-less (line ends with ;), consume the two marker lines only. ALSO fold in the sweep-5 P2: block comments /* */ with odd quotes inside break brace counting (48.40 regression) — track block-comment state alongside string state.

Source: Sweep 5 (kf-code-review.md, 2026-08-29, HEAD fae3ada2). 3 P1 (minify rust/py entry-exit logic — the un-treated half of the 48.x scanner wave), 4 P2. All verified with adversarial round-trip harnesses.

## Root cause

Per-language scanner patches fixed quote-INTERIOR state but never entry/exit decision logic (docstring detection, raw-string entry, module-exit matching); teardown fail-open patterns recurring (channel vs Drop guard); byte-vs-column width.

## Approach

Read the Problem section. Minimal correct fix. The whole shared::minify suite (100+ tests) is the regression oracle.

## Files

src/shared/minify/lang.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p kf-code + cargo test -p kf-code --lib shared::minify + fmt.

## Done

Code: bfb88550 (fix(minify): strip_test_blocks entry/exit logic — one-liner + brace-less mods, block comments).

`strip_test_blocks` (src/shared/minify/lang.rs) entry/exit decisions fixed on top of the 48.40 string-state work: (1) the marker branch no longer `continue`s past the entry line — it sets suppress and falls through to the brace scan, so braces on the entry line count and a one-liner `#[cfg(test)] mod tests { ... }` closes on its own line instead of anchoring on the next function's braces and eating the rest of the file; (2) new exit — `in_test_block && !test_started && trimmed.ends_with(';') && no '{'` — a brace-less decorated item (`mod tests;`, two-line or one-liner form) is complete, consume the marker lines only; (3) `in_block_comment` tracked across lines: inside `/* */` only `*/` matters (quotes, char literals, braces, `//` are comment text — the 48.40 cross-line quote state no longer opens inside block comments), and marker detection additionally requires `!in_block_comment`.

Tests added: test_strip_test_blocks_one_line_mod_keeps_rest, test_strip_test_blocks_braceless_mod_keeps_next_fn, test_strip_test_blocks_block_comment_odd_quotes.

Gates (branch wo/wo48.47, HEAD bfb88550, machine under parallel-session load ~50): cargo check --workspace --all-targets exit 0 (15m46s); cargo clippy -p kf-code --all-targets -- -D warnings exit 0 (12m41s); cargo test -p kf-code --lib shared::minify 111 passed / 0 failed (incl. post-fmt rerun of the strip_test_blocks filter, 7/7); cargo fmt --check clean. gitnexus impact (pre-edit, dev index): MEDIUM — 2 production callers (minify_with_map, minify_rust_inner), 0 processes; detect_changes N/A (worktree edits invisible to the main-checkout index).
