# WO 48 — PTY kill channel fails OPEN on sender-drop — timeout/abort orphans the child (the 48.41 class, channel edition)

## Status: Done

## Problem

P2. pty.rs:56 + bash.rs:450 + dispatch.rs:783 — executor tool-timeout/JoinHandle-abort drops the tool future → kill_tx dropped without send → the watcher exits without killing → orphaned PTY child + leaked blocking thread. 48.42 used a channel; 48.41's WalkerCancel used a Drop guard — the guard is the right pattern here too: on watcher's rx.recv() returning None (sender dropped), KILL (fail closed), don't exit quietly. One-arm fix in the watcher loop.

Source: Sweep 5 (kf-code-review.md, 2026-08-29, HEAD fae3ada2). 3 P1 (minify rust/py entry-exit logic — the un-treated half of the 48.x scanner wave), 4 P2. All verified with adversarial round-trip harnesses.

## Root cause

Per-language scanner patches fixed quote-INTERIOR state but never entry/exit decision logic (docstring detection, raw-string entry, module-exit matching); teardown fail-open patterns recurring (channel vs Drop guard); byte-vs-column width.

## Approach

Read the Problem section. Minimal correct fix. The whole shared::minify suite (100+ tests) is the regression oracle.

## Files

src/session/bash_runner/pty.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p kf-code + cargo test -p kf-code --lib shared::minify + fmt.

## Done

Watcher arm in `run_with_pty` now fails closed: `let _ = kill_rx.recv(); let _ = killer.kill();` — channel-close (executor timeout drops the tool future; bash.rs's explicit send arms never run) kills exactly like an explicit message. Normal completion also drops the sender; the kill then HUPs an already-reaped pid (ESRCH, ignored). Test `pty_sender_drop_without_send_kills_child` (pty.rs tests mod) drops the sender without send and asserts the child dies before its marker deadline. Gate outputs in workplan.md session notes.
