# WO 48 — MCP tools/call isError:true classified as Success — doom-loop breaker blinded

## Status: Done

## Problem

P2. src/session/mcp_client/mod.rs:603-642 — a failing server tool (isError:true in the result) is classified Success; only the model sees the error text. Metrics, audit, and the doom-loop breaker all count it as success. Fix: map isError:true to the failure outcome shape (keep the text as the message).

Source: post-WO-47 baseline audit (kf-code-review.md, 2026-08-28, HEAD e4725bd6).

## Root cause

See Problem. Cross-cutting theme: fixes applied at one layer/site of a duplicated
pattern (adapters, hooks, gates) leaving sibling sites divergent.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites of the same
pattern before considering it done.

## Files

src/session/mcp_client/mod.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

`McpClient::call_tool` now routes its outcome through `apply_is_error_flag`:
when the result carries `isError: true`, a `Success` outcome is remapped to
`ToolOutcome::Failure(ToolError::Execution { message: <result text>,
exit_code: None, stderr: "" })` (same convention as web_fetch's non-2xx
mapping), so metrics, audit, and the doom-loop breaker see the failure. 4 unit
tests added (`apply_is_error_flag` + call sites). Sibling-site grep: single
`tools/call` sender in src/. Fast gates green.
