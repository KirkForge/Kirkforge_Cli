# WO 48 — Docs+CI gaps from 48.44: nightly job inventory stale in TECHNICAL/ADR-074; pty tests compiled but never executed

## Status: Done

## Problem

P2 ×2. (1) TECHNICAL.md:1994 CI table misses 4 nightly jobs (feature-combos + others); ADR-074's same-day amendment says 7, actual 8 — update both. (2) The feature-combos rung is check-only; add pty TEST execution: extend the rung with cargo nextest run -p kf-code --lib --features pty bash:: (the 48.42 regression tests) — bounded, fast. Also note the ruby %-underopen and go := items as tracked residuals in the WO.

Source: Sweep 5 (kf-code-review.md, 2026-08-29, HEAD fae3ada2). 3 P1 (minify rust/py entry-exit logic — the un-treated half of the 48.x scanner wave), 4 P2. All verified with adversarial round-trip harnesses.

## Root cause

Per-language scanner patches fixed quote-INTERIOR state but never entry/exit decision logic (docstring detection, raw-string entry, module-exit matching); teardown fail-open patterns recurring (channel vs Drop guard); byte-vs-column width.

## Approach

Read the Problem section. Minimal correct fix. The whole shared::minify suite (100+ tests) is the regression oracle.

## Files

docs/TECHNICAL.md, docs/adr/074*, .github/workflows/ci-nightly.yml

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p kf-code + cargo test -p kf-code --lib shared::minify + fmt.

## Done

Docs + workflow only — no Rust code touched, so the task-directed fast gates
(yaml parse + local pty run + fmt) replace the template's Rust gates:

- `docs/TECHNICAL.md` nightly row now lists all 8 actual jobs:
  coverage, ollama, subprocess-lifecycle, e2e-exhaustive, feature-combos,
  audit, mutants, release-build (was 4).
- `docs/adr/074` amendment: appended one-line correction — count is 8,
  `feature-combos` (WO 48.44) postdates the amendment. Amendment not rewritten.
- `.github/workflows/ci-nightly.yml` feature-combos: added cargo-nextest
  install + `cargo nextest run --locked -p kf-code --lib --features pty bash::`
  (runs the WO 48.42 regression tests; check-only compiled them but never
  executed).
- `docs/workorders/README.md`: 48.51 index row.

Gates (branch wo48.51):

- python3 yaml parse of ci-nightly.yml: OK (8 jobs).
- `cargo nextest run -p kf-code --lib --features pty bash::`:
  31 tests run: 31 passed, 4000 skipped — includes
  `bash_pty_pre_cancelled_token_returns_cancelled_promptly` (1.56s) and
  `bash_pty_timeout_returns_structured_timeout` (2.43s). Fresh pty-feature
  compile took >15 min locally; CI job reuses the nightly-feature-combos
  rust-cache after the check step, so the test step is incremental.
- `cargo fmt --check`: clean.

Tracked residuals (Sweep 5 items, out of this WO's scope, no WO yet):

- ruby `%-underopen` — `%`-literal open state across lines (per Sweep 5;
  the in-tree sweep doc is not available to quote mechanism from).
- go `:=` — expander fallback ':' arm pads Go's `:=` (`s := x` →
  `s : = x`); pre-existing, noted out of scope in WO 48.38 (lessons.md).
  Blind colon-arm edits are risky (48.26 pinned tests).
