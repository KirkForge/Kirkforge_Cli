# WO 48 — minify VFS cache ignores preserve_tests on read — test-stripped entries served to the every-turn stem

## Status: Done

## Problem

P2. src/shared/minify/mod.rs:147 — minify_source_safe (preserve_tests=true) returns entries cached by plain minify_source (preserve_tests=false). Test preservation is defeated in the stem-injection and bash-output minifier paths. Fix: key the cache on (path, preserve_tests) or bypass the cache when flags differ.

Source: post-WO-47 baseline audit (kf-code-review.md, 2026-08-28, HEAD e4725bd6).

## Root cause

See Problem. Cross-cutting theme: fixes applied at one layer/site of a duplicated
pattern (adapters, hooks, gates) leaving sibling sites divergent.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites of the same
pattern before considering it done.

## Files

src/shared/minify/mod.rs

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

Fixed in `minify_source_impl` (src/shared/minify/mod.rs): the cache read is now
gated on `!preserve_tests`, so `minify_source_safe` (stem-injection
`session/prompt/mod.rs`, `stream.rs`, bash-output `bash_minify.rs`) can no longer
be served a test-stripped entry cached by plain `minify_source` (mentions,
read_file). Matches the existing write-side design ("safe variants skip cache").
Regression test: `test_safe_variant_bypasses_test_stripped_cache`.

Gates (fast, per WO): cargo check --workspace --all-targets — clean;
cargo clippy -p kf-code --all-targets -- -D warnings — clean;
cargo test shared::minify — 73 passed, 0 failed; cargo fmt --check — clean.
