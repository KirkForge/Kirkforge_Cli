# WO 48 — --no-default-features build broken — 3 un-gated kf_budget_core refs (ADR-0017 documents this build as supported)

## Status: Done

## Problem

P2. src/session/prompt/mod.rs:24, src/tui/events.rs:571,577 reference kf_budget_core::estimate_tokens without the #[cfg(feature = "budget")] gate the other 11 sites have. ADR-0017:246 documents the minimal build; no CI job exercises it. Fix: gate the 3 sites; optionally add a check job for the combo.

Source: post-WO-47 baseline audit (kf-code-review.md, 2026-08-28, HEAD e4725bd6).

## Root cause

See Problem. Cross-cutting theme: fixes applied at one layer/site of a duplicated
pattern (adapters, hooks, gates) leaving sibling sites divergent.

## Approach

Read the Problem section. Minimal correct fix. Grep for sibling sites of the same
pattern before considering it done.

## Files

src/session/prompt/mod.rs, src/tui/events.rs, .github/workflows/ (optional)

## Gate

Fast gates: cargo check --workspace --all-targets + clippy -p <touched> +
targeted tests + fmt. CI verifies the full suite.

## Done

Branch `wo48.9`, 2026-08-28.

- `src/session/prompt/mod.rs::count_tokens` — body now cfg-gated:
  `budget` on → `kf_budget_core::estimate_tokens` (unchanged), off →
  `s.len() / 4` heuristic. Function stays un-gated because it has many
  un-gated callers (summarizer, carryover, memory, budget.rs); this is the
  single choke point. Stale doc comment ("always available in release
  builds") corrected.
- `src/tui/events.rs::estimate_messages_tokens` — both direct
  `kf_budget_core::estimate_tokens` calls rerouted through
  `session::prompt::count_tokens` (pub(crate), same crate). Dedups the
  estimator and removes the direct crate refs; no cfg needed in events.rs.
- Gates (FAST GATES ONLY per owner directive): `cargo check --workspace
  --all-targets` exit 0; `cargo check --workspace
  --no-default-features` exit 0 (previously E0658-class breakage — 3
  un-gated refs); `cargo clippy -p kf-code --all-targets -- -D warnings`
  exit 0; `cargo fmt --check` clean; targeted
  `estimate_message_tokens` tests 3/3 green.
- Known pre-existing minimal-build warnings (NOT introduced here, left
  alone): `unused_mut` in `src/session/skills.rs:174` and dead-code
  `apply_budget_slice` — both only appear with `--no-default-features`.
- DEFERRED: CI job for the `--no-default-features` combo. Why: adding it
  is a new job block (~10 lines) in `ci-merge.yml`, not the 3-line
  addition the scope allowed. Remaining: add a `cargo check --workspace
  --no-default-features` step/job to `.github/workflows/ci-merge.yml`
  (and ci-pr.yml). Tracked here + state.md Pending.
