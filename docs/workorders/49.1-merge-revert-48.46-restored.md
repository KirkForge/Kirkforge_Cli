# WO 49 — MERGE REVERT: dd72a4ca silently deleted 48.46's raw-string fix + 3 tests — restored

## Status: Done

## Problem

P0 (process). The 48.47 merge took the wrong conflict side and reverted 48.46's minify_rust_inner raw-string pass; the suite stayed green (tests deleted with the fix) and the WO row said Done. Caught by Sweep 6's final gate (adversarial round-trip matrix) + independently by the session sweep. RESTORED in this commit: raw-string arm + 3 round-trip tests re-applied via 3-way from 1432f351, verified coexisting with 48.45/48.47 scanners (121 minify tests). LESSON for next session: parallel branches editing the same file (lang.rs took 5 fixes in one wave) MUST be merged in numeric order with per-merge verification that prior fixes' unique symbols survive (grep the fix's marker symbol after each merge). The wholesale-take-theirs resolution pattern is unsafe for same-file waves.

Source: Sweep 6 closing audits (kf-code-review.md, 2026-08-29, HEAD 8601fb84).

## Root cause

See Problem — merge hygiene on same-file parallel waves; expand/minify asymmetry; filter module-path mismatch; one missing guard.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/lang.rs

## Gate

Fast gates per file. CI verifies.

## Done

Restored in the same commit as this WO file — see the 49.1 body.
