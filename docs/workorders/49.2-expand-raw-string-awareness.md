# WO 49 — fallback_c_like expand side raw-string blind — colon arm splices spaces into r#"..."# URLs

## Status: Planned

## Problem

P1. src/shared/minify/expand.rs fallback_c_like — the 48.26 colon arm pads inside raw strings on the expand side: r#"http://a//b"# → http: //a//b. Reachable via model-authored envelopes (edit_file.rs:163/172/179, write_file.rs:96). The minify side got raw-string awareness in 48.46; the expand side never did. Fix: port the open_string raw-string detection into fallback_c_like's string tracking (raw strings verbatim to closer). Test: raw-string URL round-trips; verified to break at 5d616afb even with the minify fix.

Source: Sweep 6 closing audits (kf-code-review.md, 2026-08-29, HEAD 8601fb84).

## Root cause

See Problem — merge hygiene on same-file parallel waves; expand/minify asymmetry; filter module-path mismatch; one missing guard.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/shared/minify/expand.rs

## Gate

Fast gates per file. CI verifies.

## Done
—
