# WO 49 — ci-nightly pty filter 'bash::' misses the 48.49 fail-closed kill test

## Status: Planned

## Problem

P2. .github/workflows/ci-nightly.yml:229 — the filter 'bash::' selects the 48.42 tests but not session::bash_runner::pty::tests::* (module path lacks 'bash::'). Fix: -E 'test(pty)' (substring, selects all pty tests incl. the kill test). Verify with nextest list.

Source: Sweep 6 closing audits (kf-code-review.md, 2026-08-29, HEAD 8601fb84).

## Root cause

See Problem — merge hygiene on same-file parallel waves; expand/minify asymmetry; filter module-path mismatch; one missing guard.

## Approach

Read the Problem section. Minimal correct fix.

## Files

.github/workflows/ci-nightly.yml

## Gate

Fast gates per file. CI verifies.

## Done
—
