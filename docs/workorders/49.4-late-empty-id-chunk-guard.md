# WO 49 — apply_bash_partial_output empty-call_id branch lacks turn_finished guard — late straggler resurrects a ghost card

## Status: Planned

## Problem

P2. src/tui/events.rs:630-642 — the empty-call_id (legacy) branch has no turn_finished early-return; a late PTY straggler after TurnComplete from an id-omitting population pushes a ghost streaming card nothing clears (the class 48.43 fixed for id'd chunks only). One early-return fix + test.

Source: Sweep 6 closing audits (kf-code-review.md, 2026-08-29, HEAD 8601fb84).

## Root cause

See Problem — merge hygiene on same-file parallel waves; expand/minify asymmetry; filter module-path mismatch; one missing guard.

## Approach

Read the Problem section. Minimal correct fix.

## Files

src/tui/events.rs

## Gate

Fast gates per file. CI verifies.

## Done
—
