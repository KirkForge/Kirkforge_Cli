// Scout persona tests. Split out from the former single-file `mod.rs`
// (WO 15.5). No scout-specific tests currently live here; the file exists
// to match the requested sub-module structure so future scout tests have
// a home.
