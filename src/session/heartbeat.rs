//! Stub module — deepseek's in-flight work referenced this from
//! `session/mod.rs` but the file wasn't created. Added at push time
//! so `cargo check` passes; the real implementation is a follow-up.

#![allow(dead_code, unused_imports)]
